-- Backfill ต้นทุนสินค้าย้อนหลังเข้า OrderItem.cost (user สั่ง 2026-08-08)
--
-- ที่มา: OrderItem.cost เป็น snapshot ที่เกิด "ตอนสร้างออเดอร์" เท่านั้น (feature 00016, FR-EXP-02)
-- ร้านที่เพิ่งมาตั้งราคาทุนให้สินค้าทีหลัง จึงมีออเดอร์เก่าที่ cost = NULL ค้างอยู่ทั้งกอง
-- ผลคือหน้า "ยอดขายและกำไร" กับการ์ด P&L มองไม่เห็นต้นทุนของช่วงก่อนหน้าเลย (กำไรสูงเกินจริง)
--
-- ขอบเขต (user เลือกเอง 2026-08-08 — "เติมเฉพาะช่องที่ยังว่าง ทุกใบย้อนหลัง"):
--   * เติมเฉพาะแถวที่ cost IS NULL — **ห้ามทับค่าที่เคยบันทึกไว้แล้ว** เพราะนั่นคือราคาทุน ณ วันขายจริง
--     ซึ่งเป็นข้อมูลที่ถูกต้องกว่าราคาทุนปัจจุบันเสมอ (historical accuracy, D-5) และทับแล้วกู้คืนไม่ได้
--   * เฉพาะบรรทัดที่ผูกกับสินค้าจริง (productId IS NOT NULL) และสินค้านั้น "มีต้นทุน" แล้ว
--     บรรทัดที่ผู้ขายพิมพ์เอง (productId = NULL) เติมไม่ได้ — ไม่มีอะไรให้อ้างอิง ปล่อย NULL ต่อ
--     ซึ่งฝั่งคำนวณตีความว่า "ยังไม่ตั้งต้นทุน" แล้วข้ามไป (ไม่ใช่นับเป็น 0) อยู่แล้ว
--   * ไม่กรองช่วงเวลา = ย้อนหลังทั้งหมด
--
-- ค่าที่เติมคือ **ราคาทุนปัจจุบัน** ของสินค้า ไม่ใช่ราคาทุน ณ วันที่ขายใบนั้น (ระบบไม่มีประวัติราคาทุน
-- ให้ย้อนดู) — เป็นค่าประมาณที่ดีที่สุดที่มี และดีกว่า NULL ซึ่งแปลว่า "ไม่รู้เลย"
--
-- idempotent: รันซ้ำแล้วไม่มีอะไรเปลี่ยน (รอบสองไม่เหลือแถว cost IS NULL ที่เข้าเงื่อนไข)
-- ไม่แตะ schema — เป็น data migration ล้วน ไม่มีการลบ/ล้างข้อมูลใด ๆ

UPDATE "OrderItem" oi
SET "cost" = p."cost"
FROM "Product" p
WHERE oi."productId" = p."id"
  AND oi."cost" IS NULL
  AND p."cost" IS NOT NULL;
