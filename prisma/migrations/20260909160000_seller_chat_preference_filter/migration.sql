-- 00018 ส่วนขยาย 2026-09-09 (รอบสอง) — ปุ่ม "บันทึกเป็นค่าเริ่มต้น" ในแผงตัวกรอง
-- user สั่ง: จำ "ทุกอย่างในแผง" (การเรียง + ตัวกรองทั้งชุด) ไว้ให้ผู้ใช้คนนั้น
--
-- additive ล้วน: คอลัมน์ใหม่ 1 คอลัมน์บนตารางที่เพิ่งสร้างวันนี้ ไม่แตะ CHECK/คอลัมน์เดิม
--
-- ทำไม JSONB ไม่ใช่คอลัมน์รายตัว: ชุดตัวกรองของกล่องแชทงอกมาเรื่อย ๆ (tags/shipment/readState
-- เพิ่มทีหลังทั้งหมด) ถ้าแตกเป็นคอลัมน์ ทุกครั้งที่เพิ่มตัวกรองใหม่จะต้อง migration ตาม และ
-- คอลัมน์ที่ถูกลบทิ้งจะค้างเป็นซาก. ค่านี้ไม่เคยถูกใช้ใน WHERE/ORDER BY ของ query ไหนเลย
-- (อ่านทั้งก้อนมาแปลงเป็น state ของหน้าจอ) จึงไม่เสียอะไรจากการไม่มี index
--
-- 🛑 ด่านความถูกต้องอยู่ที่ src/lib/inbox-filter-pref.ts (parse แบบ fail-closed รายฟิลด์)
-- ไม่ใช่ที่ฐาน — JSONB ไม่มี CHECK รายคีย์ให้ ค่าที่อ่านไม่ออกต้องตกไปค่าตั้งต้นเสมอ
-- ห้ามให้กล่องแชทพังทั้งหน้าเพราะค่าตั้งค้างจากเวอร์ชันเก่า
ALTER TABLE "SellerChatPreference" ADD COLUMN "inboxFilter" JSONB;
