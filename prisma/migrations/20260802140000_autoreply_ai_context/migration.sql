-- feature 00023 · เก็บว่า AI ใช้ข้อมูลอะไรประกอบการตอบครั้งนั้น (user สั่ง 2026-08-01)
--
-- "อยากให้แสดงเหตุผล Prompt ทั้งหมด ว่าใช้ข้อมูลอะไรประกอบการตอบข้อมูลนั้น ๆ ด้วย"
-- เดิมกล่องรายละเอียดบอกได้แค่ "AI แต่งจากคลังความรู้" ซึ่งไม่พอให้ร้านไล่ว่าทำไมตอบแบบนั้น
--
-- WARNING: ห้ามเก็บข้อความลูกค้าหรือ PII ลงคอลัมน์นี้ — เก็บเฉพาะ "ใช้อะไรบ้าง"
-- (จำนวน/ชื่อข้อในคลัง/สวิตช์ที่เปิดอยู่) ตัวข้อความดิบอยู่ที่ rawText ตามเดิมแล้ว
ALTER TABLE "AutoReplyLog" ADD COLUMN IF NOT EXISTS "aiContext" JSONB;
