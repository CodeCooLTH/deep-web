-- feature 00022 — ร้านที่เพิ่งเชื่อม iShip ต้องไม่ถูกถามเรื่องเปิดพัสดุตั้งแต่ออเดอร์แรก
--
-- เดิม default = 'ASK' ตั้งใจให้ "ถามก่อนเสมอ" เพราะการเปิดพัสดุมีค่าใช้จ่ายจริง
-- แต่ผลจริงคือร้านที่ยังไม่ได้ตั้งค่าอะไรเลยโดนถามทุกครั้งที่สร้างคำสั่งซื้อ —
-- คำถามที่ตอบ "ไม่" ทุกครั้งคือคำถามที่ไม่ควรถาม (user report 2026-07-29)
--
-- 'OFF' ยังรักษาเจตนาเดิมไว้ครบ (ระบบไม่เปิดพัสดุแทนร้านโดยไม่ได้สั่ง) และเข้มกว่าด้วยซ้ำ
-- ร้านที่ต้องการให้ถาม/ให้เปิดอัตโนมัติ เลือกเองได้ที่หน้าตั้งค่าการจัดส่ง
--
-- เปลี่ยนเฉพาะค่าตั้งต้นของแถวใหม่ — ไม่แตะร้านที่ตั้งค่าไว้แล้ว
ALTER TABLE "ShopShippingAccount" ALTER COLUMN "createMode" SET DEFAULT 'OFF';
