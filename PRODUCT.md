# Product

<!-- impeccable:product-schema 1 -->

> Deep (codename "SafePay") — แพลตฟอร์มสร้างความน่าเชื่อถือสำหรับการซื้อขายออนไลน์ C2C
> เอกสารนี้ตอบ "ใคร/อะไร/ทำไม" (strategic). เรื่อง "หน้าตาเป็นยังไง" (สี/ฟอนต์/component) อยู่ที่ DESIGN.md

## Platform

web

> Deep-App (แอปผู้ซื้อ Expo/React Native) เป็น **คนละ repo** และคนละ product record — ดู `## Capabilities and Constraints`. โปรเจกต์นี้ออกแบบเป็น web ล้วน ไม่ต้องอิง iOS/Android HIG. (ผู้ใช้ตัดสิน 2026-08-03)

## Register

brand

> เลือก brand เป็น default (ผู้ใช้ตัดสิน 2026-06-08): surface ที่นำคือฝั่ง public/landing/profile ที่ดีไซน์ = ตัวสร้างความเชื่อใจเอง (design IS the product).
> โค้ดเบสมีฝั่ง product app หนัก (dashboard buyer/seller/admin, OMS, ฟอร์ม, ตาราง) — **override register เป็น `product` per-task** เมื่อทำงานบน app UI เหล่านั้น (เช่น seller console, admin, order form). ฝั่ง `(marketing)/**`, `/u/[username]`, `/o/[token]`, landing → ใช้ brand register ตาม default.
> ยืนยันซ้ำ 2026-08-03: แม้ปริมาณโค้ดฝั่ง `(paces)` (คอนโซลผู้ขาย) จะแซงไปมากแล้ว แกนของ product **ยังเป็นระบบสร้างความน่าเชื่อถือตามเดิม** — register default จึงไม่เปลี่ยน.

## Users

ทุกคนมี trust profile เดียวกัน — ไม่แบ่ง buyer/seller แต่กำเนิด, เปิดร้านเพิ่มได้ด้วย `isShop` flag.

- **ผู้ซื้อ (buyer)** — คนทั่วไปที่ระแวงการโอนก่อนได้ของ. context: กำลังจะตัดสินใจซื้อจากคนแปลกหน้าบนโซเชียล/มาร์เก็ตเพลส อยากเช็กว่า "คนนี้เชื่อได้ไหม" ก่อนโอน. งานหลัก: ดู trust profile, ยืนยันรับของ, รีวิว. **ส่วนใหญ่เจอ Deep ครั้งแรกในฐานะ guest ที่ยังไม่มีบัญชี** ผ่านลิงก์ออเดอร์ที่ผู้ขายส่งให้.
- **ผู้ขาย (seller)** — พ่อค้าแม่ค้าออนไลน์ที่อยากพิสูจน์ตัวตน/ลดแรงเสียดทานปิดการขาย. context: power user ที่เข้าระบบบ่อย จัดการออเดอร์/สินค้า/verification/wallet. งานหลัก: สร้างออเดอร์ → ส่งลิงก์/SMS → ติดตามสถานะ.
- **ทีมงานร้าน (staff/admin ของร้าน)** — พนักงานที่ owner เชิญเข้ามาช่วยบริหารร้าน (feature 00012). context: ทำงานในคอนโซลเดียวกับ owner แต่สิทธิ์จำกัดกว่า.
- **แอดมิน Deep (internal)** — ทีมงานหลังบ้านที่รีวิวเอกสาร verification และอนุมัติ topup. context: งาน moderation เป็นชุด ต้องเร็วและแม่น. ไม่ใช่ end-user.

ฐานผู้ใช้กว้าง ภาษาไทยเป็นหลัก mobile-first และรวมกลุ่ม digital-literacy ต่ำ/ผู้สูงวัยที่ระวังเรื่องมิจฉาชีพเป็นพิเศษ.

## Product Purpose

แก้ปัญหา "โอนแล้วไม่ได้ของ / โดนมิจฉาชีพ" บนการซื้อขายออนไลน์แบบ C2C ด้วยการทำให้ **ความน่าเชื่อถือมองเห็นและตรวจสอบได้** ก่อนโอนเงิน — ผ่าน verification หลายระดับ (OTP → เอกสาร → จดทะเบียนธุรกิจ), Trust Score, Badge ที่ได้จากพฤติกรรมจริง, และประวัติออเดอร์ที่ลิงก์ข้ามแพลตฟอร์ม.

ความสำเร็จ = ผู้ซื้อกล้าโอนเพราะเห็นสัญญาณความเชื่อใจที่จริง, ผู้ขายสุจริตปิดการขายง่ายขึ้นเพราะพิสูจน์ตัวเองได้, มิจฉาชีพอยู่ในระบบนี้ไม่ได้เพราะสร้าง trust ปลอมไม่ได้.

เครื่องมือฝั่งผู้ขายทั้งหมด (ออเดอร์/POS, แชท, สต็อก, จัดส่ง, ประมูล, จอง, ทีมงาน) มีอยู่เพื่อ **ให้ประวัติที่ป้อน Trust Score เกิดขึ้นจริงในที่เดียว** — ไม่ใช่เป้าหมายในตัวเอง.

## Positioning

กลไกที่คู่แข่งลอกแบบตรง ๆ ไม่ได้: **Trust Score ของร้านประกอบขึ้นจากหลักฐานที่ร้านเขียนเองไม่ได้** — verification ที่แอดมินรีวิว, ออเดอร์ที่ **ผู้ซื้อ** เป็นคนกดยืนยันด้วยเบอร์ตัวเอง, รีวิวที่ผูกกับออเดอร์ที่ยืนยันแล้วเท่านั้น, และประวัติที่ตามผู้ซื้อมาเองเมื่อสมัครทีหลัง (`linkBuyerHistory` จับคู่ด้วยเบอร์/อีเมล). ตรงข้ามกับ badge ที่ซื้อได้หรือประกาศเอง.

ต่างจาก marketplace: Deep ไม่ได้เป็นที่ที่ผู้ซื้อมาเดินหาของ — ผู้ขายยังปิดการขายในช่องทางของตัวเอง (โซเชียล/แชท) แล้วใช้ Deep เป็นหลักฐานและเป็นที่บันทึก. Deep จึงแข่งกับ "ไม่มีอะไรเลย" มากกว่าแข่งกับ Shopee/Lazada.

**Free core ตลอดไป** เป็นส่วนหนึ่งของจุดยืน ไม่ใช่แค่กลยุทธ์ราคา: ถ้าต้องจ่ายเงินเพื่อสะสมความน่าเชื่อถือ ผู้ขายสุจริตรายเล็กจะถูกกันออก และสัญญาณ trust จะกลายเป็นสัญญาณของกำลังจ่าย.

## Operating Context

- **ผู้ขายทำงานบนมือถือเป็นหลัก** และปิดการขายในแชทของแพลตฟอร์มอื่นก่อน แล้วค่อยสร้างออเดอร์ตามหลัง — Deep จึงมีกล่องข้อความรวมศูนย์ (เชื่อมจริงแล้ว: Facebook Messenger + Instagram ผ่าน `ShopChannel.provider`) และช่องทางสร้างออเดอร์แบบเร็ว (POS `/orders/new`, วางข้อความที่อยู่แล้วให้ระบบแยกให้).
- **ร้านมี 3 ประเภท (`Shop.vertical`)** ที่เปลี่ยน workflow และเมนูทั้งชุด: `ONLINE_SALES` (ขายออนไลน์ — สต็อก/จัดส่ง/ประมูล), `SERVICE_QUEUE` (สินค้าและบริการ — คิวงาน/นัดหมาย), `LODGING` (บ้านพัก — ห้อง/การจอง). SSOT: `src/lib/lodging.ts`, `docs/20 - Features/00028 - Shop Business Type/`.
- **ลิงก์ออเดอร์สาธารณะ `/o/{token}` คือจุดสัมผัสแรกของผู้ซื้อ** — เปิดจากมือถือ ปลดล็อกด้วยเบอร์โทร ยืนยันรับของโดยไม่ต้องสมัคร. flow นี้ต้อง "ง่ายมาก ๆ" เป็น design constraint ระดับ product.
- **โปรไฟล์สาธารณะมี 2 เส้น:** `/u/{username}` (ทุกร้าน) และ `/b/{slug}` (เฉพาะบัญชี BUSINESS) — ใช้ component เดียวกัน แก้เส้นเดียวไม่พอ.
- **แยก subdomain:** main (buyer), `seller.*`, `admin.*` — session แยกกันแต่เป็นบัญชีเดียว. ผู้ใช้คนเดียวสลับระหว่าง Personal account กับร้านได้ผ่าน account switcher.
- **แอดมิน Deep ทำงานเป็นชุด:** คิวรีวิวเอกสาร verification (L2/L3) และคิวอนุมัติ topup พร้อมสลิป.
- **บริการภายนอกที่ผูกอยู่:** apitel (SMS/OTP), iShip (พัสดุ), Supabase (Postgres + storage), Vercel (deploy), Gemini (ผู้ช่วยร่างข้อความ/แชทบอท), Meta Graph API (Messenger/Instagram).

## Capabilities and Constraints

**โมเดลรายได้ (ของจริง ณ 2026-08-03):** free core ตลอดไป + ขายฟังก์ชันช่วยเหลือแบบ **à la carte** เท่านั้น — SMS Order Link ฿1/ข้อความ และ Inventory Add-on ฿199/เดือน หักจาก `SellerWallet`. **ยังไม่มีรายได้จริงจากผู้ใช้.** subscription tier ใน feature 00008 (Growth ฿159 / Pro ฿599 / Business ฿1,299) และ Verified Badge ฿299/เดือน ใน PRD §6 เป็น **เอกสารที่ยังไม่ได้ขายจริง** — ห้ามอ้างเป็นราคาที่ใช้อยู่ (ผู้ใช้ยืนยัน 2026-08-03).

**ข้อจำกัดที่งานอนาคตต้องรักษา:**

- **Free core ต้องทำงานได้ครบด้วยมือ** — สร้างออเดอร์/สินค้า, ส่งลิงก์เอง, ดู history, Trust Score, public profile. add-on ขาย "ความสะดวก" ไม่ใช่ขายความน่าเชื่อถือ.
- **ไม่แบ่ง role แต่กำเนิด** — ทุกบัญชีมี trust profile เดียวกัน, `isShop` เปิดร้านเพิ่ม.
- **เบอร์โทร immutable** — ตั้งได้ครั้งเดียว เปลี่ยนไม่ได้ เพราะมีผลต่อ Trust Score.
- **`Shop.slug` บังคับ + onboarding บังคับ** — ไม่มี slug หรือเบอร์ = ถูก force-redirect ไป `/onboarding` ทุก route.
- **`Shop.vertical` immutable หลัง onboarding จบ** — ตั้งได้ตอน `slug === null` เท่านั้น หลังจากนั้น 409 `VERTICAL_LOCKED`.
- **Trust Score MVP มีแต่ขึ้น** — ยังไม่มี penalty. สูตร: Verification 35% / Orders 25% / Rating 20% / Age 10% / Badges 10%.
- **ไม่มี email+password login (ตัดถาวร)** — buyer: Facebook / phone OTP; seller: username+password + phone OTP; LINE พร้อมใช้, Instagram ปิด flag อยู่.
- **Deep-App เป็น client ภายนอกที่พึ่ง API นี้** — แอปผู้ซื้อ Expo/React Native คนละ repo กินผ่าน `/api/app/*` ด้วย HMAC Bearer token (ไม่ใช่ cookie/NextAuth) และถูกยกเว้น CSRF Origin-check. **`/api/app/*` เป็นสัญญาห้ามพัง** — เปลี่ยน shape แล้วแอปที่ผู้ใช้ติดตั้งไว้จะเสีย. ดู `docs/buyer-app-api.md`.
- **ห้ามส่ง PII เข้า AI** — เบอร์โทร/อีเมล/ที่อยู่ ต้องไม่ถูกส่งเข้าโมเดลภาษา (feature 00019/00023).
- **UI เป็นภาษาไทยทั้งหมด** — ไม่มี i18n หลายภาษา; เอกสารภายในก็เป็นไทยเป็น default.
- **dev DB แยกจาก prod แล้ว** (ตั้งแต่ 2026-08-01) — ก่อนหน้านั้นใช้ฐานเดียวกัน และเคยถูกล้างทั้งฐานมาแล้วครั้งหนึ่ง (2026-07-31).

**คำศัพท์ที่มี SSOT ห้ามตั้งเอง:** *tier* ของ Trust (`docs/10 - Business Rules/Tier Lists.md`), *vertical* ของร้าน (3 ค่าเท่านั้น, `GENERAL` ถูกลบถาวร), *Personal account* vs *Business account*, *Trust Score* vs *badge* (คนละอย่าง).

**ยังไม่ตัดสิน (open):** ราคา/แพ็กเกจจริง, LINE OA และ TikTok เป็นช่องทางแชท (มีเอกสารแล้ว ยังไม่ลง schema), cross-platform stats บนโปรไฟล์สาธารณะ (ตอนนี้เป็น placeholder).

## Brand Commitments

- **ชื่อการค้าคือ "Deep"** — UI copy ทุกจุดใช้ Deep. "SafePay" เป็น internal codename เท่านั้น (repo, identifier, ตาราง DB) และต้องไม่โผล่ให้ผู้ใช้เห็น.
- **โดเมน:** prod `deepthailand.app`, dev `deepth.local`.
- **ฟอนต์ Anuphan ตัวเดียวทุก surface ทุก subdomain ทุกสกิน** (ยกเว้น monospace ใน code block + icon font) — `docs/conventions/anuphan-font.md`.
- **ห้าม emoji ใน UI ทุกจุด** — ใช้ icon จริงเท่านั้น. จุดที่ควรมี icon แต่สเปกไม่ระบุตัว ต้องถามก่อน ห้ามเดา — `docs/conventions/no-emoji-use-icons.md`.
- **สองสกินในโค้ดเบสเดียว:** buyer/landing/public = Vuexy (ม่วง `#7367F0`); seller/admin = Paces (น้ำเงิน `#236dc9`). ห้ามข้ามสีระหว่างสกิน.

## Brand Personality

**มั่นใจ · โมเดิร์น · เป็นมิตร** (confident, modern, friendly)

- **เสียง/โทน:** น่าเชื่อถือแบบ fintech ที่เข้าถึงง่าย ไม่ใช่สถาบันแข็งทื่อ. พูดกับคนเหมือนคนช่วยคน ไม่ใช่เอกสารราชการ. ภาษาไทยตรงไปตรงมา เข้าใจง่าย ไม่มีศัพท์เทคนิคเกินจำเป็น.
- **อารมณ์ที่อยากให้เกิด:** "อุ่นใจ" + "เชื่อได้" เป็นแกน — ผู้ใช้รู้สึกปลอดภัยพอจะกล้าโอน. credibility ที่อบอุ่น ไม่ใช่ credibility ที่ข่มขู่.
- **จุดยืน:** ความน่าเชื่อถือต้อง "แสดงให้เห็น" ด้วยข้อมูลจริง (verified, ประวัติออเดอร์, รีวิวจริง) ไม่ใช่ "ป่าวประกาศ" ด้วย badge ตกแต่ง.

## Anti-references

Deep **ต้องไม่ดูเหมือน**:

- **องค์กร/ธนาคารเย็นชา** — แข็งทื่อ เป็นทางการ น่ากลัว ฟอร์มเหมือนกรอกเอกสารราชการ ไม่มีความเป็นมนุษย์. (ความน่าเชื่อถือของ Deep ต้องอบอุ่น ไม่ใช่ข่มขู่)
- **เทมเพลต AI-SaaS โหลๆ** — ไล่สีม่วง, การ์ดตัวเลขใหญ่ (hero-metric template), กริดการ์ดไอคอนเหมือนกันเป๊ะซ้ำๆ, eyebrow ตัวพิมพ์ใหญ่จิ๋วเหนือทุก section, gradient text. หน้าตา default ของงาน AI generate.
- **crypto/web3 จัดจ้าน** — นีออนบนพื้นดำ, ไล่สีรุนแรง, copy โอเวอร์/ไฮป์, อนิเมชั่นเยอะเกิน.

> หมายเหตุ: ผู้ใช้ **ไม่ได้** เลือก "marketplace ดูไม่น่าไว้ใจ" เป็น anti-ref โดยตรง แต่โดยพันธกิจของ product (กันมิจฉาชีพ) การ์ด badge เกร่อ/เร่งเร้าปลอมๆ/รกแบบประกาศขายของเถื่อน ก็ขัดแกนแบรนด์อยู่แล้ว — หลีกเลี่ยงด้วย.

## Evidence on Hand

**ของจริงที่มี:**

- ข้อมูลบนโปรไฟล์สาธารณะ `/u/{username}` มาจาก DB จริงทั้งหมด — trust tier, verified chip, badges, สินค้า, avg rating, order count, completion rate. นี่คือรูปธรรมของ "show, don't tell".
- โลโก้ช่องทางจริงและ asset กลางอยู่ที่ `public/images/logos/` — ค้นก่อนสร้างใหม่ (`feedback_reuse_existing_shared_assets`).
- ธีมต้นทางที่ copy ได้จริง: `theme/vuexy/` (buyer) และ `theme/paces/` (seller/admin).
- เอกสาร feature 24 ชุดใต้ `docs/20 - Features/` — เป็น product truth ที่เขียนไว้แล้ว ไม่ต้องเดาใหม่.

**สิ่งที่ยังไม่มี และห้ามแต่งขึ้น:**

- **ไม่มี testimonial, case study, ตัวเลขลูกค้า, หรือ benchmark ที่ใช้อ้างได้.** landing เคยมีโลโก้บริษัทต่างชาติที่ติดมากับธีม (Netflix/Airbnb/…) — ถอดออกแล้ว 2026-07-04 **ห้ามใส่กลับ**.
- **ฐานผู้ใช้จริงยังเล็กมาก** — ณ 2026-08-02 prod มี User 10 คน. ห้ามออกแบบ copy หรือ social proof ที่บอกเป็นนัยว่ามีผู้ใช้จำนวนมาก.
- **ยังไม่มีรายได้จริงจากผู้ใช้** — ห้ามอ้างยอดขาย/MRR/จำนวนร้านที่จ่ายเงิน.
- สถิติ cross-platform และ on-time/response rate บนโปรไฟล์สาธารณะเป็น **placeholder ที่ติดป้าย "ตัวอย่าง"** — ห้ามทำให้ดูเหมือนข้อมูลจริง.

## Product Principles

1. **Trust ต้องแสดง ไม่ใช่ป่าวประกาศ (show, don't tell)** — credibility มาจากสัญญาณจริงที่ตรวจสอบได้ (verified chip, ประวัติออเดอร์, completion rate, รีวิวจริง) ไม่ใช่ badge ตกแต่งหรือ copy คุยโว. ทุก element ที่อ้างความน่าเชื่อถือต้องมีข้อมูลจริงหนุนหลัง.
2. **Credibility ที่อบอุ่น (warm credibility)** — น่าเชื่อถือได้โดยไม่เย็นชา. เป็นมิตรและเป็นมนุษย์เสมอ ไม่ใช่ฟอร์มธนาคาร. ความปลอดภัยสื่อผ่านความชัดเจน/legible ไม่ใช่ความเข้มงวดที่ข่มขู่.
3. **Mobile-first และเข้าถึงคนวงกว้าง** — ออกแบบสำหรับมือถือและกลุ่ม digital-literacy ต่ำ/ผู้สูงวัยก่อน: ภาษาไทยเรียบง่าย tap target ใหญ่ ภาระคิดต่ำ ไม่มีศัพท์เทคนิค.
4. **แบรนด์ที่ขอบ งานที่ทำให้หายไปตรงกลาง** — surface สาธารณะ (landing, `/u/[username]`, `/o/[token]`) แบกตัวตนแบรนด์เต็มที่; surface ใช้งานจริง (seller/admin app) ต้อง "หายไปในงาน" earned-familiarity ตามมาตรฐาน product ที่ดี.
5. **อินเทอร์เฟซเองต้องรู้สึกปลอดภัย (anti-scam by design)** — Deep แทนที่ความรู้สึกเสี่ยงของมาร์เก็ตเพลสมิจฉาชีพ ดังนั้นหน้าตาต้องตรงข้าม: สะอาด อ่านง่าย ไม่เร่งเร้าปลอม ไม่ซ่อนข้อมูลสำคัญ.
6. **เครื่องมือผู้ขายรับใช้ trust ไม่ใช่แทนที่มัน** — ทุกฟีเจอร์ที่เพิ่มเข้าคอนโซลผู้ขายต้องตอบได้ว่าทำให้ประวัติที่ป้อน Trust Score เกิดขึ้นครบขึ้นอย่างไร. ถ้าตอบไม่ได้ มันคือ scope creep ไม่ใช่ทิศทางใหม่ของ product.

## Accessibility & Inclusion

เป้าหมาย: **WCAG 2.1 AA + เข้าถึงพิเศษ** สำหรับกลุ่ม digital-literacy ต่ำ/ผู้สูงวัย.

- **AA baseline:** body contrast ≥4.5:1 (large/bold ≥3:1, placeholder ก็ 4.5:1), คีย์บอร์ดใช้งานได้ครบ, focus state ชัดทุก interactive, รองรับ `prefers-reduced-motion`, tap target ≥44px.
- **เข้าถึงพิเศษ:** ค่า default ขนาดตัวอักษร/spacing ใหญ่กว่าค่ามาตรฐานเล็กน้อย, copy ภาษาไทยเรียบง่ายมาก (อ่านออกแม้ไม่คุ้นศัพท์ออนไลน์), tap target ใหญ่, ลด jargon ให้เหลือน้อยสุด, ลำดับความสำคัญข้อมูลชัดเพื่อไม่ให้สับสน.
- **ฟอนต์:** Anuphan เท่านั้นทุก surface (ดู `docs/conventions/anuphan-font.md`) — รองรับไทยเต็มช่วงน้ำหนัก 100–700.
