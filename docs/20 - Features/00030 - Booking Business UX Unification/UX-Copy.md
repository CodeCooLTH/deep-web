---
title: "UX Copy Spec — 00030 Booking Business UX Unification"
owner: shinobu22
status: draft
module: M00030-BookingBusinessUXUnification
version: "1.0"
created: 2026-08-04
tags: [feature, ux-copy, wording, vertical, clarify]
related: ["[[PRD]]", "[[BRD]]"]
---

> **เอกสารนี้คืออะไร:** ผลของ `/impeccable clarify` บน copy path ของ feature 00030 — คำทุกคำที่ผู้ใช้เห็นใน order lifecycle และหน้าเลือกประเภทร้าน พร้อมข้อความจริงที่จะขึ้นบนจอ
> **ไม่ใช่:** สเปก layout/component (นั่นคือ `UX-Design-Spec.md` ที่ `safepay-ux` จะออกก่อน implement ตาม Hard Rule 8)
> **สถานะ:** คำศัพท์หลักผ่าน user แล้ว 2026-08-04 (§1) — ข้อความรายจุดใน §4/§7 รอ review

---

## 1. คำศัพท์ที่ล็อกแล้ว (user เคาะ 2026-08-04)

| # | Decision |
|---|----------|
| C-1 | **`SERVICE_QUEUE` = "การเข้ารับบริการ"** (ไม่ใช่ "ใบสั่งงาน" ที่โค้ดใช้อยู่) — มองจากมุมลูกค้า: เหตุการณ์ที่นัดไว้ ไม่ใช่เอกสารที่ร้านออก |
| C-2 | **`ONLINE_SALES` = "คำสั่งซื้อ" ทุกจุด** — เลิกใช้ "ออเดอร์" ทั้งหมด. ต่อยอดจากที่ user เคยสั่งไว้ 2026-07-23 ซึ่งตอนนั้นเปลี่ยนแค่แท็บเดียวในแชท |
| C-3 | **`LODGING` เก็บ 2 คำไว้ตามเดิม** — "บิลเข้าพัก" (`/orders`) และ "การจอง" (`/bookings`) เป็นคนละของจริง แต่ **ต้องเติมคำอธิบายความต่าง** ให้ผู้ใช้แยกออก (§6) |

**ผลข้างเคียงที่ต้องรับรู้:** C-2 ผ่อนกฎใน PRD §4.1 ที่เขียนว่า "ร้าน `ONLINE_SALES` ต้องไม่เห็นอะไรเปลี่ยนเลย" — ร้านขายออนไลน์จะเห็นคำเปลี่ยนจริงในบางจุด (`<title>`, ปุ่มสร้าง, toast) เพราะจุดเหล่านั้น**ไม่นิ่งอยู่ก่อนแล้ว**. ต้องอัปเดต PRD §4.1/§1.2 ให้ตรง — กฎที่เหลือคือ "เปลี่ยนไปหาคำที่ user เคาะแล้ว ไม่ใช่คำใหม่ที่ประดิษฐ์ขึ้น"

---

## 2. ปัญหาที่ clarify พบ: มี SSOT ของคำอยู่ 2 ที่ ที่ไม่รู้จักกัน

| | `ORDER_MENU_LABELS`<br>`src/lib/seller-menu.ts:353` | `VERTICAL_CTA`<br>`(chat)/inbox/[conversationId]/components/CustomerPanel.tsx:124` |
|---|---|---|
| ONLINE_SALES | คำสั่งซื้อ | ปุ่ม "สร้างออเดอร์" · แท็บ "คำสั่งซื้อ" · empty "ยังไม่มีประวัติออเดอร์" |
| SERVICE_QUEUE | **ใบสั่งงาน** | ปุ่ม "สร้างออเดอร์" · แท็บ **"คำสั่งซื้อ"** · empty "ยังไม่มีประวัติออเดอร์" |
| LODGING | **บิลเข้าพัก** | ปุ่ม "เปิดการจอง" · แท็บ **"การจอง"** · empty "ยังไม่มีประวัติการจอง" |

ทั้งสองก้อนมีคอมเมนต์อธิบายตัวเองว่าเป็นแหล่งความจริงเดียว และถูกเขียนคนละวันโดยไม่รู้ว่าอีกอันมีอยู่ — คอมเมนต์บน `ORDER_MENU_LABELS` เขียนว่า *"เลี่ยงคำว่า 'การจอง' สำหรับ LODGING โดยตั้งใจ"* ขณะที่อีกไฟล์ใช้คำนั้นเป๊ะ ๆ

**สิ่งที่ต้องทำ:** ยุบเป็น SSOT เดียว (`ORDER_MENU_LABELS` ขยายเป็น `ORDER_VOCAB`) แล้วให้ `VERTICAL_CTA` อ่านจากตัวนั้นแทนที่จะประกาศคำเอง — `VERTICAL_CTA` ยังคงอยู่ในฐานะตัวถือ `href`/`icon` ซึ่งเป็นเรื่องของ routing ไม่ใช่เรื่องของคำ

---

## 3. รูปร่างของ SSOT ใหม่

คำไทยผันไม่เท่ากันทุกช่อง (`"สร้าง" + noun` ใช้ไม่ได้กับ LODGING) และบางช่องแคบเกินกว่าจะรับคำเต็ม จึงต้องประกาศ **4 ช่อง** ต่อ vertical ไม่ใช่ noun เดี่ยวแล้วเอาไปต่อสตริง

| ช่อง | ใช้ที่ไหน | ONLINE_SALES | SERVICE_QUEUE | LODGING |
|---|---|---|---|---|
| `noun` | เมนู, breadcrumb, `<title>`, หัวข้อ, แท็บ | คำสั่งซื้อ | การเข้ารับบริการ | บิลเข้าพัก |
| `nounShort` | bottom nav, chip, ที่กว้าง <120px | คำสั่งซื้อ | เข้ารับบริการ | บิลเข้าพัก |
| `createLabel` | ปุ่มหลัก, หัวฟอร์ม, page title ของ `/orders/new` | สร้างคำสั่งซื้อ | สร้างการเข้ารับบริการ | เปิดบิลเข้าพัก |
| `createLabelShort` | bottom nav FAB, ปุ่มในแถบเครื่องมือแชท | สร้างคำสั่งซื้อ | เข้ารับบริการใหม่ | เปิดบิลเข้าพัก |

**fail-safe:** vertical ที่ไม่รู้จัก → ชุดของ `ONLINE_SALES` (เหมือน `resolveOrderMenuLabel` เดิม)

**ห้ามทำ:** ประกาศ `noun` แล้วให้ call site เขียน `` `สร้าง${noun}` `` เอง — จะได้ "สร้างบิลเข้าพัก" ซึ่งไม่ใช่ภาษาที่คนพูด และวันที่เพิ่มคำที่ผันไม่ตรงแบบจะไม่มีอะไรเตือน

---

## 4. ตารางเปลี่ยนคำรายจุด

> **ยืนยันกับ `72412fa0` (origin/main 2026-08-04 15:0x)** — ระหว่างเขียนเอกสารนี้ อีก session push งาน "รื้อหัวออเดอร์" (`812bd2e6`) ซึ่งแตะไฟล์ชุดเดียวกัน ทำให้เลขบรรทัดของ `ShippingActivity.tsx` และ `[token]/page.tsx` เลื่อน และเกิดจุดใหม่ 3 จุด (`order-action-set.ts`, `OrdersList.tsx:485`, `OrderCard.tsx:146`) ตารางนี้ปรับตามแล้ว
> 🛑 **ยังต้อง re-grep ก่อนลงมือทุกครั้ง** — รีโปนี้มีหลาย session push แข่งกัน เลขบรรทัดคือของที่เน่าเร็วที่สุดในเอกสาร ให้ยึด "ข้อความ" ในคอลัมน์กลางเป็นตัวค้น ไม่ใช่เลขบรรทัด

### 4.1 หน้ารายการ `/orders`

| ไฟล์:บรรทัด | ตอนนี้ | ต้องเป็น |
|---|---|---|
| `orders/page.tsx:28` | `title: 'ออเดอร์'` | `noun` |
| `orders/page.tsx:53` | "ต้องสร้างร้านก่อนจึงจะดู**ออเดอร์**ได้" | "ต้องสร้างร้านก่อนจึงจะดู{noun}ได้" |
| `orders/page.tsx:228` | `<PageBreadcrumb title="คำสั่งซื้อ">` | `noun` |
| `orders/loading.tsx:19` | `title="คำสั่งซื้อ"` | `noun` (ต้องรับ vertical เข้ามาด้วย — เดิมเป็น skeleton ที่ไม่รู้จัก shop) |
| `orders/components/OrdersList.tsx:441` | ปุ่ม "สร้างออเดอร์" (เดสก์ท็อป) | `createLabel` |
| `orders/components/OrdersList.tsx:485` | empty state action "+ สร้างออเดอร์แรก" | "+ {createLabel}แรก" |
| `orders/components/OrderCard.tsx:146` | `aria-label` "ดูรายละเอียด**ออเดอร์** {displayId}" | "ดูรายละเอียด{noun} {displayId}" — accessible name ที่ผู้ใช้ screen reader ได้ยิน ต้องเป็นคำเดียวกับที่คนอื่นเห็น |

### 4.2 หน้ารายละเอียด `/orders/[token]`

| ไฟล์:บรรทัด | ตอนนี้ | ต้องเป็น |
|---|---|---|
| `[token]/page.tsx:50` | `title: 'รายละเอียดคำสั่งซื้อ'` | "รายละเอียด{noun}" |
| `[token]/page.tsx:175` | `<h1 class="sr-only">รายละเอียดคำสั่งซื้อ` | "รายละเอียด{noun}" |
| `[token]/page.tsx:178-179` | breadcrumb title + trail "คำสั่งซื้อ" | `noun` ทั้งคู่ |
| `order-action-set.ts:50,51` | เมนู ⋮ — "แก้ไขคำสั่งซื้อ" / "ยกเลิกคำสั่งซื้อ" | "แก้ไข{noun}" / "ยกเลิก{noun}" — **ไฟล์นี้เพิ่งเกิดจาก commit `812bd2e6` (2026-08-04) เป็น SSOT ของ action set อยู่แล้ว ต้องรับ vertical เข้ามา** |
| `ShippingActivity.tsx:92,94` | "ผู้ขายสร้าง**คำสั่งซื้อ**แล้ว" | "ผู้ขาย{createLabel}แล้ว" |
| `ShippingActivity.tsx:121` | "**คำสั่งซื้อ**ถูกยกเลิก" | "{noun}ถูกยกเลิก" |
| `ShippingActivity.tsx:235` | "สร้าง**คำสั่งซื้อ**นี้แล้ว" | "{createLabel}นี้แล้ว" |
| `ShippingActivity.tsx:260` | หัวการ์ด "ประวัติคำสั่งซื้อ" | "ประวัติ{noun}" — **Q-2 ปิดแล้ว: ผันตาม SSOT** (หัวการ์ดเป็น label ของ timeline ไม่ใช่เนื้อหาพัสดุ จึงไม่ขัด D-1) |
| `OrderReviewCard.tsx:50` | "ยังไม่มีรีวิวสำหรับ**คำสั่งซื้อ**นี้" | "ยังไม่มีรีวิวสำหรับ{noun}นี้" |
| `CancelOrderButton.tsx:32,44,46` | "ยกเลิกคำสั่งซื้อนี้?" / "...ไม่สำเร็จ" / "...แล้ว" | "ยกเลิก{noun}นี้?" ฯลฯ + **แก้เนื้อความตาม §5 D-1** |

### 4.3 หน้าสร้าง `/orders/new` (POS)

| ไฟล์:บรรทัด | ตอนนี้ | ต้องเป็น |
|---|---|---|
| `(fullscreen)/orders/new/page.tsx:26` | `title: 'สร้างออเดอร์'` | `createLabel` |
| `(fullscreen)/orders/new/page.tsx:142` | `title="สร้างออเดอร์"` | `createLabel` |
| `new/components/CartPanel.tsx:556` | ปุ่ม "บันทึกออเดอร์" | "บันทึก{noun}" |
| `new/components/QuickSummaryPanel.tsx:90` | ปุ่ม "บันทึกออเดอร์" | "บันทึก{noun}" |
| `new/components/SubmitStatusSheet.tsx:42` | `aria-label` "กำลังสร้างคำสั่งซื้อ" | "กำลัง{createLabel}" — **ดู §5 D-2** |
| `new/components/SubmitStatusSheet.tsx:60` | "สร้างออเดอร์ไม่สำเร็จ" | "{createLabel}ไม่สำเร็จ" |
| `new/components/OrderCreateForm.tsx:667` | "แก้ไข**คำสั่งซื้อ**ไม่สำเร็จ" / "สร้าง**ออเดอร์**ไม่สำเร็จ" | "แก้ไข{noun}ไม่สำเร็จ" / "{createLabel}ไม่สำเร็จ" |
| `new/components/OrderCreateForm.tsx:681` | toast "สร้างออเดอร์แล้ว แชร์ลิงก์ให้ผู้ซื้อ" | "{createLabel}แล้ว แชร์ลิงก์ให้ลูกค้า" — **"ผู้ซื้อ" ไม่ตรงกับร้านบริการ/บ้านพัก ใช้ "ลูกค้า" เป็นกลาง** |
| `new/components/CustomerQuickBlock.tsx:348` | "บันทึก**ออเดอร์**ได้ แต่ต้องเติมก่อนเปิดพัสดุ" | เฉพาะ ONLINE_SALES — ประโยคนี้พูดถึงพัสดุ ร้าน vertical อื่นไม่ควรเห็นเลย (ดู §8 verify) |

### 4.4 chrome + แชท + dashboard

| ไฟล์:บรรทัด | ตอนนี้ | ต้องเป็น |
|---|---|---|
| `_shared/SellerBottomNav.tsx:49` | label "สร้างออเดอร์" | `createLabelShort` |
| `CustomerPanel.tsx:124-146` | `VERTICAL_CTA` ประกาศคำเอง 3 ชุด | อ่าน `noun`/`createLabelShort` จาก SSOT; เหลือไว้แค่ `href`/`icon` |
| `dashboard/components/RecentOrder.tsx:156` | empty "ยังไม่มีออเดอร์" | "ยังไม่มี{noun}" |
| `dashboard/components/RecentActivityFeed.tsx:91` | "สร้างออเดอร์แรกเลย" | "{createLabel}แรกเลย" |
| `dashboard/components/BestSellerStrip.tsx:40` | "จิ้มที่การ์ดเพื่อเพิ่มลง**ออเดอร์ใหม่**" | "…เพิ่มลง{noun}ใหม่" |
| `expenses/components/PnlReportCard.tsx:84` | "ยังไม่มีออเดอร์" | "ยังไม่มี{noun}" |
| `badges/BadgeDetailModal.tsx:99,107` · `badges/_constants/badge-labels.ts:50` | "สร้างออเดอร์ใหม่ผ่านเมนู \"ออเดอร์\"" ฯลฯ | ผันตาม `noun`/`createLabel` — ชื่อเมนูในคำแนะนำต้องตรงกับเมนูที่ผู้ใช้เห็นจริง |

---

## 5. ข้อบกพร่องที่ clarify พบ (แก้ไปพร้อมกัน ไม่ต้องรอ decision)

### D-1 — กล่องยืนยันการยกเลิกสัญญาสิ่งที่ระบบไม่ได้ทำให้ร้านส่วนใหญ่

`CancelOrderButton.tsx:32` แสดงว่า *"สินค้าจะถูกคืนเข้าสต็อก · ลิงก์ที่ส่งให้ผู้ซื้อจะใช้ไม่ได้ · ย้อนกลับไม่ได้"*

ตรวจโค้ดจริง: `cancelOrder` เรียก `restockFromCancelledOrder` ซึ่ง query `orderItem where stockDeducted != null` แล้ว **`if (items.length === 0) return`** (`inventory-stock.service.ts:91-95`). `stockDeducted` ถูกเซ็ตเฉพาะตอนตัดสต็อกผ่าน Inventory Add-on (฿199/เดือน) เท่านั้น

แปลว่าประโยคนี้เป็นเท็จสำหรับ:
- ร้าน `SERVICE_QUEUE` และ `LODGING` ทั้งหมด (ไม่มีเมนู `seller:inventory` เลย)
- ร้าน `ONLINE_SALES` ที่ไม่ได้ซื้อ Add-on ซึ่งคือ**ส่วนใหญ่ของร้านทั้งหมด**

นี่คือกล่องยืนยันการทำลายล้างที่ย้อนกลับไม่ได้ — จุดที่ผู้ใช้ต้องเชื่อระบบมากที่สุด

**แก้เป็น:** ระบุผลที่เกิดจริงเท่านั้น ไม่เดาผลใหม่มาแทน

| vertical / เงื่อนไข | ข้อความ |
|---|---|
| ONLINE_SALES + มี item ที่ `stockDeducted != null` | "สินค้าจะถูกคืนเข้าสต็อก · ลิงก์ที่ส่งให้ลูกค้าจะใช้ไม่ได้ · ย้อนกลับไม่ได้" |
| กรณีอื่นทั้งหมด | "ลิงก์ที่ส่งให้ลูกค้าจะใช้ไม่ได้ · ย้อนกลับไม่ได้" |

> **ห้ามเดาผลแทน** เช่นเขียนให้ `SERVICE_QUEUE` ว่า "คิวที่จองไว้จะถูกปล่อยคืน" โดยยังไม่ได้พิสูจน์ว่า `cancelOrder` ปล่อย `serviceSeat` จริง — ถ้าอยากได้ประโยคนั้นต้องไปยืนยันพฤติกรรมของ EXCLUDE constraint/appointment ก่อน แล้วค่อยเพิ่มเป็นเคสที่สาม

### D-2 — ชื่อสำหรับ screen reader ไม่ตรงกับข้อความที่ตาเห็น

`SubmitStatusSheet.tsx` — `aria-label` (บรรทัด 42) = "กำลังสร้าง**คำสั่งซื้อ**" แต่ข้อความที่ render จริง (บรรทัด 60) = "สร้าง**ออเดอร์**ไม่สำเร็จ". คนละคำในคอมโพเนนต์เดียวกัน ผู้ใช้ screen reader ได้ยินคนละอย่างกับที่คนข้าง ๆ เห็นบนจอ

**แก้:** ทั้งคู่มาจาก `createLabel` ตัวเดียว

---

## 6. `LODGING` — อธิบายความต่างของ 2 คำ (C-3)

ร้านบ้านพักเห็นเมนู "การจอง" (`/bookings`) และ "บิลเข้าพัก" (`/orders`) พร้อมกัน เพราะ `seller:orders` ไม่ได้อยู่ในรายการที่กรองตาม vertical (`seller-menu.ts:310-320`) ตอนนี้ไม่มีอะไรบอกความต่างเลย

**เติมคำอธิบายที่ 2 จุด (ไม่เพิ่มหน้าจอใหม่):**

| จุด | ข้อความ |
|---|---|
| หัวหน้า `/bookings` (subtitle ใต้ breadcrumb) | "การจองห้องพัก — วันเข้าพักและห้องที่กันไว้" |
| หัวหน้า `/orders` เมื่อ `vertical === 'LODGING'` | "บิลเข้าพัก — ยอดเงินและการชำระของแต่ละการจอง" |

เขียนเป็นคู่ตรงข้ามโดยตั้งใจ (**วันและห้อง** vs **ยอดเงินและการชำระ**) เพื่อให้อ่านผ่านตาแล้วแยกออกทันทีว่าจะไปหน้าไหน โดยไม่ต้องกดเข้าไปลอง

---

## 7. หน้าเลือกประเภทร้าน — copy ของ onboarding 2 ขั้น

ปัญหาของชุดคำปัจจุบัน: ป้าย 3 ใบ (`SHOP_VERTICALS`) คือ "ขายออนไลน์" / "สินค้าและบริการ" / "บ้านพัก" — **ไม่ขนานกันและไม่ฟังดูแยกขาดจากกัน**. "สินค้าและบริการ" ฟังเหมือนเป็นเซตที่ครอบ "ขายออนไลน์" อยู่ในตัว. ตัวที่ทำหน้าที่แยกจริงคือ badge "มีจัดส่งสินค้า / ไม่มีจัดส่งสินค้า" ที่อยู่ข้าง ๆ ไม่ใช่ป้ายเอง

### ขั้นที่ 1

- **หัวข้อ:** "ร้านของคุณเป็นแบบไหน" (คงเดิม — ดีอยู่แล้ว)
- **คำอธิบายใต้หัวข้อ:** "เลือกได้ครั้งเดียว เปลี่ยนภายหลังไม่ได้ — ถ้าเลือกผิดต้องสร้างร้านใหม่"

| การ์ด | ป้าย | คำอธิบาย | → |
|---|---|---|---|
| 1 | ขายของออนไลน์ | ลูกค้าสั่งแล้วคุณจัดส่งให้ | `ONLINE_SALES` |
| 2 | รับนัดหมายและจอง | ลูกค้านัดวันแล้วมาที่ร้าน หรือมาเข้าพัก | ไปขั้นที่ 2 |

### ขั้นที่ 2 (เฉพาะเมื่อเลือกการ์ด 2)

- **หัวข้อ:** "ลูกค้ามาแบบไหน"

| การ์ด | ป้าย | คำอธิบาย | → |
|---|---|---|---|
| A | มาใช้บริการแล้วกลับ | เช่น ร้านตกแต่งรถ ร้านนวด คลินิก — กำหนดได้ว่าวันหนึ่งรับได้กี่คิว | `SERVICE_QUEUE` |
| B | มาพักค้างคืน | เช่น บ้านพัก รีสอร์ต — คิดราคาต่อคืน จองข้ามหลายคืนได้ | `LODGING` |

**"แล้วกลับ" vs "ค้างคืน" คือความต่างเชิงโครงสร้างจริงของสองระบบ** (slot capacity ต่อวัน vs ช่วงวันที่ต่อเนื่องคูณราคาต่อคืน) เขียนออกมาเป็นภาษาที่ผู้ใช้ตัดสินได้ทันทีโดยไม่ต้องรู้ศัพท์ระบบ

### สิ่งที่ต้องถอดออก

- **badge "มีจัดส่งสินค้า / ไม่มีจัดส่งสินค้า"** (`onboarding/page.tsx:270`) — คำอธิบายของการ์ดขั้นที่ 1 พูดเรื่องนี้ไปแล้ว การมี badge ซ้ำทำให้อ่านสองรอบเพื่อได้ข้อมูลเดียว
- **บรรทัดเตือนใต้การ์ดใน `CreateBusinessForm.tsx:169-172`** ("เลือกแล้วเปลี่ยนภายหลังไม่ได้…") — ย้ายขึ้นไปเป็นคำอธิบายใต้หัวข้อ ให้เหมือนฝั่ง Personal. **ข้อจำกัดต้องมาก่อนการเลือก ไม่ใช่หลัง** และสองหน้าต้องใช้ประโยคเดียวกัน (ตอนนี้เป็นคนละประโยคที่พูดเรื่องเดียวกัน)

### `SHOP_VERTICALS` (ป้ายที่ใช้แสดงประเภทร้านนอก onboarding)

เสนอให้ตรงกับป้ายการ์ด: `ONLINE_SALES` = "ขายของออนไลน์" · `SERVICE_QUEUE` = "รับนัดใช้บริการ" · `LODGING` = "บ้านพัก"

> **ก่อนเปลี่ยนต้อง grep ทั้ง repo** ว่าคำใหม่ไม่ชนกับ label ที่มีอยู่ — บทเรียน 00028 ที่ "สินค้าและบริการ" ไปชนกับป้ายแท็บสินค้าบนโปรไฟล์สาธารณะตรงตัว (`docs/conventions/enum-value-removal.md`)

---

## 8. Verify (ทำก่อนปิดงาน)

1. `rg "ออเดอร์" "src/app/(paces)/seller/"` — เหลือเฉพาะในคอมเมนต์ ไม่มีในสตริงที่ผู้ใช้เห็น
2. `rg "คำสั่งซื้อ" "src/app/(paces)/seller/"` — ทุก hit ต้องอยู่ใน SSOT หรือมาจาก SSOT ไม่มี literal ลอย
3. เปิด `/orders`, `/orders/new`, `/orders/[token]` **ครบทั้ง 3 vertical** ด้วยร้านจริง — ไม่ใช่เดาจากโค้ด
4. อ่านหน้า `/orders/new` ด้วย screen reader ตรวจว่า `aria-label` = ข้อความที่เห็น (D-2)
5. กดยกเลิกจริงบนร้านที่ไม่มี Inventory Add-on แล้วยืนยันว่าไม่มีคำว่า "คืนเข้าสต็อก" ขึ้น (D-1)
6. ตรวจความยาวคำที่ 320px: bottom nav (`createLabelShort`), sidebar (`noun`), breadcrumb — "การเข้ารับบริการ" เป็นคำที่ยาวที่สุดในชุด ต้องไม่ตัดคำหรือดันเลย์เอาต์
7. **ยังไม่ยืนยัน:** ร้าน `LODGING` เข้า `/orders/new` ได้จริงหรือไม่ (`(fullscreen)/orders/new/page.tsx:81` มีเงื่อนไขผูก `ONLINE_SALES` แต่ยังไม่ได้ไล่ว่าบล็อกทั้งหน้าหรือแค่บางส่วน) — ถ้าเข้าไม่ได้ `createLabel` ของ LODGING จะไม่มีที่ใช้ ต้องตัดออกจาก SSOT ไม่ใช่ปล่อยไว้เป็นคำที่ไม่มีใครเห็น

---

## 9. ส่วนขยาย 2026-08-07 — Mobile Command Center (user report จากร้าน BT Premium บน prod)

user เปิดหน้าแรกบนมือถือของร้าน `SERVICE_QUEUE` จริงแล้วพบว่า **เมนูลัดในจอเดียวกันเขียน "การเข้ารับบริการ" ถูกแล้ว แต่อีก 4 บล็อกยัง hardcode "คำสั่งซื้อ"** — พิสูจน์ว่า SSOT มีอยู่จริงและใช้ได้ เพียงแต่ไม่เคยมีใครเดินสายเข้าบล็อกเหล่านี้ (ตาราง §4.4 ครอบ `RecentOrder`/`RecentActivityFeed`/`BestSellerStrip` แต่ไม่เคยครอบ hero, กราฟยอดขาย, การ์ดสถานะ)

### 9.1 คำที่เปลี่ยน

| ไฟล์ | ตอนนี้ | เป็น |
|---|---|---|
| `dashboard/components/CompactHero.tsx` | "1 **คำสั่งซื้อ** · 1 รีวิว" | `{noun}` |
| `dashboard/components/SalesChartCard.tsx` | legend + ชื่อซีรีส์ + aria-label "คำสั่งซื้อ" | `{noun}` |
| `dashboard/components/OrderStatusBand.tsx` | หัวการ์ด "คำสั่งซื้อ" | `{noun}` |
| `dashboard/components/BestSellerStrip.tsx` | "สินค้าขายดี" / "ดูสินค้าทั้งหมด" / "สั่งซื้อแล้ว N ชิ้น" | `PRODUCT_VOCAB` (ดู §9.2) |

**ใช้ `noun` เต็ม ไม่ใช่ `nounShort` แม้ในช่องแคบ** — ตรงนี้คำอยู่ *หลังตัวเลข* ("1 บริการ") ซึ่งอ่านเป็น "บริการ 1 อย่าง" ไม่ใช่ "งาน 1 ใบ"; ทั้งสองจุดมี `truncate`/`flex-wrap` รองรับคำยาวอยู่แล้ว. `nounShort` ยังคงมีไว้สำหรับแท็บล่าง/หัวโมดัลตามเดิม

**ชื่อซีรีส์ของกราฟถูกอ้างซ้ำที่ `yaxis[2].seriesName`** — ต้องมาจากตัวแปรเดียวกัน ไม่งั้นร้านที่ผันคำแล้วสองที่ไม่ตรงกัน ApexCharts จะผูกเส้นเข้าแกนผิด (เส้นแบนติดพื้น) โดยไม่มี error

### 9.2 `PRODUCT_VOCAB` — SSOT ชุดที่สอง (คำฝั่งสินค้า)

`ORDER_VOCAB` ครอบเฉพาะคำฝั่ง order lifecycle ส่วนบล็อก "ขายดี" พูดถึงตัวสินค้าและ **หน่วยนับการขาย** ซึ่งเป็นคนละแกน — เอาไปยัดเป็นช่องเพิ่มใน `OrderVocab` ไม่ได้เพราะ call site ส่วนใหญ่ของ `ORDER_VOCAB` ไม่รู้จักสินค้าเลย. อยู่ไฟล์เดียวกัน (`src/lib/seller-menu.ts`) เพื่อไม่ให้เกิดคำชุดคู่ขนานคนละไฟล์

| ช่อง | ONLINE_SALES | SERVICE_QUEUE | LODGING |
|---|---|---|---|
| `bestSellerTitle` | สินค้าขายดี | บริการยอดนิยม | ห้องพักยอดนิยม |
| `viewAllLabel` | ดูสินค้าทั้งหมด | ดูบริการทั้งหมด | ดูห้องพักทั้งหมด |
| `soldIcon` | `package` | `tool` | `bed` |
| `soldLine(n)` | สั่งซื้อแล้ว {n} ชิ้น | ใช้บริการแล้ว {n} ครั้ง | เข้าพักแล้ว {n} ครั้ง |

`soldLine` เป็น **ฟังก์ชันคืนทั้งประโยค** ไม่ใช่ verb+unit ให้ call site ต่อเอง — เหตุผลเดียวกับ §3 (ประโยคไทยผันไม่เท่ากันทุก vertical) · "ขายดี" → "ยอดนิยม" เพราะ "ขายดี" ฟังเป็นของที่ขายเป็นชิ้น

### 9.3 ไทล์ที่ 2 ของการ์ดสถานะ: "กำลังจัดส่ง" → "นัดวันนี้" (ร้าน `SERVICE_QUEUE`)

ไม่ใช่แค่เรื่องคำ — **ร้าน `SERVICE_QUEUE` มี `fulfillmentMode = NO_SHIPPING` จึงไม่มีวันเข้าสถานะ `SHIPPED` เลย** (`src/lib/order-display.ts:6`) ไทล์นั้นขึ้น 0 ตลอดกาล กินพื้นที่ 1 ใน 4 ช่องของแถบ "งานค้างวันนี้" ไปเปล่า ๆ. เปลี่ยนเป็นแค่คำจึงไม่พอ ต้องเปลี่ยนสิ่งที่นับ

- ตัวนับ = `getTodayAppointmentCount(shopId)` (`appointment.service.ts`) — เกณฑ์ = ช่วงนัด **คาบเกี่ยว** วันไทยวันนี้ (ชุด predicate เดียวกับ `listAppointments`) ไม่ใช่ `serviceStart` อยู่ในวันนี้ ไม่งั้นนัดข้ามหลายวันจะหายจากวันกลาง ๆ
- ขอบเขตวันผ่าน `thaiMidnightUtc` — ห้ามตัดวันด้วย UTC (บทเรียน 00033)
- นับรวมนัดที่จบไปแล้ววันนี้ (`COMPLETED`/`NO_SHOW`) โดยตั้งใจ — ป้ายเขียนว่า "นัดวันนี้" = ข้อเท็จจริงของทั้งวัน ไม่ใช่ "ที่ยังไม่ได้ทำ"; ตัดออกเฉพาะ `CANCELLED`
- icon `solar:calendar-mark-bold-duotone` + `text-info` **ยกจาก `shortcut-icons.ts` (`seller:bookings`)** ไม่ได้เลือกใหม่ — ไอคอนของ "การนัด" ถูกตัดสินไว้แล้วที่นั่น
- ลิงก์ไป `/queues` ไม่ใช่ `/orders?status=` — ตัวเลขนี้เป็นแกนวันที่ ตัวกรองสถานะรับไม่ได้
- query ยิงเฉพาะร้านที่ผ่าน `canUseAppointments` (BR-RSV-01); fetch ล้ม → ไทล์ตกกลับเป็น "กำลังจัดส่ง" ไม่ใช่โชว์ "นัดวันนี้ 0" (เลข 0 ที่ผิดอันตรายกว่าไทล์ที่ไม่เกี่ยว)

### 9.4 ยังไม่ทำในรอบนี้ (user จำกัดขอบเขตไว้ที่ Mobile Command Center)

- **เดสก์ท็อป** — `OrderStatusBand` ตัวเดียวกันถูก render ที่ `dashboard/page.tsx` (บล็อก `hidden lg:block`) โดยไม่ส่ง prop ใหม่ และ `TopSellingProducts.tsx` ยัง hardcode "สินค้าขายดี"/"ยังไม่มีคำสั่งซื้อ" → **จอเดสก์ท็อปยังพูดคนละคำกับมือถือ**
- ตาราง §4.4 ที่ยังไม่ได้ทำ (`RecentOrder`, `RecentActivityFeed`, `PnlReportCard`, badge copy) ยังค้างเหมือนเดิม
- browser QA — ยังไม่เคยกดจริง

### 9.5 หนี้ที่เจอระหว่างทาง (แก้ไปด้วย)

`src/lib/seller-menu.test.ts` เทส `resolveOrderVocab` แดงมาตั้งแต่ feature 00033 เพิ่มช่อง `dateLabel` — `toEqual` เทียบ 4 ช่องกับของจริงที่มี 5 ช่อง. **เทสที่แดงค้างคือ guard ที่หยุดทำงาน**: ถ้ามีใครแก้คำใน `ORDER_VOCAB` ผิดในช่วงนั้น เทสก็แดงเหมือนเดิม ไม่มีใครแยกออก. แก้ให้ครบ 5 ช่องแล้ว + เพิ่มชุดเทสของ `PRODUCT_VOCAB` (25/25 เขียว)
