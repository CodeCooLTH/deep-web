# 00018 — Extensions (2026-08-08) — แจ้งเตือนเข้าแอปผู้ขาย

> | # | Extension | สรุป |
> |---|---|---|
> | E1 | **ปลายทาง** ของ noti — กด noti ของอีกร้านแล้วขึ้น "ไม่พบบทสนทนานี้" | payload ไม่มี `shopId` → หาร้านเจ้าของเธรดฝั่ง server แล้วสลับให้อัตโนมัติ (**บันทึกย้อนหลัง** — งานลงบน prod แล้ว 2026-08-06/07 แต่ไม่เคยมีเอกสารฟีเจอร์) |
> | E2 | **เนื้อหา** ของ noti — `[ชื่อเพจ] / [ชื่อคนส่ง] / [ข้อความ]` | เดิมมีแค่ชื่อผู้ส่ง ผู้ขายหลายเพจ/หลายร้านดูไม่ออกว่าข้อความมาจากไหนจนกว่าจะกดเปิด (**แก้ 3 รอบในวันเดียว** — ดู E2.3a, E2.3b) |
>
> **ไม่มี migration ทั้งสองข้อ** — ใช้คอลัมน์เดิมทั้งหมด (`Conversation.channel`, `Conversation.shopChannelId`,
> `ShopChannel.name`) และไม่มี API route ใหม่ · **ไม่ต้อง build/ส่ง OTA แอป** ทั้งสองข้อเป็นงานฝั่งเว็บล้วน
>
> ไฟล์ที่แตะรอบ E2: `src/lib/chat-channel.ts` (ใหม่) · `src/lib/expo-push.ts` ·
> `src/services/app-push.service.ts` · `src/services/seller-push.service.ts` ·
> `src/app/(paces)/seller/(chat)/inbox/components/ChannelBadge.tsx` (+ เทส 2 ไฟล์)

---

## E1 — กด noti ของอีกร้านแล้ว "ไม่พบบทสนทนานี้"

> 🛑 หัวข้อนี้เป็นการ **บันทึกย้อนหลัง** งานที่ deploy ไปแล้ว (`90c1dc43`, `3ca3ba57` และฝั่งแอป
> `bb1f27f`) ทั้งสามคอมมิตไม่ได้แตะ `docs/` เลย — เขียนไว้ที่นี่เพื่อไม่ให้ตรรกะการสลับร้าน
> อัตโนมัติกลายเป็นของที่มีอยู่ในโค้ดโดยไม่มีใครรู้ที่มา

### E1.1 อาการที่ผู้ใช้เห็น

ผู้ขายที่ถือ **2 ร้าน และผูก Facebook Page คนละเพจไว้คนละร้าน** จะได้ push ของทั้งสองร้านลง
เครื่องเดียวกัน (ถูกต้องแล้ว — `shopAudience()` ส่งให้เจ้าของ+สมาชิกของร้านนั้น ซึ่งเป็นคนเดียวกัน)

แต่พอกด noti ของร้าน B ขณะที่ active อยู่ร้าน A ระบบขึ้น **"ไม่เจอช่องแชทนี้"** ทั้งที่เธรดมีอยู่จริง
และผู้ใช้มีสิทธิ์เต็ม — ไม่มีอะไรบอกว่าต้องไปสลับร้านก่อน ผู้ใช้ต้องเดาเอง

### E1.2 Root cause

payload ของ push มีแค่:

```json
{ "type": "chat", "url": "/inbox/{conversationId}", "conversationId": "…" }
```

**ไม่มี `shopId`** ขณะที่ ownership guard ของหน้าเธรด scope ด้วย `activeShopId`
(`WHERE { id, shopId: activeShopId }`) → เธรดของร้าน B หาไม่เจอเมื่อ active อยู่ร้าน A

### E1.3 ทางเลือกที่ไม่เลือก และเหตุผล

| ทางเลือก | ทำไมไม่เอา |
|---|---|
| เพิ่ม `shopId` ลง payload | ต้อง **build แอปใหม่ + ส่ง App Store ใหม่** ขณะที่ build 2 ยังอยู่ในคิวรีวิว · และเครื่องที่ยังไม่อัปเดตจะยังพังเหมือนเดิมตลอดไป |
| ให้ผู้ใช้สลับร้านเอง (ขึ้นข้อความบอก) | ผู้ใช้กด noti เพราะอยากอ่านข้อความ ไม่ได้อยากเรียนรู้เรื่องบริบทร้าน |

**ที่เลือก:** resolve ร้านเจ้าของเธรดจาก `conversationId` **ฝั่ง server** — payload เดิมใช้ได้
ทุกเวอร์ชันของแอปที่ปล่อยไปแล้ว ไม่ต้องแตะแอปเลย

### E1.4 กลไก

1. `listAccessibleShopIds(userId)` (`src/lib/shop-context.ts`) — ร้านที่ผู้ใช้เข้าถึงได้ทั้งหมด
   (เจ้าของ + สมาชิก) กรอง `deletedAt: null, purgedAt: null`
2. `findConversationShopForUser(conversationId, userId)` (`src/services/chat.service.ts`) —
   **scope สิทธิ์อยู่ใน `WHERE` (`shopId: { in: accessibleShopIds }`) ไม่ใช่ดึงมาแล้วเทียบทีหลัง**
   (กัน TOCTOU/IDOR ตามแบบเดียวกับ `getConversationToastPreview`)
3. `page.tsx` ของเธรด: เจอเธรด → render ปกติ · ไม่เจอ → ถามข้อ 2 ว่าเป็นของร้านไหนที่เข้าถึงได้
   → render `ChatShopAutoSwitch`
4. `ChatShopAutoSwitch.tsx` — สลับร้านผ่าน `useShopSwitcher` แล้ว hard-navigate กลับ
   `/inbox/{id}?switched=1`

🛑 **`?switched=1` คือตัวกันวน ห้ามเอาออก** — ถ้าสลับไม่สำเร็จจริง (session ไม่โปรพาเกต)
`page.tsx` เห็นค่านี้แล้วจะไม่ render ตัวสลับซ้ำ แต่ตกไปหน้า error ให้กดกลับได้

### E1.5 บั๊กที่เจอตอน self-review — overlay ค้างตลอดไป (`90c1dc43`)

เดิม `ChatShopAutoSwitch` hardcode `show={true}` — ถ้าสลับล้มเหลว (403 / เน็ตหลุด / 500)
ผู้ใช้จะ **ติดอยู่หน้าสปินเนอร์ ออกไปไหนไม่ได้เลย**

`useShopSwitcher` ไม่มี error state ให้อ่าน จึงต้องอนุมานจากพฤติกรรม:

| ผลลัพธ์ | `switching` |
|---|---|
| สำเร็จ | ค้าง `true` จน browser unload หน้า (ไม่เคยกลับเป็น false) |
| ล้มเหลว | เด้ง `pacesToast` แล้วตั้งกลับเป็น `false` |

→ **"เคยเป็น true แล้วกลับเป็น false" = ล้มเหลวเสมอ** → render `SellerErrorState`

### E1.6 Performance (`3ca3ba57`) — ~3.8 วิ → ~1.6 วิ

| ที่มาของเวลา | เดิม | ใหม่ |
|---|---|---|
| overlay ขั้นต่ำ | 3000ms (ค่าเดิมของการสลับร้านด้วยมือ) | **800ms** ผ่าน option `minOverlayMs` |
| query ตอนเธรดไม่ใช่ของร้านที่ active | 6 query (5 ตัวเสียเปล่า) | `conversation.findFirst` รันก่อน แล้วค่อย `Promise.all` ที่เหลือ |
| happy path | 3 รอบ round trip เรียงกัน | 2 รอบ |

**ทำไมลด 3000 → 800 ได้:** 3000ms เดิมมีไว้กันจอกระพริบตอนผู้ใช้ *กดสลับร้านเอง* — เขาเพิ่งเลือก
ร้านจากรายการ ต้องเห็นว่ากำลังไปร้านไหนสักพัก. แต่เส้นทางนี้ผู้ใช้ **ไม่ได้ตั้งใจสลับร้าน** เขากด
แจ้งเตือนเพื่ออ่านข้อความ การถ่วง 3 วินาทีอ่านเป็น "ระบบช้า" ทั้งที่งานจริงจบตั้งแต่ ~300ms

**ทำไมไม่ตัดเป็น 0:** ต้องเหลือเวลาให้ผู้ใช้อ่านทันว่าระบบกำลังสลับไปร้านไหน ไม่งั้นจอเปลี่ยนวูบเดียว
แล้วเขาจะงงว่าเมื่อกี้เกิดอะไรขึ้น — ปลอดภัยที่จะสั้นเพราะ `session.update()` เป็น `await`
(cookie ถูกตั้งเรียบร้อยแล้วตอน promise คืนค่า) เวลาที่เกินจากนั้นเป็นเรื่อง "ความรู้สึก" ไม่ใช่ความถูกต้อง

### E1.7 ฝั่งแอป — deep-link ที่พังคนละสาเหตุ (`bb1f27f` ใน deep-mobile-seller)

หลังแก้ E1.4 หัวหน้ารายงานว่า noti **ไม่พาไปแชทเลย** ซึ่งเป็นคนละบั๊ก 2 ตัวซ้อนกัน:

1. **`injectJavaScript` แข่งกับการโหลดแรกของ WebView** → แก้ด้วย `takePendingPath()` ที่ consume
   คิวมาใช้เป็น `initialUri` ตั้งแต่ render แรก (ได้ผลพลอยได้: ไม่ต้องโหลด `/dashboard` ทิ้งก่อน)
2. **cold start: response มาก่อน listener ถูก subscribe** → เพิ่ม
   `takeLaunchNotificationResponse()` อ่านค่าจาก native (`getLastNotificationResponse`)

🛑 **ลำดับสำคัญ: ต้องอ่าน launch response *หลัง* เช็ค OTA จบเท่านั้น** — ตัวอ่านนี้ล้างค่าฝั่ง native
ทิ้งหลังอ่าน ถ้าอ่านตอน mount แล้ว `applyPendingUpdate()` เจอ update ใหม่ →
`Updates.reloadAsync()` รีสตาร์ท JS context → คิวหายไปด้วย **และค่าฝั่ง native ก็ถูกล้างไปแล้ว**
= ปลายทางหายทั้งสองทาง (บั๊กที่ผมสร้างเองระหว่างแก้ แล้วจับได้ตอนตรวจซ้ำ)

dedupe ด้วย `responseId` เพราะบาง OS ยิงทั้งสองทางสำหรับการกดครั้งเดียว

---

## E2 — noti บอกด้วยว่าลูกค้าทักเข้ามาที่เพจไหน

### E2.1 คำขอ

> user 2026-08-08: "ถ้าแจ้งเตือน noti อยากได้ page ว่าเป็น facebook หรือ ig อื่น ๆ ด้วย
> ทำได้มั้ยนะ ตอนนี้มีแต่ชื่อใช่ปะ"

ใช่ — เดิม `pushNewChatMessage` ส่งแค่ `title = preview.senderName` และ `body = ข้อความ`

**นี่คือคำขอเดียวกับที่ user เคยสั่งเมื่อ 2026-07-23 สำหรับ `ChannelBadge` ในกล่องแชท** —
*"ร้านที่มีหลายเพจ ต้องรู้ว่าลูกค้าคนนี้ทักมาจากเพจไหน คำว่า Messenger ซ้ำกันทุกเธรดจนไม่ให้ข้อมูลอะไร"*
แค่คนละพื้นผิว จึงยึดคำตัดสินเดิมเป็นฐาน: **ชื่อเพจคือตัวระบุ ส่วนช่องทางเป็นบริบท**

### E2.2 ข้อมูลมีอยู่แล้ว ไม่มี query เพิ่ม

`getConversationToastPreview()` ที่ push เรียกอยู่แล้ว **คืน `channel` และ `channelName`
(= `ShopChannel.name` ชื่อเพจ) มาทุกครั้งอยู่แล้ว** — เดิมทิ้งไปเปล่า ๆ

### E2.3 ทำไมต้องมี `subtitle` ไม่ใช่ยัดสองอย่างรวมบรรทัดเดียว

ชื่อเพจกับชื่อผู้ส่งเป็นชื่อไทยเต็ม ๆ ทั้งคู่ ยัดรวมบรรทัดเดียว (` · `) ความยาวเกินเพดานของ iOS
เกือบทุกใบ แล้ว `…` **จะไปลงตรงตัวที่อยู่ท้าย = ตัดทิ้งสิ่งที่เพิ่งเพิ่มเข้ามาเพื่อจะบอกเสียเอง**
— `subtitle` ทำให้ได้สามบรรทัดแยกกันจริง แต่ละบรรทัดมีเพดานของตัวเอง

ลำดับสุดท้าย (ผ่านการแก้ 3 รอบ ดู E2.3a และ E2.3b):

```
┌────────────────────────────────────┐
│ ▣  BT Premium Auto Xenon คลอง4 …   │  title    ← ชื่อเพจ
│    ศิริพงศ์ชาวด์ แอนด์มิวสิค          │  subtitle ← ชื่อคนส่ง
│    เปลี่ยนเลนใหม่ด้วยใช่มั้ยครับ       │  body     ← ข้อความ
└────────────────────────────────────┘
```

ยืนยันจากเอกสาร Expo Push API: `subtitle` (string) — Optional (**iOS Only**) — "The subtitle to
display in the notification below the title"

### E2.3a 🛑 รอบแรกใส่ชื่อช่องทางนำหน้า แล้วถอดออกวันเดียวกัน

รอบแรกส่งขึ้น prod เป็น `"{ช่องทาง} · {ชื่อเพจ}"` — หัวหน้าทักทันทีจากเครื่องจริง:

> "ทำไมมันมี Messenger มาด้านหน้า / เสียพื้นที่"

ภาพจากเครื่องจริง: `Messenger · BT Premium Auto Xenon คลอง…`

คำว่า `Messenger · ` กิน **12 ตัวอักษรแรก** ของบรรทัด แล้วดันชื่อเพจจนโดนตัด — **หางของชื่อเพจ
คือที่ที่เพจของร้านเดียวกันต่างกัน** จึงเท่ากับเอาพื้นที่ไปให้ของที่ซ้ำกันทุกใบ แล้วตัดตัวระบุที่
ผู้ใช้ต้องการจริงทิ้ง

**นี่คือคลาสความผิดพลาดเดียวกับ E2.3 เป๊ะ แค่ย้ายมาเกิดอีกบรรทัดหนึ่ง** — เหตุผลที่เลือก
`subtitle` แทน `title` คือ "อย่าให้ของประกอบเบียดตัวระบุจนโดนตัด" แล้วก็ทำสิ่งเดียวกันซ้ำใน
บรรทัดใหม่ เพราะประเมินว่า "บรรทัดกลางมีที่ว่างเหลือเฟือ" **โดยไม่ได้วัด**

ตอนนี้: **ชื่อเพจล้วน** · ช่องทางไม่ได้หายไปไหน — ผู้ใช้ยังรู้จากชื่อเพจเอง (เพจ Facebook กับ
บัญชี IG คนละชื่อกัน) และ `channel` ยังอยู่ใน `data` ให้แอปหยิบใช้ได้ตลอด · เธรด `DEEP` ที่ไม่มีเพจ
ถอยไปใช้ชื่อช่องทางแทน (บรรทัดห้ามว่าง ไม่งั้น iOS ดันเนื้อหาขึ้นชิดหัวเรื่อง = ผู้ใช้เห็น noti
สองแบบสลับกันโดยไม่มีเหตุผล)

มีเทส `[blocker]` ผูกไว้แล้วว่าบรรทัดนี้ **ห้ามขึ้นต้นด้วยชื่อช่องทาง และห้ามมี `·`**

### E2.3b ลำดับสุดท้าย — ชื่อเพจขึ้น `title` (แก้รอบที่ 3 ในวันเดียว)

หลัง E2.3a หัวหน้าเขียนโครงมาให้ตรง ๆ:

```
[ ชื่อเพจ ]
[ ชื่อคนส่งข้อความ ]
[ ข้อความ ]
```

คือ **สลับ `title` ↔ `subtitle`** จากที่ทำไว้ — เดิมชื่อคนส่งอยู่บนสุด ชื่อเพจอยู่บรรทัดกลาง

เหตุผลเชิงการใช้งานที่รองรับ: `title` เป็นบรรทัดที่เด่นที่สุดบน iOS **ผู้ขายที่ดูแลหลายเพจต้องรู้
"ใบนี้ของเพจไหน" ก่อนจะสนใจว่าใครทัก** — เพจคือกล่องงาน ส่วนชื่อลูกค้าคือรายละเอียดข้างใน

🛑 **หนี้ Android เปลี่ยนหน้าตาไปพร้อมกัน:** เดิม Android (ไม่มี `subtitle`) จะเสีย *"มาจากเพจไหน"*
แต่หลังสลับแล้วจะเสีย **"ใครทัก"** แทน (เหลือชื่อเพจ + ข้อความ) — ยังไม่กระทบเพราะปล่อยแค่ iOS
แต่วันเพิ่มคอลัมน์ `platform` ต้อง**ประกอบข้อความของ Android ขึ้นใหม่ให้มีชื่อคนส่งด้วย**
ไม่ใช่ก็อปชุด iOS ไปตรง ๆ

**ลำดับนี้ถูกแก้ 3 รอบในวันเดียว** (ชื่อคนส่งขึ้นก่อน → เติมชื่อช่องทางนำหน้าเพจ → สลับให้เพจขึ้นก่อน)
จึงผูกเทส `[blocker]` ที่ระดับ `pushNewChatMessage` ไว้ด้วย — ยืนยันว่าเพจอยู่ `title`
คนส่งอยู่ `subtitle` ข้อความอยู่ `body` ไม่ให้ใครสลับกลับโดยไม่ตั้งใจ

### E2.4 ทำไม *ไม่* ใส่ชื่อร้านคู่ไปด้วย

พิจารณาแล้วตัดออก — ชื่อเพจแยกได้ครบทุกกรณีที่มีเพจอยู่แล้ว:

| สถานการณ์ | ชื่อเพจพอไหม |
|---|---|
| 2 ร้าน คนละเพจ (เคสหัวหน้า) | พอ — และชื่อร้านจะกลายเป็นการพูดเรื่องเดิมสองรอบ |
| ร้านเดียว หลายเพจ | พอ — ชื่อร้านไม่ช่วยแยกอะไรเลย |

**เคสที่ชื่อเพจช่วยไม่ได้คือเธรด `DEEP`** (ลูกค้าทักผ่านแอป/เว็บของเราเอง ไม่มี `shopChannelId`
→ `channelName` เป็น `null`) — ตอนนี้เหลือคำว่า `Deep` ล้วน ถ้าวันหนึ่งพบว่าไม่พอ ให้เอาชื่อร้านมา
เสียบ *ตรงนี้ที่เดียว* ไม่ใช่ใส่คู่กันทุกใบ

🛑 หมายเหตุถ้าจะทำ: `ShopChannel.name` (ชื่อเพจบน Facebook ตอนกดเชื่อม) กับ `Shop.shopName`
(ผู้ขายพิมพ์เองในระบบเรา) **ตั้งคนละที่ ไม่การันตีว่าเหมือนกัน** — ใช้แทนกันไม่ได้

### E2.5 `src/lib/chat-channel.ts` (ใหม่) — ทำไมต้องแยกไฟล์

ชื่อช่องทางเดิมอยู่ใน `CHANNEL_DISPLAY` ของ `ChannelBadge.tsx` ซึ่งเป็น `'use client'` และ import
Icon wrapper (→ `@iconify/react`) — ให้ service ฝั่ง server (ถูกเรียกจาก webhook ของ Meta)
import ตรง ๆ เท่ากับลาก React + icon set เข้า bundle ของ route นั้นเพียงเพื่ออ่านสตริงสามคำ

🛑 **แต่จะ "พิมพ์คำเอง" ก็ไม่ได้ (Hard Rule 16)** — ถ้าวันหนึ่งมีคนแก้คำที่ไฟล์เดียว ผู้ขายจะเห็น
ช่องทางเดียวกันถูกเรียกคนละชื่อบนสองหน้าจอ **โดยไม่มี tsc/build ตัวไหนฟ้อง** (มันเป็นสตริงที่ถูกต้อง
ทั้งคู่) → แยกเป็น lib บริสุทธิ์ แล้ว `ChannelBadge` ดึงคำจากที่นั่น + re-export ให้ call-site เดิมใช้ต่อได้

ส่วนที่เป็น "หน้าตา" (ไอคอน/โลโก้แบรนด์/สี) ยังอยู่ที่ `CHANNEL_DISPLAY` ตามเดิม

### E2.6 ข้อจำกัดที่ยอมรับ — Android

`subtitle` เป็น **iOS-only** — ตอนนี้ปล่อยเฉพาะ iOS จึงยังครบ

🛑 วันที่ปล่อย Android ต้องเพิ่มคอลัมน์ `platform` ใน `PushToken` ก่อน (ตอนนี้เก็บแค่
`userId` + `token` แยกรูปแบบข้อความรายเครื่องไม่ได้) แล้วส่งแบบยัดรวมใน `title` เฉพาะ Android
— **การที่ Android ไม่ error ไม่ได้แปลว่าไม่มีใครต้องการข้อมูลนี้ มันแค่หายไปเงียบ ๆ**

ระหว่างนี้ใส่ `channel` + `channelName` ลง `data` ไปด้วยแล้ว → ฝั่งแอปหยิบไปทำเองได้ทันที
(จัดกลุ่ม noti ตามเพจ / วาดหน้าตาเอง / รองรับ Android) โดยไม่ต้องแก้เว็บซ้ำแล้วรอ deploy อีกรอบ

### E2.7 การพิสูจน์

- `npx tsc --noEmit` — 0 error (ต้องรัน `prisma generate` ก่อน เพราะ client ในเวิร์กทรีเก่ากว่า
  schema ของทีมเรื่องฟิลด์ `chatScopeMode` — ไม่เกี่ยวกับงานนี้ และไม่แตะฐานข้อมูล)
- **เทียบ baseline ด้วย `git stash`**: ก่อนแก้ 30 ไฟล์ไม่ผ่าน / 18 จาก 1406 · หลังแก้
  **30 ไฟล์เดิมรายชื่อตรงกันทุกบรรทัด / 18 จาก 1415** → ไม่มี regression
  (ที่แดงคือ e2e ที่ต้องมี DB + หนี้เดิมของทีม)
- เทสใหม่ 9 ข้อ (`src/lib/__tests__/chat-channel.test.ts`,
  `src/services/__tests__/seller-push-channel-line.test.ts`) — คลุม `channelName` เป็น `null` /
  ช่องว่างล้วน / ช่องทางที่ไม่รู้จัก / ห้ามมี `· ` ค้างท้าย
- grep gate HR7 / HR9 / HR12 บนไฟล์ที่แตะ = 0 ทั้งหมด

### E2.8 ยังไม่ได้ทำ

- **จัดกลุ่ม noti ตามเพจ/ร้านด้วย `thread-id` ของ iOS** — ตรงโจทย์ผู้ขายหลายร้านมาก แต่
  **ยังไม่ยืนยันว่า Expo Push API เปิดฟิลด์นี้ให้** (เราเรียก `exp.host` ตรง ๆ ไม่ผ่าน SDK)
  ต้องเช็คก่อนถึงจะทำ
- ยังไม่ได้ทดสอบบนเครื่องจริง — ต้องรอ deploy แล้วให้ลูกค้าทักเข้ามาจริง (ดู E2.9)

### E2.9 วิธีทดสอบบนเครื่องจริง

1. deploy เว็บ (push เป็นสิ่งที่ server ส่งไปทั้งก้อน — **ไม่ต้อง build/OTA แอป**)
2. ให้ลูกค้าทักเข้าเพจ Facebook → บรรทัดบนสุดเป็น **ชื่อเพจล้วน** (ไม่มี `Messenger` นำหน้า) · บรรทัดกลางเป็นชื่อคนส่ง · บรรทัดล่างเป็นข้อความ
3. ทักผ่าน Instagram → บรรทัดบนสุดเป็นชื่อบัญชี IG
4. ทักผ่านแอป/เว็บ Deep → บรรทัดบนสุดเป็น `Deep` (เคส fallback — `title` ห้ามว่าง)
5. throttle 25 วินาทีต่อเธรดยังทำงานเหมือนเดิม (พิมพ์รัวได้ noti ใบเดียว)

**สถานะ:** ยืนยันบนเครื่องจริงแล้ว 2 รอบ (18:14 และ 18:50 น. — หัวหน้าส่งภาพ noti มาทั้งสองครั้ง)
แต่ทั้งสองรอบเป็น**ลำดับก่อน E2.3b** — ลำดับสุดท้ายยังไม่เคยเห็นบนเครื่องจริง เหลือข้อ 2–5 ต้องกดใหม่
