---
title: "TestCase — 00036 Service Order Surface"
owner: shinobu22
status: draft
module: M00036-ServiceOrderSurface
version: "1.0"
created: 2026-08-07
tags: [feature, testcase, service-queue, orders, appointment]
related: ["[[BRD]]", "[[SRS]]", "[[API]]"]
---

> **โมดูล:** M00036-ServiceOrderSurface · **วันที่:** 2026-08-07
> **สถานะการทดสอบ:** static ผ่านครบ · **browser QA ยังไม่เคยกดจริงสักเคส**

# TestCase: หน้าการเข้ารับบริการสำหรับร้านคิวงาน

---

## 1. ที่รันแล้ว (อัตโนมัติ)

| เครื่องมือ | ผล | ครอบอะไร |
|-----------|-----|---------|
| `tsc --noEmit` | **0 error** | type ทั้งโปรเจกต์ |
| `vitest run src/lib/` | **749 passed / 57 files** | รวมเทส `ORDER_VOCAB` ที่ครอบ `fulfillLabel` ครบ 3 vertical แบบ `toEqual` เป๊ะ |
| `next build` | **EXIT=0** | ไม่มี RSC serialization error / prerender error |
| `detect.mjs` × 5 ไฟล์ | **exit 0 ทุกไฟล์** | design detector |
| `theme-guard.sh` × 10 ไฟล์ | **exit 0 ทุกไฟล์** | HR7 / HR9 / HR10 / HR12 |
| grep gate HR9 / HR10 | **0 hit** | `react-toastify` และ chart lib ใน `(paces)` |

**สิ่งที่เครื่องมือเหล่านี้มองไม่เห็น** (จึงต้องกดเอง): ไอคอนที่ชื่อถูกแต่สื่อผิด · คอลัมน์ที่ล้นจอ · ชิปที่เลื่อนไปนอกจอโดยไม่มี scrollbar · ปุ่มที่กดพลาดง่าย · สีในโหมดมืด

---

## 2. Browser QA — ยังไม่ได้กดสักเคส

### 2.1 ร้าน `SERVICE_QUEUE` ที่ใช้ระบบนัด (เช่น BT Premium)

| # | เคส | คาดหวัง | ผล |
|---|-----|--------|-----|
| T-1 | เปิด `/orders` เดสก์ท็อป | ไม่มีหัวคอลัมน์ "ที่อยู่จัดส่ง" · ไม่มีข้อความ "ยังไม่มีที่อยู่"/"ไม่มีหมายเลขพัสดุ" ที่ไหนเลย | ⬜ |
| T-2 | ดู toolbar | ไม่มีดรอปดาวน์ "ขนส่ง" · มีดรอปดาวน์ "สถานะนัด" พร้อมตัวเลขต่อกอง | ⬜ |
| T-3 | ดูคอลัมน์ "นัดหมาย" | วันเวลา + ชื่อคิวงาน + ป้ายสถานะ ครบ 3 อย่างในแถวที่มีนัด | ⬜ |
| T-4 | หาปุ่ม "ดึงจาก iShip" | ไม่มีทั้งมือถือและเดสก์ท็อป | ⬜ |
| T-5 | ดูเช็กลิสต์คอลัมน์ "สถานะ" | เขียนว่า "เริ่มให้บริการแล้ว" ไม่ใช่ "ยืนยันการจัดส่ง" | ⬜ |
| T-6 | กดดรอปดาวน์ "สถานะนัด" → เลือกกองหนึ่ง | **จำนวนแถวที่เหลือ = ตัวเลขบนตัวเลือกนั้นเป๊ะ** | ⬜ |
| T-7 | รีเฟรชหลังกรอง | ตัวกรองยังอยู่ (`?appt=` ใน URL) | ⬜ |
| T-8 | กดปุ่ม back ของเบราว์เซอร์ | กลับไปสถานะกรองก่อนหน้า | ⬜ |

### 2.2 มือถือ (390px)

| # | เคส | คาดหวัง | ผล |
|---|-----|--------|-----|
| T-9 | แถวชิป 6 ตัว | เลื่อนได้ · **ตรวจว่ามีสัญญาณว่ายังมีชิปทางขวาไหม** (scrollbar ถูกซ่อน — จุดเสี่ยงที่ Assessment A ชี้) | ⬜ |
| T-10 | ปัดซ้าย/ขวาทั้งจอ | สลับชิปสถานะนัดได้เหมือนแกนอื่น | ⬜ |
| T-11 | การ์ดของใบที่มีนัด | มีบล็อกนัดหมาย (ไอคอน + วันเวลา + ชื่อคิว + ป้าย) ในตำแหน่งที่บล็อกพัสดุเคยอยู่ | ⬜ |
| T-12 | การ์ดของใบ walk-in | **ไม่มีบล็อกนี้เลย** ไม่ใช่บล็อกว่าง | ⬜ |
| T-13 | ชื่อคิวงานยาว ๆ + ป้ายยาว | ดูว่าอะไรถูกตัดก่อน อ่านออกไหม (บล็อกเดียว 4 ชิ้นไม่มี wrap) | ⬜ |
| T-14 | เปิดโมดัลตัวกรอง → เลือกสถานะการขาย → ปิด | **จุดแดงบนปุ่มตัวกรองต้องติด** (บั๊กที่เพิ่งแก้ `2f50f6ba`) | ⬜ |

### 2.3 หน้ารายละเอียด — การ์ดการนัดหมาย

| # | เคส | คาดหวัง | ผล |
|---|-----|--------|-----|
| T-15 | เปิดใบที่มีนัดและถึงเวลาแล้ว | เห็นการ์ด "การนัดหมาย" เป็นการ์ดแรกของคอลัมน์ข้าง + ปุ่ม 2 ปุ่มกดได้ | ⬜ |
| T-16 | กด "ให้บริการแล้ว" | Swal ปุ่ม**เขียว** + ไอคอน warning + ข้อความมีชื่อลูกค้า + วันนัด + "ย้อนกลับไม่ได้" | ⬜ |
| T-17 | ยืนยัน | toast + ป้ายเปลี่ยนเป็น "ให้บริการแล้ว" (เขียว) + **ปุ่มหายทั้งคู่** | ⬜ |
| T-18 | ตรวจ DB หลัง T-17 | `appointmentStatus='COMPLETED'` และ **`Order.status` ไม่เปลี่ยน** | ⬜ |
| T-19 | กด "ไม่มาตามนัด" ในอีกใบ | Swal ปุ่ม**แดง** (ไม่ใช่เหลือง) | ⬜ |
| T-20 | เปิดใบที่ยังไม่ถึงเวลานัด | ปุ่มจาง กดไม่ได้ + ข้อความ "ปิดผลได้ตั้งแต่ …" อ่านชัด | ⬜ |
| T-21 | **เปิดใบไว้รอจนถึงเวลานัด (ไม่รีเฟรช)** | **ปุ่มปลดล็อกเอง** — เคสสำคัญที่สุดของรอบแก้ (`2f50f6ba`) | ⬜ |
| T-22 | เปิด 2 แท็บ กดปิดผลทั้งคู่ | แท็บที่สองได้ toast "นัดนี้ถูกปิดผลไปแล้ว" ไม่ crash | ⬜ |
| T-23 | ใบที่ไม่มีนัด (walk-in) | **ไม่มีการ์ดนี้เลย** | ⬜ |
| T-24 | ใบงานบริการ walk-in | **ไม่เจอฟอร์ม "กรอก URL เพื่อส่งมอบให้ผู้ซื้อ"** (บั๊กที่แก้ใน `2f50f6ba`) | ⬜ |
| T-25 | ปุ่มบนมือถือ | สูง ≥44px · ห่างกันพอไม่กดพลาด | ⬜ |
| T-26 | สถานะ "ลูกค้าขอเลื่อน" | ดูว่าผู้ขายเข้าใจไหมว่าต้องทำอะไรต่อ (**รู้อยู่แล้วว่าเป็นทางตัน** — ดู §4) | ⬜ |

### 2.4 ร้าน walk-in ล้วน (ไม่เคยใช้คิวงาน)

| # | เคส | คาดหวัง | ผล |
|---|-----|--------|-----|
| T-27 | เปิด `/orders` | ไม่มีคอลัมน์นัดหมาย · ไม่มีดรอปดาวน์สถานะนัด · ไม่มีชิปสถานะนัด · ไม่มีคอลัมน์ที่อยู่จัดส่ง | ⬜ |
| T-28 | แถวชิปมือถือ | เป็น "สถานะการขาย" ตามเดิม | ⬜ |
| T-29 | หน้าจอโดยรวม | ไม่มีช่องว่าง/คอลัมน์เปล่าที่ต้องอธิบาย | ⬜ |

### 2.5 Regression — ห้ามเปลี่ยน

| # | เคส | คาดหวัง | ผล |
|---|-----|--------|-----|
| T-30 | ร้าน `ONLINE_SALES` `/orders` | คอลัมน์ที่อยู่ · ดรอปดาวน์ขนส่ง · ชิปพัสดุ · hover panel · เช็กลิสต์ "ยืนยันการจัดส่ง" เหมือนเดิมทุกจุด | ⬜ |
| T-31 | ร้าน `ONLINE_SALES` ที่เชื่อม iShip | ปุ่ม "ดึงจาก iShip" ยังอยู่และใช้ได้ | ⬜ |
| T-32 | ร้าน `ONLINE_SALES` ที่**ยังไม่**เชื่อม iShip | ปุ่มหายไป (พฤติกรรมใหม่ที่ถูกต้อง — เดิมโชว์ทุกร้าน) | ⬜ |
| T-33 | ร้าน `LODGING` | ทุกอย่างเหมือนเดิม ยกเว้นปุ่ม iShip ที่หายไป · เช็กลิสต์เขียน "รับเข้าพักแล้ว" | ⬜ |
| T-34 | ออเดอร์เก่าที่มีพัสดุจริงในร้าน `SERVICE_QUEUE` | หน้ารายการไม่โชว์พัสดุ แต่**เปิดใบแล้วยังเห็นที่อยู่/พัสดุครบ** (BR-SOV-10) | ⬜ |
| T-35 | ปฏิทินคิวงาน `/queues` | "ลูกค้ายืนยันแล้ว" = น้ำเงิน · "ให้บริการแล้ว" = เขียว (สลับจากของเดิม) และตรงกับป้ายในหน้า orders | ⬜ |

### 2.6 โหมดมืด + a11y

| # | เคส | คาดหวัง | ผล |
|---|-----|--------|-----|
| T-36 | สลับโหมดมืด (toggle ใน topbar) | ป้ายสถานะนัดทั้ง 5 อ่านออก · ปุ่มขอบเขียว/แดงบนการ์ดมืดยังอ่านออก (**ยังไม่มีใครเคยเห็น**) | ⬜ |
| T-37 | Tab ไล่ทั้งการ์ด | ปุ่มปิดผลโฟกัสได้ · มีเส้นโฟกัสเห็นชัด | ⬜ |
| T-38 | screen reader | แถววันเวลาอ่านว่า "วันเวลานัด …" · แถวคิวงานอ่านว่า "คิวงาน …" (sr-only ที่เพิ่งเพิ่ม) | ⬜ |

---

## 3. E2E (Playwright) — ยังไม่ได้เขียน

| # | flow | หมายเหตุ |
|---|------|---------|
| E-1 | seller SERVICE_QUEUE → `/orders` → กรองสถานะนัด → ยืนยันจำนวนแถว = ตัวเลขบนชิป | ตรวจ BR-SOV-06 แบบอัตโนมัติ |
| E-2 | เปิดใบ → ปิดผล COMPLETED → ยืนยันว่า `Order.status` ไม่เปลี่ยน | ตรวจ BR-RSV-33 |
| E-3 | ยิง `outcome` ซ้ำ → 409 | ตรวจ guard |
| E-4 | seller ONLINE_SALES → ยืนยันว่าคอลัมน์ที่อยู่ยังอยู่ | regression |

🛑 เทสทุกตัวต้องใช้ `deleteTestData({ userIds, shopIds })` เท่านั้น — ห้าม `deleteMany()` ไม่มี `where` (Hard Rule 13)

---

## 3b. Browser QA รอบ 2 (`6fc3f12e`) — ยังไม่ได้กดเช่นกัน

| # | เคส | คาดหวัง | ผล |
|---|-----|--------|-----|
| T-39 | ชิป/ตัวเลือก "ไม่มีนัด" | อยู่ถัดจาก "ทั้งหมด" ทั้งชิปมือถือและดรอปดาวน์เดสก์ท็อป · กดแล้วเหลือเฉพาะใบ walk-in · **เลข "ทั้งหมด" = "ไม่มีนัด" + 5 สถานะ พอดี** | ⬜ |
| T-40 | ตารางเดสก์ท็อป + การ์ดรายละเอียด | เห็นช่วงเต็ม "1 ส.ค. 09:00–10:30" ไม่ใช่แค่เวลาเริ่ม | ⬜ |
| T-41 | นัดทั้งวัน | ทุก surface เขียน "· ทั้งวัน" ไม่มีช่วงเวลาโผล่ | ⬜ |
| T-42 | ไอคอน `user-cog` | เมนู "คิวงาน" · หน้า `/queues` · onboarding · ตาราง orders · การ์ดนัด ตรงกันหมด | ⬜ |
| T-43 | กด "เลื่อนนัด" | แผงเต็มจอเปิด · คิวงานเดิม pre-select · วัน/เวลาเดิมเติมไว้ | ⬜ |
| T-44 | กดเลือกวันในแผงเลื่อนนัด | ปฏิทินซ้อนเปิด · **วันเดิมของนัดนี้ต้องไม่ขึ้นว่าเต็ม** (`excludeOrderToken`) | ⬜ |
| T-45 | เปลี่ยนเวลาเริ่ม | เวลาสิ้นสุดเติมอัตโนมัติตาม `durationMinutes` และแก้ทับได้ | ⬜ |
| T-46 | ยืนยันเลื่อนนัด | toast "เลื่อนนัดแล้ว · ลูกค้าต้องยืนยันนัดใหม่อีกครั้ง" · การ์ดกลับเป็น "นัดแล้ว" · `rescheduleCount` +1 | ⬜ |
| T-47 | เลื่อนไปทับช่วงที่เต็ม | 409 พร้อมชื่อคิวงาน+ความจุ · **แผงเปิดค้างให้แก้ต่อ** ไม่ปิดทิ้ง | ⬜ |
| T-48 | สถานะ "ลูกค้าขอเลื่อน" | การ์ดมีแถบซ้าย info + กล่องข้อความลูกค้า + ปุ่มเขียนว่า "เลือกเวลาใหม่ให้ลูกค้า" | ⬜ |
| T-49 | นัดทั้งวัน + เปิดแผงเลื่อนนัด | ไม่มีช่องเวลาเริ่ม/สิ้นสุด (มีแต่วัน) และบันทึกแล้วยังเป็นนัดทั้งวัน | ⬜ |
| T-50 | ร้านที่ปิดคิวงานหมด | แผงบอก "ไม่มีคิวงานที่เปิดใช้งานอยู่" + ปุ่มยืนยันกดไม่ได้ | ⬜ |
| T-51 | เลื่อน 2 แท็บพร้อมกัน / อีกแท็บปิดผลไปแล้ว | 409 TERMINAL → **ปิดแผง + รีเฟรช** (ต่างจาก error อื่น) | ⬜ |
| T-52 | ปฏิทินเลือกวัน (`AppointmentDateSheet`) | ป้ายสถานะใช้สีชุดเดียวกับ `/queues` และหน้า orders ทั้งหมด (เพิ่งดึงเข้า SSOT) | ⬜ |
| T-53 | ฟอร์มสร้างออเดอร์ (regression) | ตั้งนัดครั้งแรกยังทำงานเหมือนเดิมทุกอย่าง (`excludeOrderToken` ไม่ส่ง = พฤติกรรมเดิม) | ⬜ |
| T-54 | เลื่อนนัดบนมือถือ | แผงเต็มจอตรึง scroll ได้ · ลากนิ้วบนตัวแผงไม่เลื่อนหน้าข้างหลัง · ปุ่มยืนยันอยู่ในโซนนิ้วโป้ง | ⬜ |

---

## 3c. ป้ายนัดในแถวกล่องแชท (ส่วนขยาย 2026-08-08 — SRS §3.1)

**เทสอัตโนมัติที่รันแล้ว:** `vitest run src/lib/__tests__/order-stage.test.ts` → **31 passed** (เพิ่ม 9 เคสของนัด: SCHEDULED/`appointmentStatus` ว่าง/CONFIRMED_BY_BUYER/3 สถานะที่ไม่เดินตามแผน/CANCELLED ชนะ/ไม่มี `serviceStart`/ISO string/ข้ามวันเมื่อคิดเป็น UTC)

| # | เคส | คาดหวัง | ผล |
|---|-----|--------|-----|
| T-55 | ร้านคิวงาน เปิด `/inbox` แล้วดูแถวลูกค้าที่มีนัด | ชิปขึ้น **"นัด 16 ส.ค. 69"** ไม่ใช่ "สั่งซื้อแล้ว" · ไอคอน `calendar-event` | ⬜ |
| T-56 | ลูกค้ายืนยันนัดแล้ว | ข้อความเดิม แต่ชิปเป็นสี primary + ไอคอน `calendar-check` (**ห้ามเขียว**) | ⬜ |
| T-57 | กดปิดผล "ให้บริการแล้ว" / "ไม่มาตามนัด" / ลูกค้าขอเลื่อน แล้วกลับมาดูกล่องแชท | ชิปเปลี่ยนเป็น **คำสถานะ** ("ให้บริการแล้ว" เขียว / "ไม่มาตามนัด" แดง / "ลูกค้าขอเลื่อน" info) ไม่ใช่วันที่ค้าง | ⬜ |
| T-58 | ยกเลิกออเดอร์นัด | ชิปเป็น "ยกเลิกแล้ว" ตามเดิม (ข้อมูลนัดไม่ทับ) | ⬜ |
| T-59 | ร้านคิวงานที่ออเดอร์เป็น walk-in (ไม่มีนัด) | ชิป "สั่งซื้อแล้ว" ตามเดิม | ⬜ |
| T-60 | ร้าน `ONLINE_SALES` (regression) | ชิปทุกขั้นเหมือนเดิมทั้งชุด — สั่งซื้อแล้ว/สร้างพัสดุแล้ว/พิมพ์ N ครั้ง/กำลังจัดส่ง/จัดส่งสำเร็จ | ⬜ |
| T-61 | มือถือ 320px | ชิปไม่ดันแถวสูงขึ้น · ข้อความยาวถูกตัดด้วย `…` ไม่ตกบรรทัด | ⬜ |
| T-62 | hover ชิปบนเดสก์ท็อป | `title` ขึ้นข้อความเต็ม · กดแล้วยังเปิดแผงรายการออเดอร์เหมือนเดิม | ⬜ |
| T-63 | กล่องแชทรวมหลายร้าน (00037) | เธรดของร้านคิวงานขึ้นป้ายนัด · เธรดของร้านขายของในกล่องเดียวกันยังขึ้นป้ายเดิม | ⬜ |

---

## 4. สิ่งที่รู้อยู่แล้วว่ายังไม่ปิด (ไม่ใช่บั๊กที่รอค้นพบ)

| # | เรื่อง | สถานะ |
|---|-------|-------|
| 1 | ปิดผล/เลื่อนนัดได้ทีละใบ ไม่มี bulk | ร้านที่เคลียร์คิวตอนเย็น 12 ใบ = 12 รอบ |
| 2 | ไม่มี `OrderEvent` ชนิดนัดหมาย → ไม่มีร่องรอยว่าใครกดปิดผล/เลื่อนตอนกี่โมง (มี `AppointmentReschedule` แต่ไม่ได้แสดงรายละเอียด แสดงแค่จำนวนครั้ง) | PRD Out-of-scope #6 |
| 3 | เกณฑ์ `hasAppointmentAxis` = มีนัด > 0 ใบ — ร้านที่มีนัดใบเดียวใน 500 ใบก็ถูกสลับแถวชิปทั้งแถว | user เคาะให้คงเกณฑ์เดิม + เพิ่มชิป "ไม่มีนัด" แทน (2026-08-07) |
| 4 | หัวคอลัมน์ยังเป็น "รายการสินค้า" ในร้านที่ขายบริการ ทั้งที่ `PRODUCT_VOCAB.itemColLabel` มีอยู่แล้ว | นอก FR ของรอบนี้ |
| 5 | ไทล์ shortcut บนหน้าแรกยังเป็น `solar:armchair-2-bold-duotone` | คนละ icon set กับ `user-cog` (tabler) ต้องเลือกตัวแทนแยก — ยังไม่ได้เคาะ |
| 6 | ฝั่งร้านเลื่อนนัดไปเวลาที่เป็นอดีตได้ | service ไม่ได้ห้าม (ต่างจากฝั่งลูกค้า) — ถ้าธุรกิจอยากห้าม ต้องเพิ่ม guard ที่ service ไม่ใช่กันที่ UI |
| 7 | ชิปในกล่องแชทของนัดที่ **เลยเวลาไปแล้วแต่ยังไม่มีใครกดปิดผล** ยังโชว์ "นัด 16 ส.ค. 69" ตามเดิม | ไม่มีสถานะ "เลยกำหนด" ในระบบ — เป็น business rule ใหม่ที่ต้องให้ `safepay-product` นิยามเกณฑ์ก่อน (กี่ชั่วโมงหลัง `serviceStart`) |
| 8 | ชิปแสดง **วันที่อย่างเดียว ไม่มีเวลาและไม่มียอดมัดจำ** | user เคาะ 2026-08-08 (มัดจำไม่ต้องแสดง) · เวลา/มัดจำอ่านได้จากแผงออเดอร์ที่กดชิปแล้วเปิดขึ้น |

### ปิดไปแล้วในรอบ 2 (`6fc3f12e`)

- ~~`RESCHEDULE_REQUESTED` เป็นทางตัน~~ — มีแผงเลื่อนนัดแล้ว
- ~~ไม่แสดง `serviceEnd` ที่ไหนเลย~~ — ตาราง + การ์ดรายละเอียดแสดงช่วงเต็ม
- ~~ไอคอน `armchair` ยังไม่ได้เคาะ~~ — user เลือก `user-cog` เปลี่ยนครบทุกจุดที่หมายถึงคิวงาน
- ~~ไม่มีชิป "ไม่มีนัด"~~ — เพิ่มแล้วทั้ง 2 surface
