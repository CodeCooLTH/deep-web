---
title: "TestCase — Service Appointment Booking"
owner: shinobu22
status: draft
module: M00024-ServiceAppointmentBooking
version: "1.0"
created: 2026-07-30
tags: [feature, test, qa, e2e, booking, appointment]
related: ["[[PRD]]", "[[BRD]]", "[[SRS]]", "[[SDS]]", "[[API]]", "[[DATABASE]]"]
---

> **โมดูล:** M00024-ServiceAppointmentBooking
> **ประเภทเอกสาร:** Test Cases
> **เวอร์ชัน:** 1.0
> **วันที่จัดทำ:** 2026-07-30
> **สถานะ:** Draft — ยังไม่รัน (ยังไม่มี implementation)
> **เจ้าของเอกสาร:** safepay-qa (ดู [[Feature-Docs-Ownership]])

# TestCase: ระบบนัดหมายวันเข้าใช้บริการ

---

## 1. Overview

### 1.1 ขอบเขตการทดสอบ

| ระดับ | เครื่องมือ | ครอบอะไร |
|-------|-----------|----------|
| **Unit** | Vitest | การจัดสรรที่นั่ง, ตัวกั้นฟีเจอร์, การตรวจ error, state transition |
| **Integration (DB)** | Vitest + Prisma บน DB จริง (transaction + rollback) | EXCLUDE constraint, CHECK, FK RESTRICT, การกดพร้อมกัน |
| **E2E** | Playwright | โหมด A, โหมด B, ปฏิทิน, zero-regression |
| **Visual/Browser QA** | Chrome DevTools MCP / browser-harness | ตัดสิน "สวย/เป็นแบรนด์ไหม" + payload ไม่มี PII |

### 1.2 ข้อบังคับตาม memory ของโครงการ

- **E2E เป็นภาคบังคับ** ทุกเมนู — bypass login ด้วย `e2e/helpers/auth.ts` (`feedback_qa_playwright_e2e_mandatory`)
- QA ทดสอบบน `*.deepth.local:4000` — **ห้าม `localhost`** (`feedback_qa_domains`, `project_dev_port_4000`)
- 🛑 **QA ห้ามรัน `prisma db pull`** เด็ดขาด (`feedback_qa_agent_no_prisma_pull`) — จะลบ EXCLUDE ทิ้ง
- grep + type-check ผ่าน ≠ ใช้ได้ — ต้องกดจริงในเบราว์เซอร์ (`feedback_browser_qa_catches_what_static_misses`)

### 1.3 ข้อมูลทดสอบที่ต้องเตรียม

| ชื่อ | ค่า |
|------|-----|
| ร้าน A | `kind = BUSINESS`, `vertical = GENERAL` — ร้านหลักที่ใช้ทดสอบ |
| ร้าน B | `kind = PERSONAL`, `vertical = GENERAL` — ต้องถูกกั้น |
| ร้าน C | `kind = BUSINESS`, `vertical = LODGING` — ต้องถูกกั้น |
| ทรัพยากร R1 | ความจุ 1 ("หมอนวด A") |
| ทรัพยากร R2 | ความจุ 8 ("คลาสเช้า") |
| ทรัพยากร R3 | `isActive = false` |

---

## 2. Test Scenarios

### 2.1 กลุ่ม A — การกันจองเกินความจุ (Integration, สำคัญที่สุด)

| ID | เคส | ขั้นตอน | ผลที่คาดหวัง | อ้างอิง |
|----|-----|---------|--------------|---------|
| **TC-A01** | ที่นั่งต่างกัน เวลาทับกัน | R2 (ความจุ 8): จอง 8 นัดที่ 09:00-10:00 | สำเร็จทั้ง 8 | BR-RSV-06 |
| **TC-A02** | เกินความจุ | ต่อจาก A01 จองรายที่ 9 ช่วงเดิม | 409 `APPOINTMENT_SLOT_FULL` | BR-RSV-16 |
| **TC-A03** | ทับบางส่วน | R1 มีนัด 14:00-15:30 → จอง 15:00-16:00 | 409 | BR-RSV-16 |
| **TC-A04** | ต่อกันพอดี | R1 มีนัด 14:00-15:30 → จอง 15:30-16:30 | สำเร็จ | BR-RSV-14 |
| **TC-A05** | 🛑 **กดพร้อมกัน** | R2 ความจุ 8 → ยิง **12 request พร้อมกัน** ช่วงเวลาเดียวกัน | **สำเร็จพอดี 8, ล้มเหลว 4** ด้วย 409 — ห้ามเกิน 8 เด็ดขาด | NFR-1, BR-RSV-18.1 |
| **TC-A06** | ยกเลิกแล้วคืนที่ว่าง | เต็ม 8/8 → ยกเลิก 1 → จองใหม่ | สำเร็จ | BR-RSV-17 |
| **TC-A07** | ทรัพยากรอื่นไม่กวนกัน | R1 เต็ม → จอง R2 ช่วงเดียวกัน | สำเร็จ | — |
| **TC-A08** | ที่นั่งแรกชนแล้วต้องไปที่นั่งถัดไป | R2 ความจุ 3, ที่นั่ง 1 เต็ม → จองใหม่ | สำเร็จ (ต้องไม่ error) — **จับการลืม `SAVEPOINT`** | SRS §8 |

> **TC-A05 คือเทสที่ห้ามข้าม** — เป็นเทสเดียวที่จับได้ว่า developer implement ด้วย `count()` แทนการวนที่นั่ง ถ้า implement ผิด เทสนี้จะได้ผลสำเร็จ > 8

### 2.2 กลุ่ม B — ตัวกั้นฟีเจอร์

| ID | เคส | ผลที่คาดหวัง | อ้างอิง |
|----|-----|--------------|---------|
| **TC-B01** | ร้าน A เข้าหน้าตั้งค่าทรัพยากร | เข้าได้ | BR-RSV-01 |
| **TC-B02** | ร้าน B (`PERSONAL`) เข้าหน้าเดียวกัน | ถูก redirect ออก + เมนูไม่แสดง | BR-RSV-02 |
| **TC-B03** | ร้าน C (`LODGING`) เข้าหน้าเดียวกัน | ถูก redirect ออก + เมนูไม่แสดง | BR-RSV-02 |
| **TC-B04** | 🛑 ร้าน B เรียก API ตรงด้วย curl | **403** — ไม่ใช่แค่ซ่อนเมนู | TFR-001 |
| **TC-B05** | ร้าน C เรียก API ตรง | 403 | TFR-001 |
| **TC-B06** | ร้าน C ใช้ปฏิทินบ้านพักเดิม | ทำงานเหมือนเดิมทุกประการ | BR-RSV-03 |

### 2.3 กลุ่ม C — ทรัพยากร

| ID | เคส | ผลที่คาดหวัง | อ้างอิง |
|----|-----|--------------|---------|
| **TC-C01** | สร้างทรัพยากรโดยไม่ระบุความจุ | `capacity = 1` | BR-RSV-06 |
| **TC-C02** | สร้างด้วยความจุ 0 / -1 / 2.5 | 400 ทุกกรณี | BR-RSV-06 |
| **TC-C03** | เพิ่มความจุ 2 → 5 | สำเร็จ และจองเพิ่มได้ทันที | BR-RSV-06.2 |
| **TC-C04** | ลดความจุ 5 → 2 ขณะมีนัดใช้ที่นั่ง 4 | 409 `CAPACITY_REDUCTION_BLOCKED` + บอกออเดอร์ที่ติด | BR-RSV-06.2 |
| **TC-C05** | ลดความจุขณะที่นัดที่ติดผ่านไปแล้ว | สำเร็จ (นัดในอดีตไม่บล็อก) | SDS §3.4 |
| **TC-C06** | ปิดการใช้งานทรัพยากร | เลือกสำหรับนัดใหม่ไม่ได้ (409) แต่นัดเดิมยังเปิดดูได้ | BR-RSV-07 |
| **TC-C07** | ลบทรัพยากรที่มีนัด | 409 + ข้อความแนะให้ปิดการใช้งานแทน | BR-RSV-08 |
| **TC-C08** | ลบทรัพยากรที่ไม่มีนัด | 204 | — |
| **TC-C09** | แก้ระยะเวลามาตรฐานหลังมีนัดแล้ว | นัดเดิมเวลาไม่เปลี่ยน | BR-RSV-09 |
| **TC-C10** | ร้าน A เรียกทรัพยากรของร้านอื่น | 404 | BR-RSV-22 |

### 2.4 กลุ่ม D — โหมด A (ร้านเลือกวันเอง, E2E)

| ID | เคส | ผลที่คาดหวัง | อ้างอิง |
|----|-----|--------------|---------|
| **TC-D01** | สร้างออเดอร์พร้อมนัดในฟอร์มเดียว | สำเร็จ, `type = SERVICE`, สถานะนัด `SCHEDULED` | FR-RSV-03 |
| **TC-D02** | 🛑 สร้างออเดอร์**ไม่**ระบุนัด | สำเร็จ, ฟิลด์นัดเป็น NULL ทั้งหมด, เส้นทางเดิมไม่เปลี่ยน | BR-RSV-04 |
| **TC-D03** | เลือกทรัพยากรแล้วดูช่วงที่จองไปแล้ว | เห็นจำนวนจองแล้ว/ความจุต่อช่วง | FR-RSV-03 |
| **TC-D04** | เลือกทรัพยากรที่มีระยะเวลามาตรฐาน | เวลาสิ้นสุดถูกเติมอัตโนมัติ และแก้ได้ | FR-RSV-03 |
| **TC-D05** | เวลาสิ้นสุด ≤ เวลาเริ่ม | 400 | BR-RSV-13 |
| **TC-D06** | ตั้งนัดย้อนหลัง | สำเร็จ แต่ต้องเห็นคำเตือนก่อนบันทึก | BR-RSV-15 |
| **TC-D07** | ปฏิทินคิวรายวัน/รายสัปดาห์ | แสดงครบ, กรองตามทรัพยากรได้, กดไปหน้าออเดอร์ได้ | FR-RSV-04 |
| **TC-D08** | ปฏิทินไม่แสดงนัดที่ยกเลิก | ไม่แสดงโดยค่าเริ่มต้น | FR-RSV-04 |
| **TC-D09** | วันที่ในปฏิทินเป็น พ.ศ. | ทุกจุดเป็น พ.ศ. + เวลาไทย | BR-RSV-41 |

### 2.5 กลุ่ม E — โหมด B (ลูกค้ายืนยันผ่านลิงก์, E2E)

| ID | เคส | ผลที่คาดหวัง | อ้างอิง |
|----|-----|--------------|---------|
| **TC-E01** | เปิดลิงก์โดยยังไม่ login | ถูกบังคับ login ตามกติกา 00015 (ไม่เห็นนัด) | BR-RSV-20 |
| **TC-E02** | login ด้วยบัญชีที่เบอร์ไม่ตรง | ถูกบล็อกตามกติกา 00015 เดิม | BR-RSV-20 |
| **TC-E03** | login สำเร็จ + ออเดอร์มีนัด | เห็นวัน เวลา ชื่อทรัพยากร สถานะ | FR-RSV-05 |
| **TC-E04** | 🛑 login สำเร็จ + ออเดอร์**ไม่**มีนัด | ไม่มีส่วนวันนัดใน DOM เลย (ไม่ใช่ render แล้วซ่อน) | FR-RSV-05 |
| **TC-E05** | กดยืนยันนัด | สถานะ `CONFIRMED_BY_BUYER` + ร้านได้รับแจ้งในแชท | FR-RSV-06 |
| **TC-E06** | กดยืนยันซ้ำ | 200 เหมือนเดิม `buyerConfirmedAt` ไม่เปลี่ยน | BR-RSV-26 |
| **TC-E07** | ไม่ยืนยันเลย | นัดยังมีผลและยังกันคิว | BR-RSV-26 |
| **TC-E08** | กดขอเลื่อนนัด | สถานะ `RESCHEDULE_REQUESTED`, **เวลาเดิมยังถูกกันไว้** | BR-RSV-27 |
| **TC-E09** | 🛑 ลูกค้าพยายามเปลี่ยนวันเองผ่าน API | ไม่มี endpoint ให้ทำ / 403 | BR-RSV-23 |
| **TC-E10** | ลูกค้า X เรียก action บนออเดอร์ของลูกค้า Y | 403 | BR-RSV-21 |
| **TC-E11** | ขอเลื่อนหลังเลยเวลานัด | 409 `APPOINTMENT_PAST` | BR-RSV-31 |

### 2.6 กลุ่ม F — การเลื่อนและผลของนัด

| ID | เคส | ผลที่คาดหวัง | อ้างอิง |
|----|-----|--------------|---------|
| **TC-F01** | ร้านเลื่อนนัดไปเวลาว่าง | สำเร็จ + เวลาเดิมว่างทันที (จองแทนได้เลย) | BR-RSV-29 |
| **TC-F02** | ร้านเลื่อนไปเวลาที่เต็ม | 409 `APPOINTMENT_SLOT_FULL` + นัดเดิมไม่ถูกแตะ | BR-RSV-28 |
| **TC-F03** | เลื่อน 3 ครั้ง | `AppointmentReschedule` มี 3 แถว ไม่ทับกัน | BR-RSV-30 |
| **TC-F04** | ร้านปฏิเสธคำขอเลื่อน | สถานะกลับเป็น `SCHEDULED` เวลาเดิม | FR-RSV-07 |
| **TC-F05** | กด "ให้บริการแล้ว" ก่อนถึงเวลา | 409 `APPOINTMENT_NOT_STARTED` | BR-RSV-34 |
| **TC-F06** | กด "ให้บริการแล้ว" หลังถึงเวลา | สำเร็จ | FR-RSV-09 |
| **TC-F07** | 🛑 กด "ไม่มาตามนัด" | สถานะนัดเปลี่ยน แต่ **`Order.status` ไม่เปลี่ยน** | BR-RSV-33 |
| **TC-F08** | 🛑 หลังกด "ไม่มาตามนัด" | **Trust Score ของลูกค้าไม่เปลี่ยน** | BR-RSV-35 |
| **TC-F09** | เลื่อนนัดที่ `COMPLETED` แล้ว | 409 `APPOINTMENT_TERMINAL` | BR-RSV-31 |
| **TC-F10** | ยกเลิกออเดอร์ที่มีนัด | นัดหยุดกันคิว, ข้อมูลนัดยังอยู่ | BR-RSV-32 |

### 2.7 กลุ่ม G — Zero-regression (ต้องผ่านทั้งหมดก่อน merge)

| ID | เคส | ผลที่คาดหวัง | อ้างอิง |
|----|-----|--------------|---------|
| **TC-G01** | สร้าง/ยืนยัน/รีวิวออเดอร์สินค้าปกติครบ flow | เหมือนเดิม 100% | BR-RSV-04 |
| **TC-G02** | การจองบ้านพัก (00017) ครบ flow | เหมือนเดิม 100% | BR-RSV-03 |
| **TC-G03** | หน้า `/o/{token}` ของออเดอร์เดิม | เหมือนเดิม | TFR-009 |
| **TC-G04** | Trust Score หลัง migration | ค่าเดิมไม่เปลี่ยน | BR-RSV-36 |
| **TC-G05** | ชุดทดสอบเดิมทั้งหมด | ผ่านครบ | NFR-4 |

### 2.8 กลุ่ม H — ความปลอดภัยและต้นทุนแฝง

| ID | เคส | วิธีตรวจ | ผลที่คาดหวัง | อ้างอิง |
|----|-----|---------|--------------|---------|
| **TC-H01** | 🛑 ไม่มี SMS อัตโนมัติ | `rg "sendSms"` ในไฟล์ของฟีเจอร์ + ตรวจยอด `SellerWallet` ก่อน/หลัง flow เต็ม | 0 ผลลัพธ์ + ยอดเครดิตไม่ลด | BR-RSV-38 |
| **TC-H02** | 🛑 `serviceSeat` ไม่หลุดออก API | ตรวจ response ทุก endpoint | ไม่มี key `serviceSeat` ที่ไหนเลย | [[DATABASE]] §6 |
| **TC-H03** | 🛑 ไม่มีข้อความดิบจาก Postgres | ทำให้เกิด 409 แล้วดู response | ไม่มีคำว่า `constraint`/`tstzrange`/`23P01` | TFR-004 |
| **TC-H04** | 🛑 PII ไม่รั่วใน flight payload | เปิดปฏิทินคิวแล้วค้น payload ในเบราว์เซอร์ | ไม่มีเบอร์/อีเมลลูกค้าเต็ม | TFR-010 |
| **TC-H05** | Cache header | `curl -I` endpoint ปฏิทิน | `private, no-store` | TFR-005 |
| **TC-H06** | EXCLUDE constraint ยังอยู่หลัง migrate | query `pg_constraint` | พบ `Order_service_seat_no_overlap` | [[DATABASE]] §4.2 |
| **TC-H07** | constraint ของ 00008/00017 ยังอยู่ | query `pg_constraint` | `Order_room_no_overlap` และ partial unique index ยังอยู่ครบ | SRS §8 |

### 2.9 กลุ่ม I — UI/Visual QA

| ID | เคส | ผลที่คาดหวัง | อ้างอิง |
|----|-----|--------------|---------|
| **TC-I01** | ไม่มี emoji ในทุกหน้าใหม่ | grep ช่วง emoji = 0 | Hard Rule 12 |
| **TC-I02** | toast ใน `(paces)` เป็น `pacesToast` | `rg "react-toastify" "src/app/(paces)/"` = 0 | Hard Rule 9 |
| **TC-I03** | ไม่มี arbitrary Tailwind value | grep `text-[`/`bg-[`/`rounded-[` ในไฟล์ใหม่ = 0 (ยกเว้นที่มี comment กำกับ) | Hard Rule 7 |
| **TC-I04** | commit มี `Base:` ชี้ theme file | ตรวจ commit message | Hard Rule 3 |
| **TC-I05** | ฟอนต์ Anuphan ทุกจุด | วัด computed font | Hard Rule 5 |
| **TC-I06** | ปฏิทินใช้งานได้จริงบนมือถือ | ทดสอบที่ 375px | NFR-3 |
| **TC-I07** | 🛑 ตัดสิน "สวย/เป็นแบรนด์ไหม" | browser QA + `/impeccable critique` | ไม่ใช่แค่ render ผ่าน | `feedback_visual_quality_gate` |
| **TC-I08** | copy/error message ผ่าน | `/impeccable clarify` | ข้อความเข้าใจง่าย ไม่ใช่ศัพท์เทคนิค | Hard Rule 8 |

---

## 3. Traceability Matrix

| Business Rule | Test Case |
|---------------|-----------|
| BR-RSV-01/02 | TC-B01…B05 |
| BR-RSV-03 | TC-B06, TC-G02 |
| BR-RSV-04 | TC-D02, TC-G01 |
| BR-RSV-06 | TC-C01, TC-C02, TC-A01 |
| BR-RSV-06.2 | TC-C03, TC-C04, TC-C05 |
| BR-RSV-07 | TC-C06 |
| BR-RSV-08 | TC-C07 |
| BR-RSV-09 | TC-C09 |
| BR-RSV-13 | TC-D05 |
| BR-RSV-14 | TC-A04 |
| BR-RSV-15 | TC-D06 |
| BR-RSV-16 | TC-A02, TC-A03, **TC-A05** |
| BR-RSV-17 | TC-A06 |
| BR-RSV-18.1 | **TC-A05**, TC-A08 |
| BR-RSV-19 | TC-H03 |
| BR-RSV-20 | TC-E01, TC-E02 |
| BR-RSV-21 | TC-E10 |
| BR-RSV-22 | TC-C10 |
| BR-RSV-23 | TC-E09 |
| BR-RSV-26 | TC-E06, TC-E07 |
| BR-RSV-27 | TC-E08 |
| BR-RSV-28/29 | TC-F01, TC-F02 |
| BR-RSV-30 | TC-F03 |
| BR-RSV-31 | TC-E11, TC-F09 |
| BR-RSV-32 | TC-F10 |
| BR-RSV-33 | TC-F07 |
| BR-RSV-34 | TC-F05, TC-F06 |
| BR-RSV-35 | TC-F08 |
| BR-RSV-36 | TC-G04 |
| BR-RSV-38 | TC-H01 |
| BR-RSV-41 | TC-D09 |

---

## 4. เคสที่เป็น Blocker (ห้าม merge ถ้าไม่ผ่าน)

| ID | ทำไมถึงเป็น blocker |
|----|---------------------|
| **TC-A05** | เทสเดียวที่จับได้ว่า implement ความจุผิดวิธี — ผิดแล้วลูกค้าจองทะลุคิวจริงบน production |
| **TC-A08** | จับการลืม `SAVEPOINT` ซึ่งทำให้จองไม่ได้ทั้งที่ยังว่าง |
| **TC-B04** | ตัวกั้นที่ซ่อนแค่เมนู = ไม่ได้กั้นจริง |
| **TC-D02 / TC-G01 / TC-G02 / TC-G05** | zero-regression — กระทบร้านที่ใช้งานจริงอยู่แล้วทั้งระบบ |
| **TC-E01 / TC-E02** | ถ้าพลาด = เปิดช่องให้คนอื่นเห็นนัดของคนอื่น |
| **TC-F07 / TC-F08** | ถ้าพลาด = ฟีเจอร์ใหม่ไปแตะ lifecycle และคะแนนที่อยู่บน production |
| **TC-H01** | ถ้าพลาด = ร้านโดนหักเงินโดยไม่ได้สั่ง |
| **TC-H06 / TC-H07** | ถ้าพลาด = constraint ของฟีเจอร์อื่นบน production ถูกลบไปด้วย |

---

## 5. ผลล่าสุด

| รอบ | วันที่ | ผล | หมายเหตุ |
|-----|--------|-----|----------|
| Spike กลไกความจุ (raw SQL) | 2026-07-30 | ✅ **ผ่าน 9/9** | `spike-capacity.cjs` — พิสูจน์ EXCLUDE + ที่นั่ง + timezone + zero-regression บน DB จริง (rollback สะอาด) |
| Dry-run migration | 2026-07-31 | ✅ **ผ่าน 27/27 statement** | รัน `migration.sql` ทั้งไฟล์ใน transaction ที่ ROLLBACK — ไม่เหลือตาราง/คอลัมน์บน DB |
| Apply migration | 2026-07-31 | ✅ สำเร็จ | `migrate deploy` บน DB ที่แชร์กับ prod (user อนุมัติ) — ยืนยันหลังรัน: `Order_service_seat_no_overlap`, `Order_service_fields_all_or_none`, `ServiceResource_capacity_positive` มีจริง **และ** constraint ของฟีเจอร์อื่นยังอยู่ครบ (`Order_room_no_overlap`, `Shop_userId_personal_key`, `OrderShipment_active_order_key`) → **TC-H06/TC-H07 ผ่าน** |
| ยืนยันกลไกผ่าน Prisma model call | 2026-07-31 | ✅ **ผ่าน 3/3** | เส้นทางเดียวกับ production (ไม่ใช่ `$executeRaw`) ใน transaction ที่ ROLLBACK: ความจุ 2 จอง 3 ราย → ได้ `[1, 2, เต็ม]`; ช่วงต่อกันพอดี 11:00-12:00 ได้คิวคืน; ออเดอร์สินค้าปกติ 2 ใบสร้างได้ (zero-regression) |
| tsc | 2026-07-31 | ✅ **0 error** | baseline ของ worktree นี้เปลี่ยนจาก 78 เป็น **0** หลังรัน `npm run build` ครั้งแรก (build สร้าง type declaration ของ static asset ให้ error `Cannot find module '@/assets/images/*.svg'` หายหมด) — ต่อจากนี้เจอ error แม้ตัวเดียว = ของใหม่จริง |
| Unit (Vitest) — มัดจำ | 2026-07-31 | ✅ **ผ่าน 14/14** | `src/services/__tests__/appointment-deposit.test.ts` ครอบ BR-RSV-43/44/47/48/51 — ฟังก์ชันบริสุทธิ์ไม่แตะ DB |
| **TC-A05 ยิงพร้อมกันจริง (รอบแรก)** | 2026-07-31 | ❌ **ไม่ผ่าน — เจอบั๊กจริง** | สำเร็จ **0 จาก 12** (คาดหวัง 8) ด้วย deadlock (40P01) + transaction timeout 5 วินาที ดู §5.1 |
| **TC-A05 ยิงพร้อมกันจริง (หลังแก้)** | 2026-07-31 | ✅ **ผ่าน 6/6 เช็ค · เสถียร 4 รอบติด** | ความจุ 8 ยิง 12 → สำเร็จ 8, 409 = 4, error อื่น 0, ที่นั่ง 1–8 ไม่ซ้ำ ยืนยันจากแถวจริงใน DB. ทดสอบภาระหนักเพิ่ม: 40 ยิงบนคิว 8 → 8/32 ✅ · 60 ยิงบนคิว 20 → 20/40 ✅ (ไม่มี timeout กลับมา) |
| E2E (Playwright) | — | ยังไม่รัน | รอ browser QA |
| Visual QA | — | ยังไม่รัน | — |

### 5.1 บั๊กที่ TC-A05 จับได้ และการแก้ (2026-07-31)

**อาการ:** ยิง 12 request พร้อมกันบนความจุ 8 → **สำเร็จ 0 ราย** ไม่ใช่จองทะลุความจุ แต่จองไม่ได้เลยสักคน ได้ error 2 แบบปนกัน: `40P01 deadlock detected` และ Prisma interactive transaction timeout (5,000 ms)

**สาเหตุ:** EXCLUDE constraint บน GiST สร้าง **predicate lock** — เมื่อ transaction อื่นเขียนที่นั่งเดียวกันไว้แต่ **ยังไม่ commit** ตัวที่มาทีหลังจะ **ถูกบล็อกให้รอ ไม่ใช่ได้ error ทันที** จึงติดค้างอยู่ที่นั่งนั้น ไปลองที่นั่งถัดไปไม่ได้ พอหลายตัวรุมกันก็รอกันเป็นลูกโซ่จนเกิด deadlock และหมดเวลา transaction ยกชุด

กลไก "ลองที่นั่งแล้วชน = ข้ามไปที่นั่งถัดไป" ตั้งอยู่บนสมมติฐานว่า *ชนแล้วได้ error ทันที* ซึ่งจริงเฉพาะกับแถวที่ **commit ไปแล้ว**

**ทำไม spike รุ่นแรกไม่เจอ:** ทั้ง `spike-capacity.cjs` และรอบ "ยืนยันกลไกผ่าน Prisma model call" ยิง **ทีละตัวตามลำดับ** จึงชนกับแถวที่ commit แล้วเสมอ — ได้ error ทันทีจริงตามที่คาด ไม่เคยเจอเคสชนกับ transaction ที่ยังไม่ commit (ต่อยอดบทเรียน `feedback_spike_must_match_production_path`: spike ต้องตรงกับ *สภาพการใช้งานจริง* ไม่ใช่แค่ *เส้นทางโค้ดจริง*)

**การแก้:** เพิ่ม `pg_advisory_xact_lock(24, hashtext(resourceId))` ที่ต้น `allocateSeat()` — เรียงคิวการจัดสรร **ต่อทรัพยากรหนึ่งหน่วย** ให้ทีละราย ลูปวนที่นั่งจึงเห็นเฉพาะแถวที่ commit แล้ว ซึ่งเป็นเงื่อนไขที่กลไกต้องการ

- ทรัพยากรคนละตัวจองพร้อมกันได้ตามปกติ (lock แยกตาม resourceId)
- เป็น xact lock → ปลดเองตอน transaction จบ ไม่มีทางลืมปลด
- มี lock เดียว → ไม่มีทางเกิด deadlock
- **EXCLUDE constraint ยังเป็นตัวตัดสินสุดท้ายเหมือนเดิม** advisory lock แค่ทำให้กลไกทำงานได้ ไม่ได้แทนที่

**หมายเหตุความปลอดภัย:** ตอนที่บั๊กยังอยู่ ระบบ fail แบบปิด (fail-closed) — ไม่เคยจองเกินความจุ (0 ≤ 8) ความถูกต้องไม่เคยเสีย เสียแต่ความพร้อมใช้งาน

**สคริปต์:** `scripts/tc-a05-concurrent-capacity.ts` (ปรับความจุ/จำนวนยิงผ่าน env `TC_A05_CAPACITY` / `TC_A05_ATTEMPTS`) รันบน **Postgres บนเครื่องเท่านั้น** — มี allowlist กันชี้ไป DB ที่แชร์กับ prod ตาม Hard Rule 13

---

## 6. สรุป

- เทสที่สำคัญที่สุดคือ **TC-A05 (ยิงพร้อมกัน 12 ครั้งบนความจุ 8)** — เป็นด่านเดียวที่จับการ implement ความจุผิดวิธีได้
- กลุ่ม G (zero-regression) ต้องผ่านครบก่อน merge เพราะฟีเจอร์นี้เพิ่มฟิลด์ลงตาราง `Order` ที่ทั้งระบบใช้ร่วมกัน
- กลุ่ม H ตรวจสิ่งที่มองไม่เห็นจาก UI: ต้นทุน SMS แฝง, กลไกภายในที่หลุดออก API, PII ใน flight payload, และ constraint ของฟีเจอร์อื่นที่อาจถูกลบพร้อม migration
