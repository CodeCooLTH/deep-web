# SafePay — Trust & Reputation Platform

> **Commercial brand:** "Deep" (UI copy, prod domain `deepthailand.app`). Internal codename "SafePay" is kept in repo, identifiers, and DB. See `~/.claude/projects/.../memory/feedback_brand_naming.md`.

---

## 🛑 HARD RULES — enforced by project skills

กฎเหล่านี้ enforce ผ่าน project-local skills ใน `.claude/skills/` (trigger อัตโนมัติ) — เมื่อ skill activate ให้ทำตาม skill ไม่ใช่จำจากที่นี่. Subagents ใน `.claude/agents/` ฝัง contract เดียวกัน.

| # | Rule | Skill (auto-trigger) | Deep reference |
|---|---|---|---|
| 1 | No UI from scratch — ทุกหน้า/component ต้อง copy จาก theme file ที่ระบุ แล้วปรับ content. **ทุกครั้งก่อนทำ Frontend ต้องอ่าน guideline ก่อน** | `ui-theme-sourcing` | `docs/system/ui-guideline/README.md` (+ `customer/`,`seller/`,`admin/`) |
| 2 | No `component={Link}` ใน server component — ใช้ LinkButton/LinkChip wrapper | `rsc-mui-nav` | `docs/conventions/rsc-mui-navigation.md` |
| 3 | Commit ที่แตะ UI ต้องมี `Base:` line ชี้ `theme/...` ที่ copy มา | `ui-theme-sourcing` | `docs/system/ui-guideline/README.md` |
| 4 | Phase ≥3 tasks = agent team (Planner→Developer→Reviewer→QA→Controller, 5 gates, 3-level QA) + retro ปลาย phase | `agent-team-phase`, `phase-retro` | `docs/conventions/agent-team-workflow.md` |
| 5 | Font Anuphan เท่านั้น — ทุก surface ทุก subdomain ทุก skin (ยกเว้น monospace code block + icon font). ห้าม hardcode font อื่น (Inter/Public Sans/Poppins/Nunito/...) | `ui-theme-sourcing` | `docs/conventions/anuphan-font.md` |
| 6 | Reference ที่ user ส่ง: แยก 2 ชั้น — **asset/content ใช้ตาม ref (ห้าม redesign/ทิ้งเอง)**, **layout/integration ต้องตาม theme ปัจจุบัน** (ห้ามแปะ layout ดิบจน "ดูเป็นตัวอย่าง"). ไม่ชัดส่วนไหนเป็นไหน → ถาม user ก่อน build. **ref เป็นแอปอื่น (Shopee ฯลฯ): เอา IA/layout ตาม ref แต่ skin/สี/component = theme ปัจจุบัน** (Paces น้ำเงิน ไม่ใช่ส้ม Shopee) | `ui-theme-sourcing` (reviewer gate) | `docs/conventions/reference-vs-theme-source.md` |
| 7 | **หน้า `(paces)/**` (seller/admin) ต้องประกอบจาก Paces primitive — ห้าม arbitrary Tailwind value.** ใช้ `.card`/`.card-header`/`btn`/`badge`/`text-default-*`/`bg-primary bg-{semantic}/15`/`size-*`/`rounded-lg`/`after:border-dashed`. ห้าม `text-[NNpx]`/`bg-[rgba()]`/`shadow-[]`/`rounded-[Npx]`/hardcode hex — เว้นจำเป็นจริง (raised-FAB/safe-area ที่ Paces ไม่มี token) **เขียน comment กำกับ**. "Base: comment แต่แต่งเองด้วย arbitrary" = ละเมิด (v6/v7 บทเรียน). ม่วง #7367F0 = buyer/Vuexy เท่านั้น | `ui-theme-sourcing` (reviewer grep gate) | `docs/retro/2026-06-10-seller-cc-v8-paces-resource-retrospective.md` |
| 8 | **ทุกงาน frontend/UI ต้องผ่าน `safepay-ux` ก่อนเสมอ (mandatory gate)** — และ `safepay-ux` **ต้องอ่าน `DESIGN.md` + `PRODUCT.md` + `.impeccable/design.json` เป็นขั้นแรกทุกครั้ง** แล้วออกหัวข้อ `### Impeccable compliance` ในทุก Design Spec (ux Hard Rule 9; theme ชนะเรื่อง markup — Impeccable ชนะเรื่องสี/น้ำเสียง/ลำดับชั้น). 🛑 **หลัง build UI เสร็จ Controller ต้องรัน Impeccable CLI เป็น gate ก่อน mark complete** — `/impeccable critique` (design review + คะแนน) และ `/impeccable clarify` (ตรวจ copy/error message/label) อย่างน้อย; `/impeccable audit` เมื่อแตะ a11y/perf. ux รันเองไม่ได้ (tools = Read/Glob/Grep). — สร้าง/แก้ page/component/layout/style ใด ๆ ต้อง invoke `safepay-ux` ออก Design Spec **ก่อน** ลงมือ. ux ต้องอ่าน **Impeccable playbook ที่ตรงกับงาน** ด้วย (`~/.claude/skills/impeccable/reference/` — `shape.md` ทุกครั้ง, `operate.md` สำหรับ `(paces)/**`, `craft-floor.md` ก่อนสรุป spec) และระบุ `Mode:` ใน `### Impeccable compliance` — ux agent Read ไฟล์เหล่านี้ได้เอง. ux อิง design docs **ตาม role**: seller/admin (`(paces)/**`) → **Paces docs `theme/paces/Docs/index.html`** + `docs/system/ui-guideline/paces-component-reference.md`; buyer/landing/public (`(marketing)/**`) → **Vuexy docs `theme/vuexy/documentation.html`** + `theme/vuexy/`. ห้ามเขียน frontend ตรง ๆ ข้าม ux (บทเรียน 2026-06-15: หลงทางแก้ไป-มา เพราะข้าม ux + ไม่อิง docs → ปุ่ม/dropdown/table/spacing เพี้ยน) | `ui-theme-sourcing` | seller/admin: `theme/paces/Docs/index.html` + `docs/system/ui-guideline/paces-component-reference.md`; buyer: `theme/vuexy/documentation.html` |
| 9 | **Toast/alert ใน `(paces)/**` (seller/admin) ใช้ `pacesToast` เท่านั้น — ห้าม `react-toastify`/`toast()`/`alert()`/swal/Preline toast ดิบ.** ทุก notification เรียกผ่าน `import { pacesToast } from '@/lib/paces-toast'`; render โดย `PacesToastContainer` (mount จุดเดียวใน `AppProvidersWrapper`) ด้วย markup Paces. **placement แยกตามแหล่งที่มา:** action/ปุ่ม → `pacesToast.success/error/warning/info` (**top-right**); chat → `pacesToast.chat.*` (**bottom-right**). `react-toastify` ยังใช้ได้เฉพาะ buyer `(marketing)/**` (Vuexy). **Reviewer grep gate:** `rg "from ['\"]react-toastify" "src/app/(paces)/"` ต้องคืน 0 ก่อน merge — **ต้อง match `from 'react-toastify'` ไม่ใช่แค่คำว่า `react-toastify` เปล่า ๆ** เพราะไฟล์ที่ทำถูกกฎมักเขียนคอมเมนต์อ้างชื่อกฎ ("ห้าม react-toastify") ไว้บนหัวไฟล์ → gate แบบเดิมจะแดงตลอดกาลและถูกบันทึกเป็น "หนี้" ทั้งที่ไม่มีการละเมิดเลย (เข้าใจผิดจริงมาแล้ว 2026-08-02 → 08-03) | `ui-theme-sourcing` (reviewer grep gate) | `docs/conventions/paces-toast.md` |
| 10 | **Chart/graph ใน `(paces)/**` (seller/admin) ต้อง copy structure มาจาก `theme/paces/Admin/TS/src/app/(admin)/widgets/charts/components/` เท่านั้น** แล้วปรับ content/data — ห้าม build chart options from scratch, ห้าม source จาก Vuexy theme dir, ห้าม import `react-apexcharts`/`echarts`/`chart.js`/`recharts` โดยตรง (ต้องผ่าน `@/components/wrappers/ApexChart`). สี: `getColor('chart-*')` token เท่านั้น — ห้าม hardcode hex. Commit ต้องมี `Base:` ชี้ theme charts file (Hard Rule 3). **Reviewer grep gate:** `rg "from 'react-apexcharts'\|from 'echarts'\|from 'chart\.js'\|from 'recharts'" "src/app/(paces)/"` ต้องคืน 0 ก่อน merge | `ui-theme-sourcing` (reviewer grep gate) | `docs/conventions/paces-charts-source.md` |
| 11 | 🛑 **Documentation-First — feature ใหม่ "ต้องทำเอกสารก่อนเขียนโค้ด" เสมอ.** ทุก feature มีโฟลเดอร์ `docs/20 - Features/<NNNNN> - <FeatureName>/` ประกอบจาก template ใน `docs/99 - Rules/Feature-Templates/` (PRD→BRD→SRS→SDS→DATABASE→API→Tests). **ห้าม implement ก่อนมี PRD+BRD (อย่างน้อย) ผ่าน user review.** 🛑 **แม้ user สั่งเร่ง/อนุมัติทำยาวต่อเนื่อง — doc-first ไม่ใช่ gate ที่ downgrade ได้ด้วยความเร่งรีบ**: ความเร็วมาจากการข้าม micro-approval ระหว่าง phase (`feedback_brainstorm_pace`) ไม่ใช่ข้าม Requirement (PRD+BRD) เอง. back-fill retroactive = หนี้ ไม่ใช่ default (บทเรียน 00012 ตีความ "เร่ง" ผิดเป็น "ข้าม doc-first"). Ownership → subagent: PRD/BRD=`safepay-product`, SRS/SDS/API=`safepay-planner`, DATABASE=`safepay-database`, Tests=`safepay-qa` (Controller Write+commit). diagram ทุกชนิด = **Mermaid เท่านั้น** (ห้าม ASCII/รูปภาพ). 🛑 **นับความครบด้วยการเทียบ "ชื่อไฟล์" กับ template ไม่ใช่ด้วย "จำนวนไฟล์"** — `diff <(ls "docs/99 - Rules/Feature-Templates/") <(ls "docs/20 - Features/<NNNNN> - <Name>/")` เพราะการเพิ่มไฟล์นอก template (เช่น `UX-Design-Spec.md` ซึ่งควรมีจริงเมื่องานแตะ UI หนัก) กลบการหายไปของไฟล์ใน template ได้พอดีตัว แล้ว "7/7" จะถูกอ่านว่าครบทั้งที่เป็นคนละ 7 ไฟล์ (บทเรียน 00028: `TestCase.md` หายไปโดยไม่มีใครเห็นเพราะจำนวนครบ). 🛑 **"ครบ 7 ไฟล์" ≠ "เอกสารเสร็จ" — งานที่แตะ data model/API/enum/validation ต้อง sync `docs/SRS.md` (เอกสารระบบ) ด้วยเสมอ ไม่ใช่แค่ feature docs** เพราะ CLAUDE.md ประกาศเองว่า SRS คือที่ที่งานกลุ่มนี้ "ต้องอ่านก่อน" — SRS ที่ค้างคือกับดักที่วางไว้รอคนถัดไป (บทเรียน 00033: feature docs ครบตั้งแต่ task แรก แต่ `CreateOrderSchema` มีคีย์ใหม่ · `OrderEvent.type` 9→13 ค่า · `Order.createdAt` เปลี่ยนความหมาย · `PATCH /api/orders/[token]` และโมเดล `OrderEvent` ไม่เคยอยู่ใน SRS เลย — ค้างจนถูกถามว่า "เหลืออะไรอีกไหม") | — (Controller enforce) | `docs/99 - Rules/Feature-Docs-Ownership.md` |
| 12 | **ห้าม emoji ใน UI ทุกจุด — ใช้ icon จริงเท่านั้น** (ทุก surface/subdomain/theme). ห้ามฝัง emoji ใน string/JSX/badge/chip/ปุ่ม/label/empty-state/toast/Swal — รวม emoji ที่ "ดูเหมือน icon" (👑🔥⭐💬📦✅⚡🎉🏆📷). จุดที่ควรมี icon แต่ mockup/spec ไม่ระบุตัว → **ถาม user ก่อน (ห้ามเดา)**. carve-out: code comment marker + typographic dingbat สีเดียว (★☆✓✗♡▾) + badge.icon จาก data. **Reviewer grep gate:** `grep -rnP '[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}\x{2B00}-\x{2BFF}]'` บนไฟล์ UI ที่แตะ = 0. theme copy พา emoji ติดมา → grep เก็บทันทีหลัง copy | `ui-theme-sourcing` (reviewer grep gate) | `docs/conventions/no-emoji-use-icons.md` |
| 13 | 🛑 **ห้ามคำสั่งลบข้อมูลแบบไม่ scope ในไฟล์เทสเด็ดขาด — ไม่มีข้อยกเว้น.** `deleteMany()` ที่ไม่มี `where`, `TRUNCATE`, `DELETE FROM` ที่ไม่มี `WHERE`, `DROP TABLE/SCHEMA`, `migrate reset`, `db push --force-reset`, `prisma db pull` ห้ามปรากฏใน `tests/**`, `e2e/**`, `**/__tests__/**`, `*.test.ts`, `*.spec.ts` **ทั้งสิ้น**. เหตุผล: **dev DB = prod DB ตัวเดียวกัน** (Supabase แชร์) คำสั่งพวกนี้คือการลบข้อมูลลูกค้าจริงทั้งฐาน ที่รอแค่ให้ connection ชี้ถูกที่. ต้องล้างข้อมูลระหว่างเทส → **scope ด้วย `where` ที่ผูกกับ id ที่เทสสร้างเอง** (`deleteTestData({ userIds, shopIds })` ใน `tests/setup.ts`) หรือเขียนเป็น unit test ที่ไม่แตะ DB เลย (วางใต้ `src/**/__tests__/`). `tests/setup.ts` ต้อง fail-closed ด้วย **allowlist** (ผ่านเฉพาะ localhost) ไม่ใช่ denylist. `cleanDatabase()` เดิมถูกถอดออกแล้ว — เรียกแล้ว throw ทันที | `test-db-guard` hook (block ตอน Write/Edit) | `docs/conventions/test-db-safety.md` |
| 14 | 🛑 **คำสั่งที่ล้าง/สร้าง schema ใหม่ได้ ต้องพิสูจน์ได้จากตัวคำสั่งเองว่าชี้ Postgres บนเครื่องตัวเอง — พิสูจน์ไม่ได้ = ถือว่าเป็น prod.** ครอบ `--shadow-database-url`/`SHADOW_DATABASE_URL=`, `migrate reset`, `db push --force-reset|--accept-data-loss`, `migrate dev`, `db pull`, `supabase db reset`, `psql` ที่มี `DROP`/`TRUNCATE`/`DELETE FROM` ไม่มี `WHERE`, และ `playwright test`/`npm run e2e` (config โหลด `.env.local` = prod และ helper ลบ User/Shop จริง). ผ่านได้ทางเดียวคือ **ปักหมุด URL localhost ในคำสั่งตรง ๆ** — ห้าม `$(...)`/ตัวแปรจาก `.env.local`. **เหตุผล: 2026-07-31 22:37 น. ฐาน prod ถูกล้างทั้ง 64 ตารางด้วย `prisma migrate diff --shadow-database-url "$(grep DIRECT_URL .env.local ...)"`** — shadow DB คือฐานทิ้งขว้างที่ Prisma drop schema ทิ้งเสมอ. คำสั่งไม่มีคำว่า delete/drop/reset เลย Hard Rule 13 จึงมองไม่เห็น (มันตรวจแค่ไฟล์เทสตอนเขียน). ยังทำได้ปกติ: `migrate deploy`/`status`/`generate`/`validate`, SELECT, `vitest`, และการ grep ที่ *พูดถึง* คำสั่งเหล่านี้ | `prod-db-guard` hook (**PreToolUse บน Bash** — block ตอนจะรัน; ติดตั้งทั้งในรีโปและระดับ user) | `docs/conventions/prod-db-safety.md` |

| 15 | 🛑 **การ deploy รัน `prisma migrate deploy` ให้อยู่แล้ว — push ขึ้น `main` = migrate ขึ้น prod ในตัว. ก่อนรัน migrate ใด ๆ ต้องแจ้ง user เรื่องนี้เสมอ ห้ามเงียบ.** `vercel.json` → `"buildCommand": "prisma migrate deploy && prisma generate && next build"` แปลว่า **ไม่มีใครต้องสั่ง migrate ชี้ prod ด้วยมือ ไม่ว่ากรณีใด** — คำสั่ง migrate ที่ชี้ prod จากเครื่อง dev คือความเสี่ยงล้วน ๆ ที่ไม่ได้แลกอะไรกลับมาเลย (ฐาน prod เคยถูกล้างทั้งฐานมาแล้วจากคำสั่ง Prisma ที่ชี้ผิดที่ — ดู Hard Rule 14). เมื่อ user สั่ง/ถามเรื่อง migrate ต้องตอบให้ครบ 3 ข้อ: (1) **prod ไม่ต้องสั่งเอง** push แล้วจบ (2) **ฐาน local ยังต้อง apply เอง** — Vercel เห็นเฉพาะฐานที่ deployment ชี้ ไม่เห็น Docker บนเครื่อง user (`prisma migrate deploy` ปักหมุด localhost ตาม Hard Rule 14) (3) **migrate ล้ม = build ล้ม = deploy ไม่ขึ้น** ของเก่ายังเสิร์ฟอยู่ ไม่มีสถานะครึ่ง ๆ กลาง ๆ ต้องแก้ไฟล์ migration แล้ว push ใหม่ ไม่ใช่กด retry deploy. **ข้อควรระวัง:** `migrate deploy` รันเฉพาะไฟล์ใน `prisma/migrations/` — SQL ที่เคยรันมือบน Supabase console โดยไม่มีไฟล์กำกับจะไม่ถูกสร้างซ้ำในฐานใหม่ (นี่คือเหตุผลที่ยังห้าม `prisma db pull`: introspect ไม่เห็น unmanaged SQL แล้วจะพยายามลบทิ้ง) | — (Controller enforce ตอนตอบ/ก่อนรัน) | `docs/conventions/migrate-on-deploy.md` |
| 16 | 🛑 **ศัพท์ธุรกิจ (กำไร/ยอดขาย/ต้นทุน/ส่วนลด/ค่าธรรมเนียม/ยอดคงเหลือ) ต้องมีนิยามเดียวทั้งระบบ — ก่อนนิยามสูตรใหม่ ต้อง grep หานิยามเดิม และ *เปิดอ่านไฟล์ที่กำลังจะ import* ให้จบ.** นิยามที่ชนกันมักอยู่ในไฟล์เดียวกับ helper ที่เรากำลังหยิบ (เคสจริง 2026-08-08: การ์ดกำไรรายออเดอร์เรียกตัวเองว่า "กำไร" โดยคำนวณ `totalAmount − COGS` ซึ่ง **รวม VAT และไม่หักค่าใช้จ่าย** ขณะที่ `NET_PROFIT_FORMULA` นิยามไว้แล้วใน `src/lib/format-money.ts` = **ไฟล์เดียวกับที่การ์ดนั้น import `formatBaht` มา** — ผู้ขายที่บวกกำไรรายใบทั้งเดือนแล้วเทียบกับ `/sales` จะได้เลขไม่ตรงเสมอ). **ทุก gate ของโปรเจกต์ตรวจ "รูปแบบ" ไม่มีอันไหนตรวจ "ความหมาย"** — `tsc`/build/detector/`theme-guard`/grep ผ่านหมดเพราะทั้งสองนิยาม "ถูก" ในตัวเอง จับได้ด้วย `/impeccable critique` เท่านั้น. ถ้าของใหม่ต่างจริง → ตั้งชื่อต่างชัด (`GROSS_` vs `NET_`) + **วางติดกันในไฟล์เดียวกัน** + เขียนว่าผลรวมของสองอันจะไม่เท่ากันเพราะอะไร + **บอกผู้ใช้บนหน้าจอด้วย ไม่ใช่แค่คอมเมนต์** 🛑 **ทิศกลับก็จริงเช่นกัน: ข้อความในเอกสารที่อ้างพฤติกรรมของโค้ด ต้องยืนยันกับโค้ดก่อนใช้ตัดสินใจ โดยเฉพาะเมื่อถูกใช้เป็นเหตุผลที่จะ "ไม่ทำ" อะไร** — คำว่า "เท่านั้น/ห้าม/ไม่สามารถ" ในเอกสารคือการอ้างข้อเท็จจริง ไม่ใช่การอธิบายเจตนา (เคสจริง 2026-08-08: agent ล็อกมติว่าห้ามให้กรอกต้นทุนตอนแก้ไขออเดอร์ โดยอ้าง FR-EXP-02 ที่เขียนว่า "snapshot เกิดตอนสร้างเท่านั้น" แต่ `updateOrder` **re-snapshot อยู่แล้ว** — คอมเมนต์ในโค้ดเขียนเองว่า "เหมือน createOrder"). **ข้อจำกัดปลอมทำให้เรา *ไม่* เขียนโค้ด จึงไม่มีอะไรให้เทสจับ ไม่มีอะไรพัง ไม่มีใครรายงาน** — อยู่ได้ตลอดไปจนกว่าจะมีคนบังเอิญเปิดโค้ดดู. prompt ที่ส่ง agent ไปเขียนเอกสารต้องสั่งให้ยืนยันข้อจำกัดกับโค้ดก่อนล็อกเป็นมติเสมอ | — (Controller/planner enforce ตอนเขียน SRS) | `docs/conventions/domain-term-single-definition.md` |
| 17 | 🛑 **rebase ที่ผ่านสะอาดไม่ใช่หลักฐานว่าปลอดภัย — ถ้ามีไฟล์ที่ทั้งสองฝั่งแตะ ต้องอ่าน diff ของอีกฝั่งในไฟล์นั้นก่อนเสมอ** (`comm -12 <(git diff --name-only origin/main...HEAD\|sort) <(git diff --name-only HEAD...origin/main\|sort)` → ถ้าไม่ว่างให้ `git log/diff HEAD...origin/main -- <ไฟล์>`). git ตรวจ **บรรทัดที่ชนกัน** ไม่ได้ตรวจ **แพตเทิร์นที่ควรเหมือนกัน** — โค้ดที่เขียนคู่ขนานโดยก็อปต้นแบบจากไฟล์นั้น จะกลายเป็นเวอร์ชันก่อนแก้ทันทีที่อีกฝั่งแก้ต้นแบบ **โดยไม่มี conflict** (เคสจริง `b51afb48`: ชิปใหม่ก็อป `focus:outline-none` มาตอนที่ audit รอบ 2 กำลังแก้บรรทัดนั้นพอดี → ปุ่ม 2 ตัวในแถวเดียวกันห่างกัน 20 บรรทัด ตัวหนึ่งโฟกัสเห็นอีกตัวไม่เห็น). 🛑 **verify (tsc/test/build) ต้องอยู่ "หลัง" rebase เสมอ และเช็ค fast-forward ซ้ำก่อน push แยกคำสั่ง** — ขั้นตอนนี้จับได้ 3 อย่างคนละชนิดในรอบเดียว: Prisma client เก่าในเวิร์กทรี (build ล้ม แก้ด้วย `prisma generate` ซึ่งไม่แตะฐานข้อมูล) · มีคน push แทรกตอนกำลัง build · เคสข้างต้น | — (Controller enforce ก่อน push) | `docs/conventions/rebase-clean-is-not-safe.md` |

Subagents: `safepay-product` `safepay-planner` `safepay-ux` `safepay-database` `safepay-developer` `safepay-reviewer` `safepay-security` `safepay-qa` `safepay-docs` (ทุกตัว Sonnet; Controller = main session). **`safepay-ux` = mandatory gate ของงาน frontend ทุกชิ้น (Hard Rule 8)** — ออกแบบ UX/UI (read-only Design Spec) อิง Paces docs `theme/paces/Docs/index.html` + `docs/system/ui-guideline/paces-component-reference.md` invoke **ก่อน developer/ก่อนแก้โค้ด frontend เสมอ** (ไม่ใช่แค่ task ไม่ trivial — ทุก task). Feature เต็มรูป (7-phase) ดู skill `agent-team-feature`.

---

## Project Overview

SafePay เป็นระบบสร้างความน่าเชื่อถือสำหรับการซื้อขายออนไลน์ ผ่านระบบ Verify ตัวตน, Trust Score, Badge และ Order History เพื่อแก้ปัญหามิจฉาชีพ

## Key Documents

- **PRD (product-level):** `docs/PRD.md` — vision, personas, user stories, feature overview (FR feature-level), scope, metrics, business model, roadmap. "อะไร/ทำไม"
- **SRS (software spec):** `docs/SRS.md` — FR ฉบับเต็ม (สูตร/acceptance/edge), state machine, routing, NFR, **data model (Prisma schema), API reference, enums/constants, authorization matrix, validation rules**. "สเปกให้ dev สร้าง". 🛑 งานที่แตะ data model/API/enum/validation/auth → อ่าน SRS ก่อน
- **Business Rules:** `docs/10 - Business Rules/` — กฎธุรกิจที่เป็น SSOT. **🛑 เมื่อใดก็ตามที่พูดถึง/ทำงานกับ "Tier" (trust tier, tier name/cover/color/mapping) ต้องอ่าน `docs/10 - Business Rules/Tier Lists.md` ก่อนเสมอ แล้วยึดตามนั้น — ห้ามตั้ง mapping/ชื่อ tier เองที่อื่น**
- **UI Guideline (must-read before ANY Frontend work):** `docs/system/ui-guideline/README.md` — entry hub (universal theme-copy rule + checklist + workflow + commit rule). Role docs: `customer/`, `seller/`, `admin/` page-sourcing.md
- **Buyer App API:** `docs/buyer-app-api.md` — REST `/api/app/*` สำหรับแอปมือถือผู้ซื้อ (Deep-App): auth Bearer token, โดเมนประมูล (Auction/Bid/WatchList/Notification), Phase 2 ชนะ→Order, dev setup. 🛑 งานที่แตะ `/api/app/*` อ่านอันนี้ก่อน
- **Conventions:**
  - `docs/conventions/rsc-mui-navigation.md` — RSC + MUI + next/link pattern
  - `docs/conventions/date-format.md` — 🛑 วันที่/เวลาทั้งระบบใช้ `formatDate`/`formatDateTime` จาก `src/lib/format-date.ts` เท่านั้น (พ.ศ. `2569-06-07 10:06:13`, tz ไทย) — ห้าม `toLocaleDateString`/`Intl.DateTimeFormat` เอง
  - `docs/conventions/no-emoji-use-icons.md` — 🛑 ห้าม emoji ใน UI ทุกจุด (ทุก surface/subdomain/theme) ใช้ icon จริงเท่านั้น (`@iconify/react`/wrapper); จุดที่ควรมี icon แต่ mockup/spec ไม่ได้ระบุตัว → **ต้องถาม user ก่อน implement เสมอ** (ห้ามเดาเอง)
  - `docs/conventions/impeccable-design.md` — 🛑 งาน UI **ทุกชิ้น** ยึด **Impeccable design system** เป็นหลัก (`.impeccable/design.json` + `DESIGN.md`): north star "The Trusted Counter", One Voice (ม่วง ≤10%), Verified-Means-Green (#28C76F), Ink Plum ไม่ใช่ดำสนิท, tokens เงา/motion, anti-slop. **อ่าน design.json ก่อนทำ UI เสมอ** — ควบคู่ theme-copy (Hard Rule 1/8)
  - `docs/conventions/enum-value-removal.md` — 🛑 ลบค่าออกจาก enum/union ที่ผู้ใช้เห็น ต้อง grep **ทั้ง repo** (`src/ e2e/ scripts/ prisma/ docs/`) ไม่ใช่แค่ `src/` เพราะ e2e/scripts **seed row จริง** และคอลัมน์ `String` ใน Prisma ไม่มี type ให้ TS จับ. grep จับ **object key ไม่ได้** → ต้องขยาย type union ให้ `tsc` บังคับ key ครบเป็นด่านสอง. ตรรกะ binary (`v === 'X' ? A : B`) ไม่พังเสียงดังเมื่อค่าที่ 3 มา — เขียนเป็น allow-list + fail-closed. ตั้งชื่อไทยของค่าใหม่ต้อง grep กันชนกับ label ที่มีอยู่ก่อน
  - `docs/conventions/root-served-assets-and-proxy.md` — 🛑 ไฟล์ที่เสิร์ฟจาก **root** (ของใน `public/` + route ที่ Next สร้างเอง เช่น `manifest.ts`→`/manifest.webmanifest`) ต้องมีนามสกุลอยู่ใน allow-list ของ `src/proxy.ts` **ในคอมมิตเดียวกัน** ไม่งั้นโดน rewrite เป็น `/seller/...` = **404 เฉพาะบน subdomain** (main domain ปกติ จึงหลุด review ทุกครั้ง). พลาดมาแล้ว 2 ครั้ง — `.m4a` เสียงแจ้งเตือนไม่ดัง (2026-07-24) และ `.webmanifest` ทำ PWA "เพิ่มลงหน้าจอโฮม" บน seller/admin ใช้ไม่ได้มาตลอด (2026-08-05) **ทั้งคู่ user เป็นคนเจอ**; คอมเมนต์เล่าบั๊กครั้งก่อนอยู่เหนือ regex บรรทัดเดียวกันแต่กันซ้ำไม่ได้ เพราะคนเพิ่มไฟล์ไม่ได้เปิด `proxy.ts`
  - `docs/conventions/webhook-subscription-two-layers.md` — 🛑 การ subscribe webhook ของ Meta มี **2 ชั้น** (ระดับแอปใน App Dashboard + ระดับเพจผ่าน `subscribed_apps`) **ขาดชั้นไหนก็เงียบเหมือนกัน** และ "ไม่มี event เข้ามา" หน้าตาเหมือน "ไม่มีอะไรเกิดขึ้น" ทุกประการ (ไม่มี error/log, tsc+build เขียว, โค้ดฝั่งรับถูกทุกบรรทัด — มันแค่ไม่เคยถูกเรียก). ชั้นระดับแอป **ไม่มีในรีโปเลย** ต้องอ่านจากฝั่ง Meta ด้วย DevTools MCP — ตรวจแต่โค้ดเราจะเจอแค่ครึ่งเดียวเสมอ. เคสจริงที่พบพร้อมกัน 2026-08-08 และ **ขาดคนละชั้นกัน**: `message_deliveries` (ขาดระดับแอป → สถานะ "ได้รับแล้ว" ไม่เคยทำงานเลยตั้งแต่ 08-05) · `message_edits` (ขาดระดับเพจ → `ingestMessageEdit` ไม่เคยถูกเรียกตั้งแต่ 08-03) · `messaging_handovers` (ขาดทั้งคู่ → เธรดที่ Meta AI ถือสิทธิ์คุมไม่เข้ากล่องจนกว่าคนจะ take over). 🛑 **แอปแชทไม่ใช่แอปเดียวกับแอป login** (`FACEBOOK_ID`=Deep Thailand ส่วน webhook=Deep Chat & LIVE) เช็คผิดแอปแล้วเห็น `fields: []` จะสรุปผิดทั้งกระดาน · `vercel env pull` redact `CHANNEL_TOKEN_KEY` เป็น `[SENSITIVE]` → **re-sync ต้องยิงจากในแอปที่ prod เท่านั้น** สคริปต์จากเครื่อง dev ได้ `CHANNEL_TOKEN_KEY_INVALID` ทุกเพจ · ตัวเลขรวมหลอกได้ ต้องเทียบ **รายเธรด** กับ Business Suite
  - `docs/conventions/one-value-many-entry-points.md` — 🛑 ค่าที่ผู้ใช้กรอก (เช่น **เลขพัสดุ**) มีได้หลายทางเข้าและมักเก็บ **คนละตาราง** — ก่อนอ่าน/แสดง ให้ไล่ทางเข้าจากหน้าจอ (นับแท็บ/ปุ่ม submit ในโมดัล) ไม่ใช่จาก schema ที่กำลังแก้อยู่
  - `docs/conventions/user-supplied-image-assets.md` — 🛑 รูปที่ user ส่งมาต้อง **เปิดดูก่อนใช้**: สัดส่วน (ไม่จัตุรัส + `object-cover` = ครอปเนื้อหาทิ้ง) · พื้นหลัง (ขาวบนการ์ดขาว = ขอบหาย ต้องมี `ring`) · ขนาดไฟล์; ห้ามไปหาโลโก้แบรนด์จากอินเทอร์เน็ตเอง; `ls` ทั้งโฟลเดอร์ก่อน filter (คนเลี่ยงอักขระพิเศษในชื่อไฟล์ `J&T` → `jandt-express.png`)
  - `docs/conventions/contrast-fix-keeps-hue.md` — 🛑 แก้คอนทราสต์ปรับได้แค่ **ความเข้ม** ของสีเดิม **ห้ามสลับเฉด** (เหตุการณ์ 2026-08-03: `text-{tone}` → `-ink` 55 จุด ทำให้ดาวปักหมุดกลายเป็นน้ำตาล ปุ่ม danger กลายเป็นเลือดหมู ต้องย้อนทั้งหมด). `-ink` **ไม่ใช่ token ของธีม** (grep `theme/paces` = 0) — ก่อนใช้ token สีให้ grep `theme/` ก่อนเสมอ. ไอคอนที่ "สี = ตัวตน" (ดาว/โลโก้ช่องทาง) ไม่อยู่ใต้กฎคอนทราสต์ข้อความ. HR8 "Impeccable ชนะเรื่องสี" = ลำดับชั้น/น้ำเสียง/ความหมาย **ไม่ใช่ใบอนุญาตเปลี่ยนเฉดสีแบรนด์**
  - `docs/conventions/aria-name-requires-supporting-role.md` — 🛑 **`aria-label` มีผลเฉพาะกับ element ที่ role รองรับ "ชื่อจากผู้เขียน"** — `<p>`(role=paragraph)/`<div>`/`<span>` เปล่า **ไม่รองรับ** screen reader ที่ทำตามสเปกจะทิ้ง label ทิ้ง. แพตเทิร์น "ใส่ label ที่พ่อ แล้ว `aria-hidden` ลูกทุกตัวกันอ่านซ้ำ" จึงทำให้ **ทั้งบล็อกไม่ถูกอ่านสักคำ** ไม่ใช่แค่อ่านไม่ครบ (เคสจริง 2026-08-08: บรรทัดกำไรรายออเดอร์เงียบสนิท ทั้งที่ `MiniShipmentTimeline.tsx` โฟลเดอร์เดียวกันใช้ `role="img"` ถูกอยู่แล้ว). `title=` ไม่ใช่ตัวแทน (มือถือไม่มี hover) ต้องมีคู่กันเสมอ. ญาติกัน: `role="dialog"` ต้องมี `aria-modal="true"` ไม่งั้นผู้ใช้อ่านหลุดออกไปหลังฉาก — **ยกเว้น popover ที่เกาะปุ่มและหน้าหลังยังกดได้จริง ตรงนั้นไม่ใส่คือถูกแล้ว** (โฟลเดอร์ `orders/` พลาดพร้อมกัน 8 ใบ). **ไม่มี gate ไหนของโปรเจกต์จับคลาสนี้ได้** (`tsc`/build/detector/`theme-guard` ผ่านหมดเพราะ markup ถูกทุกตัวอักษร) — และ **สคริปต์สแกน regex เชื่อผลดิบไม่ได้ ต้องเปิดไฟล์ยืนยันทุกจุด** (รอบนั้น false positive 4 จาก 6)
  - `docs/conventions/external-payload-schema.md` — 🛑 **ชื่อฟิลด์เดียวกัน "คนละทิศทาง" ของ API เดียวกัน อาจแปลคนละอย่าง — ต้องพิสูจน์ด้วย payload จริงจากปลายทางก่อนแมป** (iShip: ขาออก `dst_district`=ตำบล · **ขาเข้า `dst_district`=อำเภอ `dst_area`=ตำบล**; เดาว่าสมมาตรแล้วผิด → **23 ออเดอร์บน prod ถูกบันทึกตำบล/อำเภอสลับกัน** ไปโผล่เป็น "ที่อยู่ไม่ผ่านการตรวจสอบ" ตอนเปิดพัสดุใบถัดไปของลูกค้าคนเดิม คนละหน้าจอคนละสัปดาห์กับต้นเหตุ 2026-08-07) · 🛑 **ห้ามอ้าง "ให้ผู้ใช้ตรวจด้วยตา" เป็นด่านกันข้อมูลเพี้ยน** — หัวไฟล์ `unlinked.ts` เขียนยอมรับเองว่าเดา แล้วรับความเสี่ยงด้วยเหตุผลว่าร้านต้องดูตารางเทียบก่อนกดผูก ผ่านไป 23 ใบไม่มีใครทัก (คนที่กดกำลังทำงานอื่นอยู่เสมอ) · **เทสที่แต่งค่าเองตามข้อสันนิษฐานของโค้ด ยืนยันได้แค่ว่า "โค้ดทำตามที่คนเขียนคิด" ไม่ใช่ว่า "คนเขียนคิดถูก"** — คู่ที่อ่อนไหวต้องยึดค่าจาก payload จริงและอ้าง track no. ไว้ในคอมเมนต์ · **ค่าที่ผู้ใช้กรอกแล้วส่งให้ระบบภายนอกตรวจ ยึดชุดข้อมูลของระบบนั้นเป็น SSOT** (`public/data/iship-address.json` — ไม่มีอำเภอชื่อ "เมือง" เดี่ยว ๆ เลย) สงสัยว่าเพี้ยน → สแกนทั้งฐาน เช็คคู่ปกติ→คู่สลับ เจอทั้งคลาสในนาทีเดียว · **UI ที่ผูกกับ error ภายนอก: ปุ่มทึบใบเดียวชี้ทางที่แก้ได้จริงตามชนิด error + ข้อความท้ายจอผูกกับชนิด error ด้วย** ห้ามซ่อนปุ่มที่ถูกลดตำแหน่ง — schema ของ payload ภายนอก (webhook/Graph) **ทุก field เป็น optional เป็นค่าตั้งต้น**: field ที่ประกาศเป็น "บังคับ" ทั้งที่ Meta ไม่ส่งเสมอ = **Valibot ตี event ตกทั้งก้อน → ข้อความหายเงียบ ๆ** (เคสจริง 2026-08-04: `AttachmentSchema.type` ทำรูป 6 ใบหายทั้งชุด) ตัวตัดสินความหมายต้องเป็น field ที่จำเป็นจริง (`payload.url` ไม่ใช่ `type`) · คอลัมน์ที่มี **ผู้เขียน 2 ราย** (เราเขียน + echo ของ Meta เขียน) ต้องตอบให้ได้ว่า "ค่าที่หายจาก payload = ไม่รู้ หรือ ถูกลบ" — ถ้าไม่รู้ **ห้ามเขียน null ทับ** (เคสจริง: รีแอ็กชันโผล่ 1 วิ แล้วหาย) · popover ต้องยึด `relative` ที่ **ปุ่ม** ไม่ใช่ที่แถว และห้าม `inset-x-0`/`w-auto` (พลาดทั้งสองทางในวันเดียว) — ที่มา `docs/retro/2026-08-04-fb-chat-comments-day-retrospective.md`
  - `docs/conventions/seller-action-placement.md` — 🛑 ตำแหน่งปุ่ม action ฝั่ง seller (4 โซน · destructive อยู่ใน `⋯`+confirm เท่านั้น). **§5.1: หน้าที่เข้าโหมด full-screen ทำให้ `SellerBottomNav` `return null` ทั้งก้อน → action ทุกตัวใน FAB หายไปพร้อมกัน** ต้องหาที่ใหม่ให้ในคอมมิตเดียวกัน (ปุ่มไอคอนขวาสุดของ sticky header); `/orders` เคยพลาดข้อนี้จน **สร้างออเดอร์บนมือถือไม่ได้เลย** โดยไม่มีอะไรฟ้อง — คอมเมนต์ที่อ้าง component อื่น ("ไม่ต้องมีเพราะ X มีให้แล้ว") ไม่ใช่หลักฐานว่า X ยัง render อยู่ในบริบทนั้น
  - `docs/conventions/sibling-surface-parity.md` — 🛑 สร้าง page/panel/tab ใหม่ใต้ route group ที่มีหน้าอื่นอยู่แล้ว **ต้องเปิดอ่านหน้าพี่น้องก่อนเขียนบรรทัดแรก** แล้วยกตัวกรอง/แท็บ/หัวข้อ/empty state/skeleton/ตัวนับมาใช้ซ้ำ — ไม่ยกมาต้องเขียนเหตุผลใน commit. theme บอกว่า "ปุ่มหน้าตายังไง" **พี่น้องบอกว่า "หน้านี้ควรมีปุ่มอะไรบ้าง"** (เหตุการณ์ 2026-08-03: แท็บความคิดเห็นถูกรื้อ 4 รอบ รอบสุดท้าย user ถามว่า "ตัวกรองทำไมไม่ทำเหมือนตรงข้อความ" ทั้งที่อยู่ใน `InboxList.tsx` โฟลเดอร์เดียวกัน). **ตัวเลข/สถานะเดียวกันที่โผล่ >1 ที่ ต้องมาจาก symbol เดียว** (เคสจริง: จอเดียวโชว์ "ยังไม่ตอบ" 7 กับ 8)
  - `docs/conventions/scroll-container-clips-popovers.md` — 🛑 popover/dropdown ที่เป็น `absolute` โดน `overflow` ของกล่อง scroll ที่ครอบมันตัดเสมอ — ต้องดูว่ากล่องนั้น **สูงเท่าไหร่** ไม่ใช่ไล่แก้ z-index. `SimpleBar` ที่สูงด้วย flex (`min-h-0 flex-1`) จะถูก `simplebar-core` เขียน `height:auto` ลง `.simplebar-content-wrapper` เอง → กล่อง scroll หดเท่าเนื้อหา แล้วตัดแผงตรงขอบล่างการ์ดพอดี **เฉพาะตอนรายการสั้น** (รายการยาวเส้นตัดไปอยู่ต่ำกว่าจอ จึงไม่มีใครเห็นมาหลายเดือน — user เจอเองบน prod 2026-08-06). แก้ด้วย `scrollableNodeProps={{ className: '… !h-full' }}` (ต้องมี `!` เพื่อชนะ inline style ที่เขียนทับทุกรอบ recalculate) — ที่เดียวได้ทุกดรอปดาวน์ในคอลัมน์นั้น. `tsc`/build/grep/hook มองไม่เห็นทั้งหมดเพราะตัวการอยู่ใน `node_modules`
  - `docs/conventions/overlay-scroll-lock.md` — 🛑 โมดัล/ชีต/เมนูเต็มจอที่ **ประกอบเองด้วย React state** ต้องเรียก `useLockBodyScroll` (`src/hooks/useLockBodyScroll.ts`) เสมอ + ทุก `overflow-y-auto` ในนั้นต้องมี `overscroll-contain`. ตัวที่ขับด้วย Preline ไม่ต้อง (`HSOverlay.open()` สั่ง `body overflow:hidden` ให้เอง) — **การ "แปลง hs-overlay เป็น controlled div" ซึ่งเป็นแพตเทิร์นมาตรฐานของโปรเจกต์นี้ ทิ้งการล็อก scroll ที่เคยได้ฟรีไปด้วยทุกใบตั้งแต่วันแรก** ไม่มีใบไหนสังเกต (เดสก์ท็อปแทบไม่มีอาการ · มือถือ = ลากทั้งหน้าจนเนื้อหาหลุดกรอบ, user เจอเอง 2026-08-07) · `touch-none` ที่ฉากเบลอกันเฉพาะนิ้วที่แตะฉากเบลอ ไม่กันนิ้วที่ลากบนตัวแผง · overlay ที่ portal ไป body มีบรรพบุรุษรับ scroll เป็น `document` เอง (chaining เดินตาม DOM tree ไม่ใช่ตำแหน่งบนจอ) · กล่องที่เนื้อหา**ยังไม่ล้น**ก็ chain ออกได้ บั๊กจึงชัดที่สุดตอนเนื้อหาสั้น
  - `docs/conventions/stored-flag-vs-owner-truth.md` — 🛑 ธงที่เก็บในแถวข้อมูล (`Product.fulfillmentMode`) คือ **ภาพนิ่ง ณ เวลาที่เขียน** ไม่ใช่ความจริงปัจจุบันของเจ้าของแถว — ร้านเปลี่ยน `vertical` ทีหลังได้ และ Quick-Create เขียนคอลัมน์เองโดยไม่ผ่าน SSOT ที่ประกาศไว้ (ตัวล็อกล็อกได้เฉพาะคนที่เดินผ่านประตู) · **กฎที่เป็น OR ต้องกั้นที่ผลลัพธ์ ไม่ใช่ operand เดียว** — ปิดข้างเดียวแล้ว repro หาย ≠ กฎถูก (2026-08-07 ร้านบริการโดนบังคับที่อยู่ **2 รอบในวันเดียว** เพราะแพตช์รอบเช้ากั้นแค่ครึ่ง) · ประกาศ SSOT ของคอลัมน์ไหนต้อง `rg "<column>:"` หาคนเขียนตรง ๆ ไม่ใช่ grep ชื่อฟังก์ชัน · ข้อมูลที่ติดธงผิดอยู่แล้วต้องมี migration ล้าง ไม่ใช่แค่ปิดทางเขียนใหม่
  - `docs/conventions/flex-header-truncation.md` — 🛑 **flexbox ตัดสินว่าจะ wrap ไหมจากขนาดเนื้อหาเต็ม *ก่อน* หด** → ใน `.card-header` ของ Paces (`flex-wrap`) การใส่ `truncate`/`min-w-0` ไม่มีผลกับการตกบรรทัดเลย ต้อง `flex-nowrap` ก่อน · เพดานความกว้างที่ตั้งไว้เพื่อเดสก์ท็อป (`max-w-56`) คือสาเหตุที่มือถือพัง ให้ไล่ตาม breakpoint · แถบเครื่องมือที่มีปุ่มบังคับมีข้อความ **ต้องกางเลขงบพื้นที่ที่ 320px ก่อนเสนอทางแก้** (ย่อคำมักไม่พอ ต้องยุบของเข้าเมนู) · popover `absolute` ยึด parent — ซ่อน "ปุ่ม" ได้ ห้ามซ่อน "กล่องยึด" ไม่งั้นแผงหายเงียบทั้ง breakpoint
  - `docs/conventions/unlayered-css-beats-utilities.md` — 🛑 **`_forms.css` ไม่ได้ห่อ `@layer` และ CSS ที่ไม่อยู่ใน layer ชนะ utility ที่อยู่ใน `@layer utilities` เสมอ ไม่ว่า specificity เป็นอย่างไร** → `className="form-input flex …"` ได้ `display:block` ตลอด (`items-center`/`justify-between`/`gap` จึงไม่เคยทำงานเลย) ต้องเขียน `flex!` (v4 suffix). กับดักพ่วง: `.form-input = h-11 lg:h-9.25` และ **`lg:` เป็น viewport query ไม่ใช่ container** → ชีตที่เรนเดอร์ในรางแชท 384px บนจอกว้างได้ช่องสูง 37px ต่ำกว่าเกณฑ์ 44px ทั้งที่นิ้วมีที่แตะเท่ามือถือ (คลาสเดียวกับ `.btn.btn-icon = 37px`) · **ฟีเจอร์ที่ทำให้ค่าที่แสดง "ยาวขึ้นเป็นปกติ" ต้องไล่ดู element ที่ขนาดคงที่ซึ่งเคยรอดเพราะเนื้อหาสั้น** — งานใหม่ไม่ได้สร้างบั๊ก แต่ปลุกบั๊กที่หลับอยู่ (user เจอเองบน prod 2026-08-09 ไอคอนตกบรรทัดในช่องวันเวลานัด)
  - `docs/conventions/ui-boolean-needs-a-testable-home.md` — 🛑 **เงื่อนไข boolean ที่ตัดสินว่า UI จะทำอะไร/ไม่ทำอะไร ห้ามอยู่ในเทอร์นารีกลาง JSX ต่อให้สั้นแค่ไหน** — เกณฑ์ไม่ใช่ "ซับซ้อนพอไหม" แต่คือ **"ถ้าเขียนกลับด้านแล้วจะมีอะไรจับได้ไหม"**. เคสจริง 2026-08-09: guard ปุ่ม "ย่อกลับ" เขียนเป็น `v ? !startsOutside : true` (กลับด้าน) → **ปุ่มไม่ทำงานเลยทุกกรณี** และผ่าน `tsc`/build/detector/theme-guard/grep ครบทุกด่าน เพราะเป็น boolean ที่ถูกต้องตามชนิดทุกประการ — สิ่งที่ผิดคือ *ความหมาย* ไม่ใช่ *รูปแบบ* · ยกเป็นฟังก์ชันบริสุทธิ์ใน `src/lib/**` + เทส `[blocker]` + **พิสูจน์ด้วย mutation** (คืนตรรกะผิดกลับไปแล้วต้องแดง) ไม่ใช่แค่เขียนเทสให้เขียว
  - `docs/conventions/ios-safe-area.md` — 🛑 `env(safe-area-inset-*)` **คืน 0 เสมอถ้า viewport ไม่มี `viewport-fit=cover`** — `pb-[env(...)]` ที่เขียนไว้ทั่ว `(paces)` จึงไม่เคยทำงานเลยจนถึง 2026-08-06 และ **ไม่มีอะไรฟ้อง** (tsc/build/CSS ถูกหมด มันแค่เป็น 0 เงียบ ๆ บนเครื่องจริง; desktop Chrome ก็คืน 0 → มองไม่เห็นจากเครื่อง dev) · `box-sizing:border-box` ทำให้ `h-18 + pb-[env(...)]` **ไม่โตขึ้น** ต้องเขียน `h-[calc(4.5rem+env(safe-area-inset-bottom))]` · **surface ที่ยึดขอบจอเป็นคนรับ inset ไม่ใช่ parent** (sticky/fixed ยึดกับ viewport ไม่ใช่ padding ของ wrapper)
  - `docs/conventions/upload-body-size-limit.md` — 🛑 **ห้ามส่งไฟล์ของผู้ใช้ผ่าน body ของ API route** — Vercel จำกัด request body **4.5MB** และตอบ `413 FUNCTION_PAYLOAD_TOO_LARGE` **ก่อนถึงโค้ดเรา** ด้วย body ที่ไม่ใช่ JSON → client อ่านเหตุผลไม่ได้ ขึ้น "ลองใหม่อีกครั้ง" ทั้งที่โค้ดเขียนเพดานไว้ 5/10/25MB ทั้ง 4 ที่ (ไม่มีที่ไหนมีผลจริง; ร้านแจ้ง 2026-08-10 ว่า "อัปโหลดคลิปเกิน 1 นาทีไม่ได้" ความจริงคือ **ทุกอย่างเกิน 4.5MB ส่งไม่ได้** ตั้งแต่คลิป 10 วินาที) · ใช้ `@/lib/upload-client` เท่านั้น (ticket → PUT ตรงเข้า storage → commit) · เพดานที่บังคับได้จริงมี 2 ชั้น: `file_size_limit` ของ bucket (Supabase ตอบ 413 เอง **และไม่เขียนไฟล์**) + commit ที่อ่านขนาดจริงด้วย HEAD แล้ว **ลบไฟล์ที่ไม่ผ่านทิ้ง** — ตัวเลขที่ client แจ้งไม่ใช่ด่าน · claim ต้องผูก `fileId` กับ `userId` (commit ลบไฟล์ได้ = ถ้าไม่ผูกก็ลบไฟล์ร้านอื่นได้) · flow ใหม่ยิง **2 request ต่อไฟล์** ต้องมี rate-limit bucket แยกให้ `/api/uploads/*` ไม่งั้นแนบรูปกริด 24 ใบโดน 429 กลางทาง · **ไม่มี gate ไหนจับคลาสนี้ได้** (`tsc`/build/detector/grep ผ่านหมด เพราะเลขที่เขียนไว้ "ถูก" ทุกตัว มันแค่ไม่มีผล) — ความรู้เรื่องเพดาน 4.5MB มีอยู่ในรีโปแล้วฝั่งขาออก (`MAX_RANGE_CHUNK`) แต่ไม่มีใครโยงมาถึงขาเข้า
  - `docs/conventions/hook-return-identity-in-deps.md` — 🛑 **ห้ามใส่ค่าที่ custom hook คืน "ทั้งก้อน" ลง dep array ถ้า hook นั้น `return` เป็น object/array literal** ให้ destructure เอาเฉพาะ `useCallback` ที่ต้องใช้ไปใส่แทน — อ็อบเจกต์ใหม่ทุก render ไม่มีวันเท่าเดิมตาม `Object.is` ถ้า effect นั้น `setState` ด้วยจะปิดวงจรเป็น **ลูปยิง API ไม่หยุด** (เคสจริง 2026-08-09: `/inbox/comments` ยิง `/api/chat/comments/posts` รัวจน user เจอเองบน prod) · 🛑 **การ memo อ็อบเจกต์ที่ hook คืนไม่ใช่ทางแก้** เพราะค่าที่เป็นสถานะต้องเปลี่ยนอยู่แล้ว = ลูปเดิมแค่ช้าลงจนไม่มีใครสังเกตอีกเลย · **ไม่มี gate ไหนจับได้ และ `react-hooks/exhaustive-deps` จะบอกให้เขียนแบบที่พังเป๊ะ ๆ** (ดีเพนเดนซีที่ครบถ้วนและผิด — เครื่องมือตรวจ "รูปแบบ" แต่สิ่งที่ผิดคือความเสถียรของ identity) · **หยิบ hook จากหน้าพี่น้องมาใช้ ต้องดูว่า "ท่าเรียก" เหมือนกันไหม** ไม่ใช่แค่ว่ามันถูกใช้ที่นั่นแล้วไม่พัง (`useListBusy` ปลอดภัยที่ `/orders` เพราะที่นั่นเรียกจาก event handler อย่างเดียว ไม่เคยมี effect ผูกกับมัน)
- **Retros:** `docs/retro/` (post-mortems of phase mistakes — read the latest one before starting a new phase)
- **Plans / specs:** `docs/superpowers/plans/`, `docs/superpowers/specs/`

## Architecture

- **Profile-Centric** — Trust Profile เป็นศูนย์กลาง ทุกอย่างไหลเข้า profile
- ไม่แบ่ง role buyer/seller — ทุกคนมี trust profile เหมือนกัน, เปิดร้านเพิ่มได้ (isShop flag)
- Subdomain routing: main (buyer), `seller.*`, `admin.*` — handled in `src/proxy.ts`
- Session แยกตาม subdomain — login/logout แยกกัน, account เดียวกัน
- Prod domain `deepthailand.app`; dev `deepth.local`

## Tech Stack

- **Framework:** Next.js 16 (App Router, Turbopack) — TypeScript strict mode
- **UI:**
  - Buyer + landing + public (`(marketing)/**`) → **Vuexy** (MUI v9 + Emotion + Tailwind 4)
  - Seller + admin (`(paces)/**`) → **Paces** (Preline 4 + Tailwind 4, no MUI)
- **Database:** PostgreSQL 16 (Supabase), Prisma ORM
- **Auth:** NextAuth.js v4 (`FacebookProvider` + `phone-otp` CredentialsProvider + `seller-credentials` CredentialsProvider bcrypt)
- **Validation:** Valibot (backend/API), Yup (frontend react-hook-form)
- **Form:** React Hook Form + `@hookform/resolvers`
- **Icons:** `@iconify/react` (on-demand) — use tabler icon names (e.g. `tabler-phone-check`)
- **Charts:** ApexCharts, ECharts, Chart.js (wrappers in `src/components/wrappers/`) — **chart ใน `(paces)/**` ต้อง copy structure จาก `theme/paces/Admin/TS/src/app/(admin)/widgets/charts/components/` และผ่าน `ApexChart` wrapper เสมอ (Hard Rule 10)**
- **Alerts:** react-toastify (mounted once in `(marketing)/ToastMount.tsx`)
- **Testing:** Vitest
- **Container:** Docker + Docker Compose (local Postgres); prod on Supabase

## Directory Structure

```
src/
├── app/
│   ├── (marketing)/           # Vuexy route group (buyer + landing + public)
│   │   ├── layout.tsx         # MUI ThemeProvider + Anuphan font + ToastMount
│   │   ├── auth/              # sign-in, sign-up, verify-otp
│   │   ├── dashboard/         # (→ to be wrapped in (buyer-app)/ per R2)
│   │   ├── orders/            # same
│   │   ├── reviews/           # same
│   │   ├── settings/          # same
│   │   ├── u/[username]/      # public profile
│   │   ├── o/[token]/         # public order
│   │   └── _components/       # shared client wrappers (mui-link, etc.)
│   ├── (paces)/               # Paces route group (seller + admin)
│   │   ├── layout.tsx         # Preline + Tailwind + AppProvidersWrapper
│   │   ├── seller/            # seller dashboard, products, orders, verification, etc.
│   │   │   ├── auth/          # sign-in, sign-up, verify-otp, reset-pass, new-pass (Paces auth/split)
│   │   │   └── onboarding/    # mandatory onboarding page (5-step; force-redirect ถ้า needsOnboarding)
│   │   └── admin/             # admin auth + (partial) dashboard
│   └── api/                   # Backend — unified across subdomains
├── @core/, @layouts/, @menu/  # Vuexy theme engine (copied from theme/vuexy)
├── assets/, components/,      # Paces scaffolds (copied from theme/paces)
│   config/, context/, hooks/,
│   layouts/, utils/
├── lib/                       # auth, prisma, otp, storage, subdomain, validations,
│                              #   sms, sms-unlock-cookie, sms-consume-rl,
│                              #   shop-slug, shop-categories, password (seller auth 2026-06-16)
├── services/                  # user, shop, verification, trust-score, badge,
│                              #   product, order, review, history-linking,
│                              #   wallet, sms-code, topup
├── types/
└── proxy.ts                   # Subdomain router (main/seller/admin)

theme/
├── vuexy/typescript-version/full-version/src/   # Vuexy source (reference only)
└── paces/Admin/TS/src/                          # Paces source (reference only)
```

## Core Systems

1. **Verification** — หลายระดับ: OTP (L1) → เอกสาร (L2) → จดทะเบียนธุรกิจ (L3), admin review
2. **Trust Score** — คำนวณจาก Verification 35%, Orders 25%, Rating 20%, Age 10%, Badges 10%. MVP มีแต่ขึ้น (no penalties)
3. **Badges** — Verification badges (auto) + 10 achievement badges (auto evaluated)
4. **Simple OMS** — Seller creates order → public `/o/{token}` → buyer phone-unlock → ยืนยัน → review → trust recalc. Types: PHYSICAL / DIGITAL / SERVICE / SUBSCRIPTION
5. **Buyer History Linking** — Buyer confirms as guest (contact) → signs up later → `linkBuyerHistory` auto-links by phone/email match. Wired in `lib/auth.ts` on phone-OTP + Facebook signup.
6. **SMS Order Link + Seller Wallet** (paid ฿1/SMS) — Seller L2+ กดส่ง link เข้า SMS buyer; link ฝัง 12-char short-code → `/api/o/sms/{code}` consume → HMAC-signed httpOnly cookie → buyer ข้าม phone-unlock อัตโนมัติ. Credit ledger: `SellerWallet` (1:1 Shop, balance ฿ integer, DB CHECK≥0), `WalletTransaction` (TOPUP/DEDUCT), `TopUpRequest` (slip → admin approve/reject, RC-7 self-block). Services: `wallet.service` (conditional-updateMany atomic deduct), `sms-code.service` (hash-at-rest single-use), `topup.service`, `lib/sms.ts` (generic sendSms apitel), `lib/sms-unlock-cookie.ts` (HMAC NEXTAUTH_SECRET), `lib/sms-consume-rl.ts` (RC-1 per-IP 10/15min globalThis).
7. **Seller Auth + Onboarding** (2026-06-16/17) — Seller login ด้วย username+password (provider `seller-credentials`, bcrypt, `lib/password.ts`) + Phone OTP signup + reset-via-OTP + Facebook OAuth (live prod). เบอร์โทร **immutable** (ตั้งครั้งเดียวผ่าน `/api/account/set-phone`, สร้าง L1 auto). `Shop.slug` (@unique, `src/lib/shop-slug.ts`) **บังคับ** — ไม่มี slug หรือ phone → `needsOnboarding=true` ใน JWT → `proxy.ts` force-redirect → `/onboarding` (mandatory 5-step page). Libs: `shop-categories.ts` (10 key), `shop-slug.ts` (normalize/validate/reserved), `password.ts` (bcryptjs).

## Conventions

- **Language:** TypeScript strict mode; UI copy ภาษาไทย
- **เอกสารทั้งหมดใช้ภาษาไทยเป็นหลัก** — retros (`docs/retro/`), convention docs (`docs/conventions/`), commit message bodies, code comments อธิบาย "ทำไม" ใช้ภาษาไทยเป็น default ยกเว้น: file paths, class/function names, library names (Next.js, Prisma, Vuexy, Paces, TanStack), technical jargon ที่ไม่มีคำแปลไทยที่ชัดเจน (RSC, JWT, OAuth, OTP), commit hashes
- **Font:** Anuphan (Google Fonts) — buyer/landing via `next/font`; Noto Sans Thai reference only
- **Mobile-first:** Tailwind breakpoints (`sm:`/`md:`/`lg:`)
- **Service layer** (`src/services/`) is separated from API layer (`src/app/api/`)
- **Input validation:**
  - Backend (API routes): Valibot schemas from `src/lib/validations.ts`
  - Frontend (forms): Yup + `@hookform/resolvers`
- **No Redux** — use Server Components + React state/context
- **Icons:** use `@iconify/react` with tabler names — never bundle a static icon set
- **Commit granularity:** one task/feature = one commit. Cite `Base:` theme file for UI commits (see Hard Rule 3).
- **QA:** Chrome DevTools MCP (`mcp__chrome-devtools__*` tools) is the baseline E2E check for UI tasks. curl + type-check alone are insufficient.

## Current State Snapshots

- **2026-04-13:** Attempted Paces-wide UI rewrite (original plan wiped Vuexy).
- **2026-04-18 (AM):** User reversed the decision for buyer side — buyer + landing back to Vuexy. Admin + seller stay Paces.
- **2026-04-18 (PM):** P1 buyer build shipped (9 commits) but retrospectively violated theme-copy rule; R1-R11 rework planned. See `docs/retro/2026-04-18-p1-retrospective.md`.
- **2026-05-17:** Phase 4 SMS Wallet complete — backend B1-B4 + UI B5-B8 built (tsc 0). ช่องว่างเหลือ: admin `/topups/[id]` detail page ยังไม่มี (Phase 5 todo). Accepted-risk Phase 5 hardening: CSRF Origin-check + slip cookie path narrow.
- **2026-05-23:** `/u/[username]` public profile redesign complete (user approve visual แล้ว) — single-column Instagram-style card (max-width 640px). ข้อมูล live จาก DB: trust banner (Deep tier names), avatar, shop identity, verified chip, badges, product grid (≤9 active), avg rating (aggregate ทั้งหมด — bug fix), order count + completion rate. Cross-platform stats + on-time/response = placeholder "ตัวอย่าง" (Phase 2). Follow/Chat = disabled "เร็ว ๆ นี้" (Phase 2). Services เพิ่ม: `getAvgRatingByUsername` (review.service), `getProductsByShop(shopId, take?)` (product.service). Spec: `docs/superpowers/specs/2026-05-23-shop-public-profile-design.md`.
- **2026-06-06:** branch `feat/seller-orders-phase-a` (push ถึง `a2218f4`). หลายงานปิดในวันเดียว:
  - **Seller Orders Phase B = VERIFIED-COMPLETE** — เคลียร์ QA-debt (safepay-qa E2E ผ่านทุก level: 4-block create, customer search, derive type, VAT/discount breakdown, DB persist 7 field). Unit D = **superseded** by Paces re-source; แทนด้วยเติม Phase B fields ในหน้า seller order detail (honest breakdown ใน OrderSummary + payment/channel/buyerName ใน CustomerDetails + การ์ด ShippingAddress) commit `d57e965`.
  - **PII RSC leak fix** — seller page อยู่ใต้ client `VerticalLayout` → Next serialize raw PII เข้า flight payload. แก้ด้วย mask + neutralize `order.buyerContact`/`order.review.reviewerContact` ที่ server boundary (S-C1). audit หน้า seller อื่น = clean. ดู memory `feedback_rsc_pii_neutralize_at_source`.
  - **CSRF + rate-limit (NFR-2.2/2.3, PRD §11 #11 CLOSED)** — `guardApi` ใน `src/proxy.ts` (Next 16 nodejs runtime): Origin-check (mutation, allowlist `*.deepthailand.app`/dev `*.deepth.local`) + per-IP rate-limit (unauth 100/auth 30 ต่อนาที, in-memory globalThis). lib: `src/lib/csrf-origin.ts` + `src/lib/api-rate-limit.ts` (+ Vitest 16 tests). `/api/auth/*` ยกเว้น; otp/sms limit เดิมคงไว้. **known-gap:** Vercel serverless = per-instance, Redis = Phase 2. spec `docs/superpowers/specs/2026-06-06-csrf-ratelimit-design.md`, plan `docs/superpowers/plans/2026-06-06-csrf-ratelimit.md`.
  - **Admin dashboard ครบ 8 metrics (PRD §11 #10 CLOSED)** — เติม Completion Rate / Avg Review Rating / Active Users (30d) ใน dashboard page + `api/admin/dashboard`.
  - **PRD gap เหลือ:** §11 #3 shippingAddress required-when-SHIPPED (quick win), FR-6.10 SUBSCRIPTION recurring (P4, effort สูง), FR-9.6 tier names ใน PRD ขัด SSOT (doc-sync — โค้ดถูกแล้ว).
  - **Carried debt:** Chrome-DevTools MCP visual QA (CSRF/RL, admin 8-card, order detail) — MCP หลุดทั้ง session, verify ด้วย authenticated-curl + DB-query แทน. รอ MCP กลับมาเก็บรวด.
- **2026-06-07: 🚀 FIRST PROD DEPLOY** — phase-a (210 commits) merged → `origin/main` (FF) + deployed prod `deepthailand.app` (smoke-test ผ่าน: 200 ทุก subdomain, CSRF guard live 403). + pre-prod gaps ปิดเพิ่ม: shippingAddress required-when-SHIPPED (FR-6.5), admin self-review bypass fix (FR-2.6 — orphan route ลบ + service guard), menu/breadcrumb i18n, FR-9.6 tier doc-sync, brand email.
  - **Prod infra (สำคัญ — ดู memory `project_prod_deploy_setup`):** Vercel project `trust-me` reconnect จาก `trustme` → **`deep-web/main`** (prod ผูก repo นี้แล้ว). prod DB = **dev Supabase ตัวเดียวกัน** (แชร์ — ควรแยกภายหลัง). **git auto-deploy ใช้ได้** (push origin main → deploy; verified commit `867c702`); deploy สำรอง `vercel deploy --prod --yes` (CLI). หมายเหตุ Hobby = 1 concurrent build — ถ้า deploy ค้าง "Initializing" นาน → `vercel remove <url> --yes` ปลด slot. prod env: apitel added (phone-OTP/SMS ใช้ได้); **FB creds ยังขาด** (FB login ปิด — OTP ใช้แทน).
- **2026-06-16: Seller Auth Redesign — P1+P2+P3 SIGNED-OFF** (branch `feat/seller-order-detail-optionD`, ยังไม่ merge main). seller ได้ **username+password login** (provider `seller-credentials`, bcrypt) + phone OTP ยืนยันตอนสมัคร + ตั้ง/ลืมรหัสผ่าน via OTP + Facebook; 5 หน้า auth จาก Paces `auth/split` (mobile เต็มจอ); **onboarding modal** (welcome→category chips→slug บังคับ→สินค้าแรก ข้ามได้) เด้งเมื่อ `session.needsOnboarding`. schema: `Shop.slug @unique` (migration applied Supabase). E2E ผ่าน real route + simulated SMS (test phone `0000000009`/`123456` dev-only). **⚠️ หลัง migrate ต้อง restart dev server** (stale Prisma client → session 500). docs: spec/plan/baseline/retro `docs/{superpowers/specs,scope,retro}/2026-06-16-seller-auth-*`; QA checklist `docs/qa/seller-auth-qa-checklist.md`; memory `project_seller_auth_resume`. carry: visual mobile QA (MCP), FB prod creds, Redis hardening.
- **2026-06-17: Facebook Login live บน prod + Mandatory Onboarding page + Mobile fixes**
  - **Facebook OAuth live (consumer + seller):** `FACEBOOK_ID`/`FACEBOOK_SECRET` ใส่ใน Vercel prod + `.env.local`; `FacebookProvider` (lib/auth.ts) ดึง avatar `graph.facebook.com/{id}/picture?type=large` (~200px); `next.config.ts` ขาว `graph.facebook.com`; `jwt` callback อัปเดต avatar ทุก login; username เริ่มต้น = `fb{facebookId}` (เดิม `user_${ts}`); backfill FB user เดิม. **เทส FB ได้บน prod เท่านั้น** (FB App ต้องการ https; App ยัง Dev mode)
  - **`/auth/callback/facebook`:** หน้า loading Paces spinner รอ session → redirect /dashboard (~1.5s); ปุ่ม FB ทุกหน้าชี้ `callbackUrl=/auth/callback/facebook`
  - **Mandatory /onboarding page** (แทน OnboardingModal บน dashboard): `src/app/(paces)/seller/onboarding/page.tsx` (AuthCardShell); 5 step: phone (FB-first) → ข้อมูลร้าน (displayName/category/username) → OTP → slug → สินค้าแรก; `proxy.ts` force-redirect seller authed+`needsOnboarding` → `/onboarding` ทุก route (ยกเว้น /auth,/api); `OnboardingModal.tsx` = dead code (ลบทีหลัง)
  - **API ใหม่:** `POST /api/account/set-phone` (phone immutable — ตั้งครั้งเดียว; 409 ถ้ามีแล้ว; สร้าง L1 PHONE_OTP auto), `POST /api/account/shop-info` (displayName/username/category), `GET /api/shops/check-slug`, `POST /api/shops/slug`
  - **Business rules ใหม่:** เบอร์โทร **immutable** (ตั้งครั้งเดียว เปลี่ยนไม่ได้ — มีผลต่อ Trust Score); onboarding **บังคับ** (ไม่มี slug หรือ phone → เด้ง /onboarding ทุก login)
  - **Mobile-responsive + theme fixes:** tap target ≥44px ทุกหน้า, Choices select match form-input, auth card width = theme, dashboard cosmetic
  - **Bug fix:** OTP signup prod "รหัสไม่ถูกต้อง" = orphan AuthAccount(PHONE) จาก block test account ไม่ครบ (P2002) — แก้ด้วย data (block AuthAccount จริง)
  - **Carry:** `OnboardingModal.tsx` dead code (ลบทีหลัง), username 30-day cooldown (edit หลัง onboarding — feature อนาคต), FB App Review `email` (เปิด public), FB App secret regenerate
- **2026-07-04: Buyer-app UI cohesion + Landing polish + Responsive (tablet) overhaul** (buyer/Vuexy `(marketing)/**` เท่านั้น; ยึด Impeccable design.json)
  - **Buyer-app cohesion — ทุกหน้า "บัญชีของฉัน" ใช้ pattern เดียวกัน:** `PageHeader` กลาง (`(buyer-app)/_components/PageHeader.tsx`) title/subtitle/action ใช้ร่วม orders/reviews/badges/messages/profile/verification. orders + reviews เขียนใหม่จาก TanStack client table → **server-rendered card list** (ลบ `OrderListTable.tsx`/`ManageReviewsTable.tsx` — เบาลง ไม่มี client JS หนัก). messages(inbox) ห่อ Card + แถวสไตล์เดียวกัน. empty-state + status สี/label (PENDING=warning/SHIPPED=info/CONFIRMED=success เขียว/CANCELLED=error) ตรงกันทั้ง dashboard widget + หน้าเต็ม
  - **Search/Filter:** `SearchBox` (client island เล็ก, `?q=` ใน URL, list ยัง server-render) — orders (ค้นหา+chips สถานะ), reviews (ค้นหา+chips ดาว ALL/5–1). กรอง in-memory (buyer มี order/review ไม่มาก)
  - **รูปแบบวันที่ไทย:** `format-date.ts` เพิ่ม `formatDateTH` "01 ส.ค. 2569" / `formatDateTimeTH` "01 ส.ค. 2569 19:30" / `formatTimeHM` "19:30" (ไม่แตะของ seller/admin). ใช้ทั่ว buyer (orders/reviews/o/messages/chat/profile). **perf:** cache `Intl.DateTimeFormat` เป็น singleton (เดิมสร้างใหม่ทุก format)
  - **Impeccable fixes:** ตัด `uppercase` eyebrow (ProfileForm) → section label subtitle2 semibold; badges `bg-primary/10` → token var. Verified-Means-Green ทั่วทุกหน้า
  - **Landing (/):** ตัดโลโก้บริษัทต่างชาติปลอมจาก theme ใน CustomerReviews (Netflix/Airbnb/… ทั้งบนการ์ด + แถบ "trusted by") → เหลือ testimonial ไทยจริง; ลบ dead files GetStarted/OurTeam/ContactUs; ย้ายเมนู "ราคา" → `/#pricing-plans` (เลื่อนไป section) + ลบหน้า `/pricing` + `views/front-pages/pricing`; navbar เมนูอยู่กลาง (logo·เมนู flex-1·actions)
  - **Header/nav:** `solidHeader` prop → header ทึบ+เงาทันทีบน buyer (ไม่ต้อง scroll); landing คงโปร่งที่ท็อป. `ScrollToTop` (`(buyer-app)/_components/`) เด้งบนสุดทุกครั้งเปลี่ยน route
  - **Responsive (desktop+tablet; mobile <768 รอจัดแยก):** เมนูซ้าย buyer โผล่ตั้งแต่ **768px** ใช้ `min-[768px]:` (Vuexy remap Tailwind `md`=900/`lg`=1200 → ต้องใช้ arbitrary variant ให้ตรง 768). content 2-col ภายใน (banner/stat) split ที่ `lg`(1200) เพราะ tablet main แคบลง 240px จาก sidebar. width container (navbar+footer/layoutSpacing+buyer) ลบ cap tier 900 → ช่วง 900–1199 fluid (iPad Pro 1024 ขอบเหลือ 24px). **Sticky footer** ที่ FrontLayout (`flex flex-col min-bs-[100dvh]` + children `flex-1`) → footer ติดล่างสุดเสมอ ไม่มีขาวใต้ footer. Footer Grid `lg`→`md`+sm 4/4/4 → iPad Pro 4-col, iPad Air/Mini brand เต็มแถว+3 คอลัมน์เท่า
  - **local-only (ห้าม commit):** `lib/otp.ts` (test accounts เดิม), `lib/csrf-origin.ts` (localhost dev — commit แล้วปลอดภัยเพราะ `!isProd` gate; ตอนนี้ commit แล้ว)
- **2026-08-02: "กระจายที่อยู่" แล้วบันทึกออเดอร์ไม่ผ่าน — merged main `ba8e464b` + prod** (retro `docs/retro/2026-08-02-order-address-paste-bugfix.md`)
  - **parser** (`lib/parse-order-message.ts`): เพิ่มรายชื่อ 77 จังหวัดสะกดตามชุดข้อมูล iShip ไว้จับกรณีไม่มี marker `จ.` (ที่อยู่ กทม. เขียนแบบนี้เป็นปกติ) + normalize `กรุงเทพมหานคร`/`กทม.` → `กรุงเทพ`; ตัดเบอร์ออกจากบรรทัดแทนการข้ามทั้งบรรทัด (ข้อความบรรทัดเดียวเคยได้ทั้งชื่อและที่อยู่ว่าง); บรรทัดที่มีแต่เบอร์ไม่ถูกหยิบมาเป็นที่อยู่
  - **`lib/shipping-address-status.ts` (ใหม่) = SSOT ฝั่งหน้าจอของ "ที่อยู่ครบพอบันทึกไหม"** — กฎเดิมเขียนซ้ำ 3 ที่แล้วนิยามไม่ตรงกับ `createOrder` (บังคับ `line1 + province + postcode`) ทำให้ปุ่มขึ้น "เลือกแล้ว" ทั้งที่ยังขาด. ปุ่มที่อยู่มี 3 สถานะจริง; ตำบล/อำเภอ ไม่บล็อกการบันทึกแต่เตือนว่าต้องเติมก่อนเปิดพัสดุ iShip
  - **`setError` ที่ไม่มีใคร render = error เงียบ** — `shippingAddress.line1/.province/.postcode` ถูก set ตอน submit มาตลอดแต่ไม่เคยแสดงทั้ง quick form และ POS; แก้แล้วทั้ง 2 surface (`is-invalid` + ข้อความใต้ช่อง + `aria-describedby`)
  - **แก้ Verified-Means-Green ที่ละเมิดจริง** — `AddressSearchPanel` ขึ้นเช็กถูกสีเขียวกับที่อยู่ที่ยังไม่ครบ (คอมเมนต์เขียนอ้างกฎนั้นอยู่บนโค้ดที่ละเมิดเอง) → primary เมื่อครบ / danger เมื่อขาด
  - **carry:** browser QA (ยังไม่เคยกดจริง), เช็กถูกสีเขียวในรายการผลค้นหาที่อยู่ทั้ง 2 picker
  - ~~`react-toastify` ตกค้างใน `(paces)` 3 ไฟล์~~ — **ตรวจซ้ำ 2026-08-03 แล้วไม่จริง ยกเลิกรายการนี้**: ทั้ง 3 จุด (`ChannelsClient`, `MovementHistoryTable`, `ConnectedAccountsClient`) เป็น **คอมเมนต์ที่อ้างถึงกฎ** ไม่ใช่ import — ไฟล์ทั้งหมดใช้ `pacesToast` ถูกต้องอยู่แล้ว. ต้นเหตุคือ grep gate ของ HR9 ที่ match คำเปล่า ๆ (แก้แล้ว ดู HR9)

- **2026-08-02 (feature 00026): หน้า `/account` "ข้อมูลส่วนตัว" + สร้างร้านส่วนตัวจาก account switcher — merged main + prod** (retro `docs/retro/2026-08-02-feature-00026-personal-account-retrospective.md`, docs `docs/20 - Features/00026 - Personal Account & Connections/`)
  - **`src/lib/onboarding-gate.ts` = SSOT ใหม่ของ `needsRegistration`/`needsOnboarding`** — ทั้ง `jwt` และ `session` callback ใน `lib/auth.ts` เรียกตัวเดียวกัน และต้องเรียก **หลัง** block ที่ resolve `activeShopId` เสมอ. กฎ: บังคับ setup เฉพาะตอน `activeShopId === personal shop id` ไม่งั้นผู้ถูกเชิญที่กดสร้างร้านส่วนตัวจะถูก proxy ขังใน `/onboarding` ทุก route ออกไม่ได้
  - **หน้า `/account` ผูกกับ `session.user.id` ล้วน — ห้ามเรียก `requireActiveShop`/อ่าน `activeShopId` ในหน้านี้** (นั่นคือเหตุผลที่หน้านี้มีอยู่: แยก "ตั้งค่าตัวคน" ออกจาก "ตั้งค่าร้าน" ที่ `/shop`)
  - `ConnectedAccountsClient` ย้ายจาก `settings/` → `account/components/` + เพิ่มแถวตั้ง/เปลี่ยนรหัสผ่าน; `/settings` เหลือเฉพาะการจัดส่ง (เมนูเปลี่ยนชื่อเป็น "การจัดส่ง"), กลุ่มเมนูใหม่ "บัญชีของฉัน" อยู่ล่างสุด
  - **security fix ที่ไปกับรอบนี้ (`eb32a937`)**: `PATCH /api/users/me` เคยส่ง body ดิบเข้า `prisma.user.update` → ยิง `{"isAdmin":true}` เป็นแอดมินได้; ปิดด้วย `UpdateProfileSchema` (Valibot allow-list) + pick field ใน service + `GET` เลิกคืน `passwordHash`
  - API ใหม่: `check-username`, `otp-for-password`, `set-password-otp` — 2 ตัวหลัง resolve เบอร์จาก session ไม่รับจาก client (คืนแค่ `phoneMasked`)
  - **ข้อมูลจริง prod 2026-08-02:** User 10 คน ไม่มีเบอร์ 6 · ไม่มีทั้งเบอร์และรหัสผ่าน 5 (กู้บัญชีไม่ได้ถ้าหลุดจาก FB/LINE) → ขึ้นแถบเตือนใน `/account` ไม่บังคับเพิ่มเบอร์
  - **carry:** browser QA 15 เคสยังไม่เคยกดจริงสักครั้ง (`TestCase.md` §3.1) · E2E ข้ามตามที่ user ตัดสิน · `AccountAvatar` ใส่ `rounded-full` ให้ทั้ง business/personal จึงไม่มี convention "วงกลม=คน สี่เหลี่ยม=ร้าน"

- **2026-08-03 (feature 00028): ประเภทร้านค้า 2 → 3 แบบ — DEPLOYED PROD** (`68348a49`, migration `20260803140000_shop_business_type` apply แล้ว; docs ที่ `docs/20 - Features/00028 - Shop Business Type/` — **มี 7 ไฟล์แต่ยังไม่ครบ template: `TestCase.md` ขาด** มี `UX-Design-Spec.md` เข้ามาแทนที่จำนวนพอดี ทำให้เคยถูกบันทึกผิดว่า "7/7"; retro → `docs/retro/2026-08-03-feature-00028-shop-business-type-retrospective.md`)
  - 🛑 **`Shop.vertical` = `ONLINE_SALES` (ขายออนไลน์) | `SERVICE_QUEUE` (สินค้าและบริการ) | `LODGING` (บ้านพัก) — ค่า `GENERAL` ถูกลบถาวร** มี CHECK constraint `Shop_vertical_check` กันที่ระดับ DB (unmanaged SQL — **ห้าม `prisma db pull`**) · label `LODGING` เปลี่ยนจาก "บ้านพักตากอากาศ" → "บ้านพัก" · SSOT อยู่ที่ `src/lib/lodging.ts`
  - **เงื่อนไขเปิดคิวงานเหลือ `vertical==='SERVICE_QUEUE'` เงื่อนไขเดียว** — ตัด `kind==='BUSINESS'` ออกจาก `canUseAppointments` (`src/lib/appointments.ts`) → **บัญชีบุคคลใช้ระบบคิวงานได้แล้ว** และ `applyAppointmentMenu` ถูกยุบเข้า `applyVerticalMenu` (ไม่ต้องมีฟังก์ชันแยกอีก)
  - **`seller-menu.ts` เปลี่ยนจาก deny-list เป็น allow-list ต่อ vertical + fail-closed** (`VERTICAL_VISIBLE_SLUGS`) — ค่าที่ไม่รู้จักตกไป `ONLINE_SALES` แทนที่จะหลุดเมนูของประเภทอื่น
  - **`vertical` immutable ยกเว้นทางเดียว:** `POST /api/shops/update` ตั้งได้ตอน `Shop.slug === null` (onboarding ยังไม่จบ) มี slug แล้ว → **409 `VERTICAL_LOCKED`** (ห้าม ignore เงียบ)
  - สินค้าที่สร้างในร้าน `SERVICE_QUEUE` ได้ `fulfillmentMode = NO_SHIPPING` เป็นค่าตั้งต้น (DB default คือ `SHIPPED`) — ส่งผ่าน parameter `shopVertical` เข้า `createProduct` ไม่ query ใน service
  - **ปิดช่องโหว่ที่มีอยู่ก่อนแล้ว 2 จุด:** ระบบประมูล (`api/seller/auctions/_shared.ts::requireSellerShop`) และ Inventory Add-on (`requireOnlineSalesVertical()` × 7 route) **ไม่เคยมี server-side vertical guard เลย** กันด้วยการซ่อนเมนูอย่างเดียวมาตลอด — ทั้งคู่ครอบ GET ด้วย ไม่ใช่แค่ mutate
  - 🛑 **public profile มี 2 เส้น ไม่ใช่เส้นเดียว** — `/u/[username]` (ทุกร้าน) + **`/b/[slug]` (เฉพาะ BUSINESS)** ใช้ `ShopProfile.tsx` ตัวเดียวกัน แก้เส้นเดียวไม่พอเสมอ · label แท็บสินค้าเปลี่ยนเป็น **"สินค้า"** (เดิม "สินค้าและบริการ" ซึ่งชนกับชื่อ vertical ใหม่ตรงตัว) · เพิ่มแท็บ "บริการ" + `PublicServiceList.tsx` (Base: `PublicRoomList.tsx`)
  - **onboarding ทั้ง 2 ที่แตกตาม vertical** (`seller/onboarding/page.tsx` + `BusinessOnboardingWizard.tsx`): step เลือกประเภทเป็นจอแรก · step สุดท้าย = ฟอร์มสินค้า (ONLINE_SALES) / ฟอร์มคิวงาน (SERVICE_QUEUE) / **ไม่มี step แล้วไป `/rooms/new`** (LODGING) · dot progress 5 หรือ 4 จุดตามที่เลือก
  - **บทเรียนสำคัญ:** ตรรกะ `vertical === 'LODGING' ? A : B` **ไม่พังแบบเห็นชัด**เมื่อเพิ่มค่าที่ 3 แต่เงียบ ๆ ปล่อยค่าใหม่ตกเข้า branch ผิด และ `rg "'GENERAL'"` จับไม่ได้ทั้งกรณี object key (`Record<ShopVertical,...>`) และกรณี binary — วิธีที่ได้ผลคือขยาย type union ให้ TypeScript บังคับ key ครบ แล้ว grep `vertical`/`isLodging` ควบคู่ไปด้วย
  - **carry:** browser QA (user รับไปกดเองบน prod 2026-08-03) · P3 Public Profile เต็มรูปตาม `UX-Design-Spec.md` §B (ตอนนี้เป็น ready-state) · retro · `docs/scope/` baseline ยังไม่มีสำหรับ 00028

- **2026-08-03 (feature 00029): แท็บ "ความคิดเห็น" Facebook ในกล่องแชท — merged main + prod** (`0389e14b`; retro `docs/retro/2026-08-04-facebook-comments-00029-retrospective.md`, docs `docs/20 - Features/00029 - Facebook Comments Inbox/` = PRD+BRD เท่านั้น ยังขาด SRS/SDS/API/Tests)
  - route `src/app/(paces)/seller/(chat)/inbox/comments/` — โพสต์ซ้าย 50% (สื่อ + ยอดไลก์/คอมเมนต์/แชร์ชิดล่าง) / คอมเมนต์ขวา 50% เลื่อนแยก; ตอบสาธารณะ + แนบรูป + อิโมจิ + คอมเมนต์ที่โพสต์เอง; **เล่นวิดีโอในหน้าเราได้** ผ่านปลั๊กอินสาธารณะของ Facebook
  - **ปลั๊กอิน Facebook เรนเดอร์ตามค่า `width` ใน URL ไม่ใช่ขนาด iframe** → ต้องวัดกล่องด้วย `ResizeObserver` แล้วส่งตัวเลขนั้นเข้า URL + ล็อก aspect-ratio จากรูปปกจริง
  - service `page-comment.service.ts`; realtime = **Broadcast from Database signal-only** (trigger ส่งแค่ `postId`/`commentId` แล้ว client refetch ผ่าน GET ที่ auth — โปรเจกต์ไม่มี RLS การส่งทั้งแถว = data leak). migration 3 ตัว apply prod แล้ว
  - **ชื่อผู้คอมเมนต์:** คอมเมนต์ที่เข้ามาทาง webhook ได้ `from.name`; ที่ดึงย้อนหลังผ่าน Graph **ไม่มี `from` เลย** (ขอ `fields=from{id,name}` ก็ตอบ 200 แล้วตัดคีย์ทิ้ง) → เติมชื่อให้คอมเมนต์เก่าโดยจับคู่ `fromExternalId` กับคอมเมนต์อื่นของคนเดียวกัน. 🛑 **ที่เคยเขียนว่า "นโยบาย Meta ไม่ใช่บั๊ก" คือคำอธิบายที่ผิดและมันปิดทางสืบสวนไป 6 วัน** — ตัวจริงคือฟีเจอร์ `Business Asset User Profile Access` ที่ยังเป็น **Standard Access** (= ให้ข้อมูลเฉพาะคนที่ **มี role บนแอป**) ไม่ใช่กฎถาวร; วันที่ผ่าน Advanced Access ชื่อจะกลับมาเองทั้ง 141 ใบที่ค้าง — ดู `docs/conventions/graph-access-depends-on-subject.md`
  - **scope ผูกกับ token ตอน grant** — เพิ่ม `pages_read_user_content`/`pages_manage_engagement` ใน `CONNECT_SCOPES` แล้ว token เดิมยังไม่มี ต้องให้ user เชื่อมเพจใหม่ + ยืนยันด้วย `debug_token`
  - งาน 00018 ที่ไปด้วยรอบเดียวกัน: พิมพ์ในแชทมือถือแล้วเธรดเด้ง (WebKit clamp `scrollTop` ตอนกล่อง composer หดชั่วขณะ — `useComposerHeight.ts`), รีแอ็กชันข้อความ + `message_edits` (**โค้ดขึ้นแล้วแต่ยังไม่เคยทดสอบ**), `rawMessage` เก็บ payload ดิบก่อน Valibot
  - **carry:** ลบ probe log `[fb-raw-evt]` ใน webhook · ทดสอบรีแอ็กชัน/`message_edits` · SRS/SDS/API/Tests · App Review 2 scope · browser QA ทั้งแท็บ · Enter ส่งคอมเมนต์ + quick message/AI ในคอมเมนต์

- **2026-08-04: กดค้างในแชท + Command Center สถานะพัสดุ + แถวออเดอร์เห็นเลขพัสดุ — DEPLOYED PROD** (`f3ff5768`; retro `docs/retro/2026-08-04-chat-longpress-command-center-retrospective.md`)
  - **กดค้างบนข้อความในมือถือ = เบลอทั้งเธรด + ยกบับเบิลนั้นขึ้นมาเพ่ง** (`MessageActionBubble.tsx`) — โคลนบับเบิลไปลอยเหนือฉาก ไม่ยกตัวจริง เพราะบับเบิลอยู่ลึกใน Chat Rail ที่มี overflow/transform ยก z-index ทะลุ portal ระดับ body ไม่ได้ · ซ่อนตัวจริงด้วย `visibility` ระหว่างเปิด ไม่งั้นขอบเบลอฟุ้งรอบโคลนเป็นเงาสองชั้น · prop เป็น union `MessageAnchor` เพราะเมนูนี้มี **2 ทางเข้า** (กดค้างมือถือ = เบลอ / ปุ่มหน้ายิ้ม hover เดสก์ท็อป = popover ห้ามเบลอทั้งจอ)
  - 🛑 **`position:fixed` บน iOS ยึดกับ layout viewport ซึ่งไม่หดตามคีย์บอร์ด** — overlay ที่คำนวณจาก `window.innerHeight` จะไปนอนใต้คีย์บอร์ด. แก้ด้วย blur ช่องพิมพ์ตอนเปิด + clamp ด้วย `visualViewport` + **reposition ไม่ใช่ close** เมื่อ resize/scroll (ไม่งั้นคีย์บอร์ดยุบแล้วเมนูเด้งหายเอง = แย่กว่าบั๊กเดิม)
  - **Command Center ร้าน `ONLINE_SALES` เปลี่ยนเป็น "สถานะคำสั่งซื้อ"** — รอเลขพัสดุ / รอรับเข้า / กำลังจัดส่ง / พัสดุมีปัญหา · เกณฑ์ทุกช่องมาจาก `deriveShippingStage()` (`src/lib/order-stage.ts`) **ตัวเดียวกับที่ `/orders?stage=` ใช้กรอง** — ห้ามนับด้วย SQL แล้วกรองด้วย TS (จะกดเลข 5 เข้าไปเจอ 4)
  - 🛑 **เลขพัสดุมี 2 ทางเข้า เก็บคนละตาราง** — ส่งเอง → `ShipmentTracking.provider` (ชื่อขนส่ง ไม่มีรหัส) · ผ่าน iShip → `OrderShipment.courierCode/courierName` · ฝั่งอ่านต้องอ่านทั้งคู่ ให้ iShip ชนะเมื่อมีพร้อมกัน (`docs/conventions/one-value-many-entry-points.md`)
  - **โลโก้ขนส่ง 8 ไฟล์** แมปที่ระดับ **แบรนด์** ไม่ใช่รายรหัส (`src/lib/iship/courier.ts` — dropdown จริงมี 17 รายการแต่เป็นแพ็กเกจของ 9 แบรนด์: Flash 6 แบบ, KEX 2, ไปรษณีย์ไทย 2, DHL 2) · ได้โลโก้ 19/20 ตัวเลือก · เติมโลโก้ใหม่ที่ `COURIER_BRANDS` ที่เดียว UI ไม่ต้องแก้
  - **แท็บ ข้อความ|ความคิดเห็น**: badge ทั้ง 2 แท็บ realtime (`/api/chat/inbox-tab-counts` + subscribe 2 channel) · แท็บไม่หายตอนเลื่อน (rail = ย้ายออกนอก scroller, มือถือ = sticky + `tabsAbove` ดันหัวรายการลงไปเกาะใต้แท็บ) · เสียงแชทดังตอนอยู่แท็บความคิดเห็น (`ChatSoundListener` แขวนแทนตำแหน่ง rail)
  - ~~carry: ตัวกรองสถานะพัสดุยังไม่มีชิปในหน้า `/orders`~~ — **ปิดแล้ว 2026-08-04 รอบสอง** (`d33290db`): ชิป `StageChips` ใน `OrdersList.tsx` render 2 ที่ (หัวสติกกี้มือถือ + เหนือการ์ดตารางเดสก์ท็อป) นับจาก symbol เดียว · `?stage=` เดิม**ไม่มีผลบนเดสก์ท็อปเลย** เพราะ `OrdersTable` รับ `orders` ก้อนดิบ ตอนนี้รับ `stageFiltered` แล้ว · `pushQuery()` คง `?status=`/`?stage=` ไว้ด้วยกัน (คนละแกน) · หน้าแรกเดสก์ท็อปได้ `OrderStatusBand` ตัวเดียวกับมือถือ · ลบ `OrderStatusRow.tsx` (dead code) · **ยังไม่ได้ browser QA** (เวิร์กทรีนี้ไม่มี `.env.local`)
  - **carry:** ปุ่ม "ทักแชท" ในแท็บความคิดเห็นยังเป็นป้ายบอกเวลา ไม่ใช่ปุ่มกดได้ (ต้องทำ Private Replies ก่อน)
  - **browser QA: ปิดหนี้แล้ว** — user กดทดสอบเองบน prod ครบทุกงาน 2026-08-04

- **2026-08-05→06: เปรียบเทียบราคาขนส่ง iShip (00022-ext) + ปรับหน้า orders ยกชุด — DEPLOYED PROD** (ล่าสุด `efc8f3a3`; docs หัวข้อ "ส่วนขยาย 2026-08-05"+"แก้ 2026-08-06" ใน 00022)
  - **ปุ่ม "เทียบราคา" ในฟอร์มสร้างพัสดุ** → `POST /api/seller/iship/price/compare` (server fan-out **ชุดละ 4**) → `PriceCompareSheet` เรียงถูก→แพง badge ถูกที่สุด/เร็วที่สุด · sheet เป็น **view swap + `@container`** (แผงแชทแคบบนจอ desktop ต้องได้ layout การ์ด — viewport breakpoint ใช้ไม่ได้)
  - 🛑 **`unwrap` ใน `lib/iship/client.ts`: object ที่ไม่มี key envelope เลย (`status/success/data/message`) = payload สำเร็จ คืนทั้งก้อน** — check-price ตอบ payload เปล่า เดิมถูกตีเป็น `UPSTREAM_ERROR http=200` ทุกคำขอ (quote รายตัวจึง**พังเงียบบน prod มาตลอด**โดยไม่มีใครเห็น เพราะ fail-silent by design — feature ที่ fail เงียบต้องมี console.error เสมอ) · `total_price ≤ 0` = ขนส่งไม่รองรับเส้นทาง เข้า `failed[]` ไม่ใช่การ์ดราคา ฿0 (Fuze Post เคยชนะ "ถูกที่สุด") · พังทุกเจ้า → ตอบ 200+`failedDetail` รายเจ้าให้อ่านจากหน้าจอ (vercel logs CLI stream ใช้ไม่ได้จริง)
  - **หน้า orders ยกชุด (user สั่งไล่ทีละจุด 2026-08-06):** คอลัมน์ "ที่มา" (รูปเพจ+badge แพลตฟอร์มห้อยมุม — `OrderSourceLogo`; Order เก็บแค่ salesChannel จึงชี้เพจได้เฉพาะร้านเพจเดียว) · คอลัมน์ขนส่ง/เลขพัสดุ/mini timeline (`MiniShipmentTimeline` — stage จาก `deriveShippingStage` SSOT เดิม) · ตัดคอลัมน์+ฟิลเตอร์ "ประเภท" · toolbar ใหม่ (ไม่มีป้าย "กรอง:", dropdown "พัสดุ" พร้อมจำนวนแทนแถบชิป desktop — ผูก `?stage=` symbol เดียวกับชิปมือถือ, "แถวละ N") · hover panel รายการสินค้า+timeline ผ่าน **`HoverPanel` (portal ระดับ body)** — cell อยู่ใน `.table-wrapper` overflow-auto, absolute โดน clip · หัว order detail ใช้รูปเพจ+badge (`OrderSummary` prop `pageLogoUrl`)
  - ปุ่มหลัก PENDING ที่เปิดพัสดุ iShip แล้ว (ขนส่งยังไม่เข้ารับ) = **"สถานะพัสดุ"** ไม่ใช่ "แจ้งเลขพัสดุ" + คัดลอกเลขใน ⋮ (`order-action-set.ts` ใช้ `shipmentSource` แล้ว) · timeline การ์ดการจัดส่งชิดขอบ content · การ์ดออเดอร์ข้างแชท: ใบยกเลิกเหลือชิปเดียว (footer badge ซ้ำถูกถอด) · `FilterDropdown` รองรับ `badge` ต่อ option
  - **browser QA: user ทดสอบเองบน prod ตลอดทั้งรอบ** (สั่ง "ไม่ต้อง QA เทส") — เจอบั๊กแล้วรายงานเป็น screenshot สด ๆ ทั้ง 4 ตัวข้างบน

- **2026-08-06 (feature 00033): เลือกวันที่/เวลาของคำสั่งซื้อได้ — DEPLOYED PROD 5 รอบ** (`6f12cd6b` ฟีเจอร์หลัก → `019d1f68` critique P1 → `0357e5ed` clarify → `632f57b9` polish → `c02efbc5` optimize; docs `docs/20 - Features/00033 - Backdated Order Date/` ครบ 7 ไฟล์ · retro `docs/retro/2026-08-06-feature-00033-backdated-order-date-retrospective.md` · scope `docs/scope/2026-08-06-00033-*`)
  - 🛑 **`Order.createdAt` เปลี่ยนความหมายเป็น "วันที่ลูกค้าสั่ง"** ผู้ขายระบุเองได้ ย้อน 90 วัน / ล่วงหน้า 7 วัน — **ทับคอลัมน์เดิมตรง ๆ ไม่เพิ่มฟิลด์แยก** (มติ D-1) จึงพา **เลขออเดอร์** (`formatOrderNo` คิดปี/เดือนจากค่านี้ และหน้าจอคำนวณสด ไม่ได้อ่านคอลัมน์ `orderNo`) **ลำดับในรายการ** (keyset `createdAt DESC`) และ **ยอดขายทุกหน้า** เคลื่อนไปพร้อมกันทั้งชุด. SSOT เพดานเวลา = `src/lib/order-date-window.ts` (ห้าม hardcode 90/7 ที่อื่น) · PATCH ที่เปลี่ยนวันที่ต้อง recompute `orderNo` ในทรานแซกชันเดียวกัน
  - 🛑 **`OrderEvent.occurredAt` = เวลาจริงที่กดเสมอ ห้ามย้อนตามวันที่ที่ผู้ใช้กรอก** — วันที่สั่งซื้ออยู่ใน `meta.orderedAt` (ประวัติคือหลักฐาน). ชนิดใหม่ `ORDER_DATE_CHANGED` → `OrderEvent.type` เป็น **13 ค่า** (คอมเมนต์ใน `schema.prisma:2725` ยังเขียน "9 ค่า" ล้าสมัย)
  - 🛑 **migration ที่แก้ CHECK แบบรายชื่อ ต้องอ่านของเดิมมาต่อท้าย ห้าม hardcode** — `20260806120000` ของเราชนกับ `_order_shipment_cod_settled` (branch iShip) พอดีตัว แล้วตัวที่รันทีหลังลบค่าของกันเองเงียบ ๆ (migrate สำเร็จทุกไฟล์ ไม่มี error ไปโผล่เป็น insert ล้มบนฐานจริง) → `docs/conventions/migration-check-constraint-additive.md`
  - ป้ายช่องวันที่ผันตาม `Shop.vertical` ผ่าน `ORDER_VOCAB.dateLabel` (ช่องที่ 5) — **ห้ามต่อ `"วันที่" + noun`** เพราะ LODGING จะได้ "วันที่บิลเข้าพัก" ซึ่งชนกับ `Order.checkIn` คนละคอลัมน์ (SERVICE_QUEUE ก็มี `serviceStart`)
  - แก้บั๊ก timezone ที่มีอยู่ก่อนไปด้วย: `/sales` + `/orders` เคยตัดวันด้วย UTC ขณะที่ dashboard/P&L ใช้เวลาไทย → ใช้ `thaiDayKey()` แล้วทั้งคู่
  - ผ่าน impeccable ครบ 4 คำสั่ง (critique 22/40 · clarify · audit 17/20 · optimize) — **ทั้ง 7 ปัญหาที่เจอ หลุด `tsc`/`build`/detector/grep gate ทั้งหมด** รวมถึง `btn-ghost` ที่ไม่มีอยู่จริงในธีม, คอนทราสต์ตก 4 จุดที่ใช้ token ถูกกฎ, และ `focus:outline-none` ที่ไม่มีตัวแทน
  - **carry:** browser QA 75 เคส (user รับไปกดเองบน prod 2026-08-06) · E2E Playwright · P2 จาก critique 3 ข้อ (ปุ่มเขียนทับโดยไม่ถาม · ปี พ.ศ. บนป้าย vs ค.ศ. ใน native picker · ลำดับชั้น 4 บรรทัดตอนขยาย)

- **2026-08-06: หน้าสินค้ามือถือเป็นการ์ดแยกใบแบบ `/orders` + บั๊กสร้างออเดอร์บนมือถือ — DEPLOYED PROD** (`d428e0b2` + `988dcf18` → main `4e21f293`; spec `docs/superpowers/specs/2026-08-06-seller-products-mobile-card-redesign.md`)
  - **ต้นเหตุที่หน้า `/products` มือถือใช้ยากคือสถาปัตยกรรม ไม่ใช่การจัดสไตล์** — ใช้ `mobileCard` prop ของ `DataTable` ซึ่ง**ตั้งใจ**ไม่ให้แถวมี `.card` ของตัวเอง (ทั้งตารางถูกครอบด้วย `.card` ใบใหญ่อยู่แล้ว) → ทางแก้คือย้ายมือถือออกมานอกการ์ดแม่ทั้งหมด แยกเป็น `ProductsTable`(เดสก์ท็อป)/`ProductCard`(มือถือ) + `ProductsListing` เป็น orchestrator ถือ state ร่วม
  - **การ์ด 2 ชั้น ไม่ใช่ 4 ชั้นแบบ `/orders`** — ก็อป *ภาษาการออกแบบ* (การ์ดแยกใบ · เส้นประคั่น · ลำดับชั้นปุ่ม · ตัวนับบนชิป) ไม่ใช่ก็อปจำนวน layer; สินค้าไม่มี items/shipping/payment ซ้อนกัน บังคับ 4 ชั้นแค่ทำให้เห็นสินค้าต่อจอน้อยลง
  - ลำดับชั้นปุ่มใหม่: **แก้ไข = primary filled** (งานที่ทำถี่ที่สุดต่อสินค้า 1 ชิ้นหลังเผยแพร่) · ปักหมุด = outline · **ลบหายจากการ์ดไปอยู่ใน `⋮` + Swal** · `⋮` มี toggle เปิด/ปิดขายกดครั้งเดียว (ไม่ต้อง confirm เพราะย้อนกลับได้ในคลิกเดียว)
  - 🛑 **toggle เปิด/ปิดขายต้อง optimistic `pinnedAt`+`pinnedCount` ไปด้วย** — `product.service.ts:351` auto-unpin (BR-PIN-11) แต่ `serializeProduct()` **ไม่มี field `pinnedAt`** → client รู้ไม่ได้จาก response ไม่งั้นการ์ดค้างไอคอนปักหมุด+badge "ปักหมุด n/m" ผิดจนกว่าจะรีโหลด. **response ที่ไม่มี field นั้น ≠ field นั้นไม่เปลี่ยน**
  - 🛑 **หน้า full-screen ซ่อน `SellerBottomNav` ทั้งก้อน → FAB "สร้าง" หายไปด้วย** — `/orders` มีบั๊กนี้อยู่จริงมาตลอด (คอมเมนต์เขียนว่า "มือถือใช้ FAB ใน bottom nav" แต่ nav ตัวนั้นไม่ render ในหน้านั้น) **สร้างออเดอร์จากหน้า `/orders` บนมือถือทำไม่ได้เลย** ไม่มีอะไรฟ้อง (ปุ่มมีจริงในโค้ด แค่ `hidden` บนมือถือ) แก้แล้วทั้ง 2 หน้าด้วยปุ่มไอคอนขวาสุดของ sticky header → `docs/conventions/seller-action-placement.md` §5.1
  - `TYPE_COLORS` (badge ประเภท 4 สี) ถูกลบทั้งมือถือ+เดสก์ท็อป — สงวนสีไว้กับ state จริง (`isActive`) อย่างเดียว; `TYPE_LABELS` ย้ายไป `data.ts` เป็น `PRODUCT_TYPE_LABELS`/`PRODUCT_TYPE_ICONS` กัน drift · ราคาบนการ์ดเลิกใช้ `text-primary` → `text-default-900` ตัวหนา · ปุ่มลบตารางเดสก์ท็อปเปลี่ยนจาก HSOverlay ดิบเป็น `pacesConfirm.danger`
  - **carry:** browser QA ทั้งหน้า (user รับไปกดเองบน prod 2026-08-06) — จุดเสี่ยงที่ static ตรวจไม่ได้: ปุ่มท้ายการ์ดกับ stretched-link ทับทั้งใบ, เมนู `⋮` ที่เด้งขึ้นบนในการ์ดใบแรกอาจโดนหัว sticky บัง · E2E · `/orders` มือถือยังเข้าไม่ถึง "ดึงจาก iShip" (desktop-only มาแต่เดิม ไม่เคยอยู่ใน FAB — คนละเรื่องกับบั๊กที่แก้)

- **2026-08-06 (รอบเย็น): iShip — "ลองใหม่" ที่ไม่ลองอะไรใหม่ / ชิปโกหก / เครดิตไม่พอ — DEPLOYED PROD** (`ec3ade1c` `b6cf5243` `bc33dc34` `61ffea08`; สเปก `docs/20 - Features/00022 …/SRS.md` §18 + `TestCase.md` E8 · retro `docs/retro/2026-08-06-iship-retry-and-credit-retrospective.md`)
  - 🛑 **`retryShipment()` ต้องอ่านที่อยู่จาก `Order.shippingAddress` ใหม่ทุกครั้ง** — `receiverSnapshot` คือบันทึกว่า "ยิงอะไรออกไป" ไม่ใช่แหล่งความจริง. เดิมอัปเดต snapshot เฉพาะตอนมี `receiverPatch` แนบมา แต่ `createShipment()` ที่เจอใบ FAILED เรียก retry โดยไม่ส่ง patch/override ต่อ → **เขียนที่อยู่ใหม่ลงออเดอร์แล้วยิงที่อยู่เก่าออกไป** ร้านแก้กี่รอบก็ไม่มีผล (`DP256908869471CB`). กติกาทั่วไป: ปุ่ม "ลองใหม่" ที่อ่านจาก snapshot เดิม = replay ไม่ใช่ retry
  - 🛑 **นิยาม "ออเดอร์นี้มีพัสดุแล้ว" = `OrderShipment.status='CREATED' AND isDryRun=false` ที่เดียวทั้งระบบ** — LATERAL join ใน `order-stage.service.ts` เคยใช้ `<> 'CANCELLED'` จึงนับใบ FAILED ด้วย แล้วแถวในกล่องแชทขึ้นชิป "สร้างพัสดุแล้ว" ทั้งที่ไม่มีเลขพัสดุ (`DP256908A896B1BE`). คอมเมนต์เตือนเรื่องนี้อยู่ที่ **ฟังก์ชันที่คำนวณ** (`order-stage.ts`) แต่บั๊กอยู่ที่ **query ที่ป้อนข้อมูลเข้าไป** คนละไฟล์ — กติกาที่ใช้ร่วมหลายที่ต้องบังคับที่จุดดึงข้อมูล ไม่ใช่คอมเมนต์เตือนใจ
  - 🛑 **regex คัดแยก error ไทยให้จับ "คำนาม" ไม่ใช่ "รูปปฏิเสธ"** — `เครดิต.*ไม่พอ` ไม่ match `"เครดิตไม่เพียงพอ"` (ไม่มีสตริง `ไม่พอ` อยู่เลย) → เงินไม่พอกลายเป็น `UPSTREAM_ERROR` ที่ **retryable** → จอเชิญให้กดวน 3-4 ครั้งทั้งที่ไม่มีทางสำเร็จ. การจัดประเภทผิดให้เป็น retryable อันตรายกว่าปกติเพราะมันสั่งให้ผู้ใช้ทำสิ่งที่ไร้ผลซ้ำ ๆ
  - **เครดิต iShip:** ยอดที่เปิดพัสดุได้ = เงินคงเหลือ − **เครดิตที่ถูกกันไว้** (สำรองค่าส่งของพัสดุที่ยังไม่เคลียร์สถานะ) หน้าแรก iShip โชว์ยอดรวม ร้านจึงเห็นว่ายังมีเงินทั้งที่เปิดไม่ได้ · **ไม่มี endpoint อ่านยอดเครดิต** รู้ได้ตอนถูกปฏิเสธเท่านั้น · ลิงก์เติมเงินต้องเป็นหน้าแรก `https://app.iship.cloud/` (`/wallet/topup` เข้าตรงไม่ได้)
  - `toShipmentView` ส่งข้อความดิบเข้า `IShipError` แล้ว — ข้อยกเว้นของ `REJECTED_BY_CARRIER` (ต่อท้ายรายละเอียดจากขนส่ง) **ไม่เคยทำงานเลยตั้งแต่วันแรก** เพราะสร้าง error จาก code เปล่า
  - **carry:** ปุ่ม "ลองใหม่" ยังไม่อ่าน `IShipError.retryable` (ต้องผ่าน ux gate) · browser QA E8-4/E8-6/E8-7 · ~~`orders/[token]/page.tsx` แก้ `hasShipment` แล้วแต่ยังไม่ commit~~ **ปิดแล้ว 2026-08-07** (`1ef05373`)

- **2026-08-07: การ์ด hover สถานะพัสดุในหน้า `/orders` ใช้งานไม่ได้จริงมาตลอด — DEPLOYED PROD** (`10c6e815` → `1ef05373`)
  - 🛑 **พื้นที่ hover เท่ากับกล่องของ wrapper ไม่ใช่ขนาดเนื้อหา** — `HoverPanel` ห่อด้วย `<div>` ธรรมดาซึ่งกว้างเต็ม cell คอลัมน์ที่อยู่กว้างกว่าบล็อก "จัดส่งโดย" หลายเท่า ที่ว่างขวามือทั้งแถบจึงเปิด panel ด้วย (user: "hover ห่างมาก ๆ ก็ขึ้น") → ผู้เรียกต้องส่ง `w-fit`/`inline-flex` มาเองเสมอ
  - 🛑 **`onMouseEnter`/`onMouseLeave` ของ React สังเคราะห์จาก React tree ไม่ใช่ DOM tree** — panel ที่ `createPortal` ไป body ยังนับเป็น "ลูก" ของ div ที่เรียกมัน การย้ายเมาส์ trigger↔panel จึง**ไม่ยิงทั้ง leave และ enter** ที่ตัว wrapper (มันคือ common ancestor) รูปแบบ "ตั้งเวลาปิดตอน leave / ยกเลิกตอน enter" เลยพังครึ่งเดียว: ขาไปรอด ขากลับ panel→trigger ไม่มีใครยกเลิกตัวนับ แล้ว panel หุบทั้งที่เมาส์ยังอยู่ → ใช้ `onMouseOver` (bubble ขึ้นมาจากทุกจุดในซับทรี รวม portal) เป็นสัญญาณ "ยังอยู่ในพื้นที่"
  - panel เคยเป็น `pointer-events-none` ทั้งที่ในการ์ดมีปุ่ม "คัดลอกเลขพัสดุ" อยู่จริง — hover panel ที่มีของกดได้ข้างในต้องออกแบบเป็นพื้นที่เดียวกับ trigger ตั้งแต่แรก ไม่ใช่ display-only แล้วค่อยแปะปุ่มทีหลัง
  - ไปด้วยรอบเดียวกัน: badge ประวัติลูกค้าในตาราง (`bg-{semantic}/15` + `-ink` แทน icon เปล่าสีเทา) · หัวการ์ดออเดอร์เลิกโชว์ชื่อ/รูปผู้ซื้อ (ซ้ำกับการ์ด "ผู้ซื้อ" — ถอด prop ทั้งสายไม่ให้ serialize ลง flight payload)
  - **carry:** browser QA ทั้ง 3 อาการ (user รับไปกดเองบน prod)

- **2026-08-07 (รอบบ่าย): แผงโหลดทับรายการในหน้า `/orders` + เก็บตก overlay scroll-lock 11 ใบ — DEPLOYED PROD** (`8ea9806a` `28d68d35` `18bd8de5` `d9c149b7`)
  - **`orders/components/ListBusyOverlay.tsx` (ใหม่) = แผงโหลด + hook `useListBusy()`** — user สั่ง "ทุกการ filter หรือ load ข้อมูลใหม่ มี preloading ขึ้นมาทับเสมอ ตามเวลาที่ใช้". `busy = isPending || floor` — `isPending` จาก `useTransition` คาไว้จนกว่างานจะจบจริง (ทั้ง `router.push`/`refresh` ที่รอ RSC payload และ setState ที่รอ React render) ส่วน `floor` คือพื้นเวลาขั้นต่ำ 350ms กันแผงกะพริบ ~16ms จากงาน client ที่จบใน 1 เฟรม (ซึ่งอ่านเป็นจอกระตุกไม่ใช่การโหลด) — **พื้นขั้นต่ำไม่ตัดของที่นานกว่าทิ้ง** เพราะเป็น OR ไม่ใช่ timeout
  - 🛑 **ทับเฉพาะพื้นที่ผลลัพธ์ ไม่ใช่ทั้งหน้า** — แถบเครื่องมือ/ชิป/ช่องค้นหาคือสิ่งที่ผู้ใช้เพิ่งกดและกำลังจะกดต่อ ถ้าทับด้วยจะกดรัวไม่ได้ และช่องค้นหาจะหายไปใต้แผงตั้งแต่ตัวอักษรแรก · ช่องค้นหาใช้ `begin()` ไม่ใช่ `run()` เพราะ **controlled input ที่ถูก defer จะพิมพ์ตามนิ้วไม่ทัน**
  - **ห่อที่ callback ระดับ table (`onSortingChange`/`onColumnFiltersChange`/`onPaginationChange`) ไม่ใช่ไล่ห่อทีละ handler** — ครอบเรียงหัวคอลัมน์/ดรอปดาวน์ทุกตัว/เปลี่ยนหน้า/แถวละ N ในที่เดียว และจะไม่หลุดเมื่อมีคนเพิ่มตัวกรองใหม่ทีหลัง · `state` อยู่ที่ `OrdersList` ตัวเดียวคุมทั้ง 2 breakpoint เพราะ `?stage=` ถูกกดจาก toolbar ของตารางแต่ navigation เกิดที่ `OrdersList`
  - พื้นแผงเป็น **ทึบเต็ม** (user สั่งรอบสอง) — เดสก์ท็อป `bg-card` (อยู่ในการ์ดใบใหญ่) · มือถือ `bg-body-bg` (การ์ดแยกใบวางบนพื้นเพจ ใช้ขาวจะกลายเป็นแผ่นแปะ) · สปินเนอร์ `sticky top-28` เพื่อให้ยังเห็นตอนเลื่อนลงไกลแล้วกดกรองจากหัวสติกกี้ · **lazy-load ตอนเลื่อนไม่ทับ** ยังใช้สปินเนอร์ inline ที่ sentinel
  - 🛑 **เก็บตก scroll-lock อีก 11 ใบที่ `01132960` มองไม่เห็น** — ทั้งหมดเป็น JSX ก้อนหนึ่งฝังในไฟล์ที่ชื่อไม่ได้ลงท้าย `Modal`/`Sheet` (`OrdersList`/`ProductsListing` = โมดัลตัวกรอง, `KeywordEditorClient` = `ExceptionSheet`, `SlipViewer` = lightbox) · **`SlipViewer` ไม่ใช่ "ไม่มีล็อก" แต่ล็อกเองไม่ครบ** (สั่งแค่ `body overflow:hidden` ตกท่า iOS 2 อย่าง) — ล็อกที่ดูเหมือนถูกกันตัวเองออกจากการค้นหา รายละเอียด + สคริปต์สแกน → `docs/conventions/overlay-scroll-lock.md` §"รอบเก็บตก 2026-08-07"
  - **หมายเหตุเครื่องมือ:** `theme-guard.sh` เช็ค carve-out comment ของ HR7 เฉพาะ **บรรทัดเดียวกับ class** (ท่าที่ผ่าน = `className={'...' /* เหตุผล */}`) — 4 จุดที่เขียนไว้บรรทัดบนแดงค้างมาตลอด แก้แล้ว · **ยังเหลือ false positive:** HR12 (emoji) ไม่ยกเว้นบรรทัดคอมเมนต์ ทั้งที่ CLAUDE.md carve-out `code comment marker` ไว้ → `🛑` ที่ใช้ขึ้นต้นคอมเมนต์ทั่ว repo ทำให้ guard แดงทุกครั้งที่แก้ไฟล์นั้น (แก้ได้บรรทัดเดียว: ให้ HR12 ใช้ `scan_nocomment`)
  - **carry:** browser QA ทั้งหมด (แผงโหลดตอนเลื่อนลงไกลแล้วกดชิป · 11 ใบที่ตรึง scroll — เปิดแผงแล้วลากนิ้วบนมือถือ) · `DraftOrderProvider` ยังไม่ตัดสิน (เอกสารยกเว้นไว้ด้วยเหตุผลของเดสก์ท็อป แต่บนมือถือเป็น `fixed inset-0` เต็มจอ — ควรผูก `matchMedia` แบบ `ExpenseCategoryFilterSheet` หรือไม่)

- **2026-08-07 (รอบบ่ายแก่): ร้านบริการโดนบังคับที่อยู่ (รอบสอง) + UI แชท 4 จุด — DEPLOYED PROD** (`24d9dcdb` `eba494ea`; retro `docs/retro/2026-08-07-service-shipping-guard-and-chat-ui-retrospective.md`)
  - 🛑 **แพตช์เมื่อเช้า (`936ab241`) กั้นแค่ครึ่งเดียว — ผู้ใช้เจออาการเดิมเป๊ะอีกครั้งใน 2 ชม.ครึ่ง** เกณฑ์ "ต้องจัดส่งไหม" เป็น OR: `(รายการพิมพ์เอง ∧ PHYSICAL) ∨ (มีสินค้าที่ธง=SHIPPED)` รอบเช้าเติมด่านให้ operand ซ้ายอย่างเดียว พอร้านเลือกสินค้าจากแคตตาล็อกใบถัดไป operand ขวายิงแทน · **`Product.fulfillmentMode` ไม่ใช่หลักฐานว่าร้านส่งของ** — ติดธงค้างได้จากร้านที่เปลี่ยน `vertical` ทีหลัง (BT 2026-08-05) และจาก **Quick-Create ใน `createOrder`/`updateOrder` ที่เขียนคอลัมน์ตรง ๆ ไม่ผ่าน `resolveFulfillmentMode`** ซึ่งประกาศตัวเองเป็น SSOT (BR-BKU-13) มาตั้งแต่ 00030 — ตัวล็อกล็อกได้เฉพาะคนที่เดินผ่านประตู · **`updateOrder` ไม่เคยมีด่านนี้เลยแม้ชั้นแรก** (กด "แก้ไข" ใบเดิมก็โดนบังคับ ไม่มีใครรายงานเพราะยังไม่มีใครไปถึง) · กั้นครบ 4 จุดแล้ว + migration `20260807150000` ล้างธงค้าง (prod 1 แถว) · SRS ได้ FR-6.5a → `docs/conventions/stored-flag-vs-owner-truth.md`
  - 🛑 **`truncate` ที่ใส่ถูกแล้วไม่ทำงาน เพราะ flex ตัดสิน wrap จากขนาดเนื้อหาเต็ม *ก่อน* หด** — `.card-header` ของ Paces เป็น `flex-wrap` หัวเธรดจึงตกบรรทัดเมื่อชื่อลูกค้า/ชื่อเพจยาว ทั้งที่ `min-w-0` ครบสายและชิปมี truncate ในตัว ต้อง `flex-nowrap` ก่อนกลไกเดิมถึงได้ทำงาน + เพดานความกว้างชิปไล่ตาม breakpoint (`max-w-28`→`lg:max-w-56`) เพราะที่ว่างจริงต่างกันตามปุ่มที่โผล่/หายตามจอ
  - **แถบเครื่องมือแชทล้นขอบจอ — ย่อคำอย่างเดียวแก้ไม่ได้ พิสูจน์ด้วยเลข**: ที่ 320px เหลือ 46px ให้ปุ่มที่บังคับต้องมีข้อความ → `<768px` ยุบ เลือกสินค้า/อิโมจิ/สติกเกอร์/AI เข้าเมนู `⋯`, `≥768px` ปุ่มสร้างออเดอร์หายเอง (`md:hidden`) ทุกปุ่มจึงอยู่ครบเหมือนเดิม · **แผงอิโมจิ/สติกเกอร์ยึด parent ไม่ใช่ portal → ซ่อนปุ่มได้ ห้ามซ่อนกล่องยึด** → `docs/conventions/flex-header-truncation.md`
  - คำ: `ORDER_VOCAB.SERVICE_QUEUE.createLabelShort` `เข้ารับบริการใหม่` → **`งานใหม่`** (มีผล 3 จุดพร้อมกัน: แถบเครื่องมือแชท/FAB/เมนูกดค้าง; ปุ่มได้ `aria-label` คำเต็ม) · ป้ายเลือกคิวงานในฟอร์มออเดอร์ `รับนัดโดย` → **`บริการ`** (user ชี้หน้าจอสั่ง — ux ทักว่าซ้ำกับหัวข้อ "วันเข้าใช้บริการ" เหนือมัน ทางเลือกคือ "คิวงาน") + การ์ดคิวงานยกภาษาการออกแบบจาก `BestSellerStrip` (แผ่นไอคอนบน ไม่ใช่กรอบรูปเทาว่าง เพราะ `ServiceResourceOption` ไม่มี field รูปเลย)
  - docs sync ไปด้วย: 00030 `UX-Copy`/`SDS`/`TestCase` — รวม `nounShort` ที่ค้างจากรอบ 2026-08-05 ซึ่งไม่มีใครสังเกตมา 2 วัน
  - **carry:** browser QA ทั้ง 5 อาการ (user ตรวจเอง) · ยังไม่มีเทสผูกกฎ "ร้านไม่ส่งของ → ห้าม SHIPPED" กับ `createOrder`/`updateOrder` โดยตรง (ต้องสกัดเป็นฟังก์ชันบริสุทธิ์ก่อน) · re-design หัวห้องแชทให้ compact (user สั่งต่อท้ายรอบนี้ — อยู่ที่ ux)

- **2026-08-07 (รอบค่ำ): ตำบล/อำเภอ ขาเข้าจาก iShip สลับกันมาตลอด (23 ออเดอร์ prod) + โมดัลสถานะพัสดุที่ชวนกดวน — DEPLOYED PROD** (`70016446` `f751a400`; retro `docs/retro/2026-08-07-iship-inbound-locality-swap-retrospective.md`)
  - 🛑 **`dst_district` แปลคนละอย่างในสองทิศทางของ API เดียวกัน** — ขาออก `create_order` = ตำบล (`dst_amphure`=อำเภอ) · **ขาเข้า `query_orders`/`get_order` = อำเภอ (`dst_area`=ตำบล)**. `parseParcelRow()` เดาว่าสมมาตร → ทุกออเดอร์ที่ **ผูกพัสดุมาจาก iShip** ถูกบันทึกสลับ. ยืนยันด้วย `GET /api/get_order` ของพัสดุจริง 2 ใบ (`TH066536981258` → `dst_district=กาญจนดิษฐ์` อำเภอ, `dst_area=ช้างซ้าย` ตำบล) เทียบไฟล์ที่อยู่ที่ iShip กำหนดให้ใช้. กติกาใหม่: ตัดสินจาก **"มี `dst_area` ไหม"** (มี = รูปขาเข้า · ไม่มี = รูปขาออก) ไม่ใช่ชื่อฟิลด์เดี่ยว ๆ
  - 🛑 **error ไปโผล่คนละหน้าจอคนละสัปดาห์กับต้นเหตุ** — ใบที่ user รายงาน (08-07) ไม่ได้ผูกจาก iShip เลย แต่หยิบ **ที่อยู่ลูกค้าเก่า** มาจากใบ 08-04 ที่ผูกมา → ความเพี้ยนเดินทางข้ามออเดอร์ผ่านฟีเจอร์ "ลูกค้าเก่า" · **ไม่มีชั้นไหนฟ้อง** เพราะค่าที่สลับยังเป็นสตริงไทยที่ดูถูกต้อง (`tsc`/Valibot/หน้าจอผ่านหมด) ทั้งที่ไฟล์ชุดคู่ที่ถูกต้องอยู่ในรีโปเราเอง
  - 🛑 **หัวไฟล์ `unlinked.ts` เขียนยอมรับเองว่าเดา แล้วรับความเสี่ยงด้วย "ร้านต้องดูตารางเทียบก่อนกดผูก"** — ผ่านไป 23 ใบไม่มีใครทักสักใบ **การรู้ตัวว่าเดา + คอมเมนต์กำกับ ไม่ได้ลดความเสียหาย** มันแค่ทำให้สืบสวนเร็วขึ้น · unit test ที่มีอยู่แต่งค่าเองตามข้อสันนิษฐานเดียวกับโค้ด จึงเขียวตลอด
  - **backfill 26 แถวบน prod** (23 สลับคืน + 3 ที่เขียน "อ.เมือง" ซึ่งไฟล์ของ iShip ไม่มีเลยสักแถว มีแต่ "เมืองสมุทรปราการ") ตรวจทุกค่ากับไฟล์ก่อนเขียนและตรวจซ้ำหลังเขียน → 73/73 ตรงแล้ว. **วิธีสแกนทั้งคลาสในนาทีเดียว:** เช็คคู่ปกติมีในไฟล์ไหม → ถ้าไม่มีเช็คคู่สลับ → ที่เหลือดูด้วยตา
  - **โมดัลสถานะพัสดุ (FAILED) re-design** ผ่าน ux gate + impeccable critique (dual-agent): ปุ่มทึบเหลือใบเดียวและผูกกับชนิด error ผ่าน `classifyRetryUX()` (อยู่ไฟล์เดียวกับ `IShipError.retryable`) — เครดิตไม่พอ→ปุ่มเติมเงิน · ที่อยู่/ขนส่งไม่ผ่าน→ปุ่มแก้ข้อมูล(+แสดงที่อยู่ให้ดูตรงนั้น) · อื่น ๆ→ลองใหม่ · ข้อความท้ายจอผูกกับชนิด error ด้วย ("กดลองใหม่ได้เรื่อย ๆ" ที่เขียนเหมือนกันหมด = คำเชิญให้กดสิ่งที่ไม่มีวันผ่าน) · ปุ่มรองเป็น ghost ไม่ใช่ outline (outline หน้าตาเหมือนปุ่ม "ปิด" ของเปลือกโมดัลเป๊ะ) · `COURIER_UNAVAILABLE` ย้ายไป fix-data ทั้งที่ `retryable=true` (สองค่าตอบคนละคำถาม)
  - 🛑 **`hasIshipShipment={Boolean(shipment)}` ใน `orders/[token]/page.tsx` นับใบ FAILED ด้วย → `ShipmentEntryModal` ซ่อนตัวเลือก "ส่งเอง" ทั้งแถบ** = ร้านที่เปิดพัสดุกับ iShip ไม่ได้ กรอกเลขพัสดุที่ไปส่งเองมาก็ไม่ได้ ทั้งที่ของส่งไปแล้ว — นิยามที่ถูกอยู่ห่างไป 30 บรรทัดในไฟล์เดียวกัน (คลาสเดียวกับที่ปิดไป 08-06 แต่รอดมาที่ call site นี้ **ยังไม่ได้ไล่ทั้งระบบ**)
  - **carry:** ยังไม่มีชั้นไหนตรวจว่าที่อยู่ที่กำลังบันทึกมีอยู่จริงในไฟล์ของ iShip (กันได้แค่ทางเข้าที่ผ่าน picker) · browser QA E8-6/7/9/10/11 (user ตรวจเอง) · เทสแดง 18 ข้อในเวิร์กทรี (`activity.service`/`chat-*`/`auto-reply` — prisma mock ไม่ครบ) เป็นหนี้ที่มีอยู่ก่อนรอบนี้

- **2026-08-07 (feature 00035): ตัวจัดหน้าร้าน (Shop Page Builder) — DEPLOYED PROD 4 รอบ** (`b8972146` ฟีเจอร์ → `c81a3c34` แก้คอลัมน์ไม่ยืด → `bd7c14c8` รื้อ canvas เลิก iframe → `97d3267e` ตัดพรีวิว+canvas ตรงหน้าจริง; docs ครบ 7/7 ที่ `docs/20 - Features/00035 - Shop Page Builder/` · retro `docs/retro/2026-08-07-feature-00035-shop-page-builder-retrospective.md` · critique `.impeccable/critique/2026-08-07T08-50-10Z__*.md`)
  - **ผู้ขายจัดหน้าร้านสาธารณะเองได้** ที่ `/seller/public-profile/builder` (`(fullscreen)`, **desktop-only** — ต่ำกว่า `xl` เห็นข้อความให้ไปใช้บนคอม แต่ `/public-profile` เดิมยังทำได้ครบบนมือถือ) · 2 คอลัมน์: คลัง 30% + canvas 70%
  - 🛑 **จัดได้ 2 อย่างเท่านั้น: ลำดับแท็บ (`ShopPageLayout.tabOrder` 7 คีย์) + บล็อกเหนือแถบแท็บ (`ShopPageBlock`: เหรียญตราเด่น 1 บล็อก ≤4 ใบ + โพสต์ Facebook รายโพสต์)** — **คงโครงแท็บเดิม ไม่รื้อ `ProfileTabs`** (user เคาะ; ทีมเสนอ single-column feed แล้วถูกปฏิเสธ) · "บล็อกโครงหน้า" (ห้องพัก/ปฏิทิน/บริการ/สินค้า) **ไม่มีตัวแทนใน DB** ยังเป็นแท็บอัตโนมัติ ปรับได้แค่ตำแหน่ง
  - 🛑 **trust guardrail บังคับในระดับโค้ด ไม่ใช่แค่ไม่มีปุ่ม** — `applyTabOrder()` (`src/lib/profile-tab-keys.ts`) **คืนคีย์ครบเท่าที่รับเข้ามาเสมอ** ไม่ว่า `tabOrder` จะเป็นอะไร (พิสูจน์ 1,792 เคส ไม่มีแท็บหาย) · หัวโปรไฟล์ตรึงบนสุด ไม่มีที่จับลาก · ไม่มี UI ซ่อนคะแนน/ป้ายยืนยัน/ยอดออเดอร์/รีวิวรายส่วน
  - **สิทธิ์ OWNER + `ShopMember(role='ADMIN')`** enforce ทั้ง API และหน้าจอ · **mirror รูปโพสต์ตอนกดเพิ่ม** (`FacebookPost.mirroredFileId`, reuse `mirrorRemoteImage()` — `thumbnailUrl` ของ Meta หมดอายุได้)
  - 🛑 **canvas เคยเป็น `iframe` ของหน้าร้านจริงแล้ว "ตายสนิทบน prod ตั้งแต่ deploy แรก"** — `postMessage` ไม่เคยส่ง rect กลับมาสักครั้ง (สงสัย session แยกตาม subdomain) โดย `tsc`/`build` ผ่านหมดและไม่มี error สักตัว · **รื้อเป็น Paces-native แล้ว** ลากจากคลังมาวางได้จริง (พิสูจน์ด้วย CDP input จริง: 0→1→2 บล็อก, สลับลำดับได้) · **แลกด้วย canvas เป็นภาพแทนที่ sync มือกับ `ProfileHero.tsx` — เพี้ยนได้ทุกครั้งที่หน้าร้านเปลี่ยน** ดู `docs/conventions/cross-context-features.md`
  - **`.card` ของ Paces เป็น `height:fit-content` ชนะ `items-stretch`** → 3 คอลัมน์คำนวณได้ 4309/230/411px จน iframe ยุบเหลือ 150px · ต้องใส่ `h-full` ที่การ์ดทุกใบในแถวที่ต้องสูงเท่ากัน (memory `feedback_paces_card_hfit_vs_hfull` มีอยู่ก่อนแล้วแต่ไม่มีใครเอะใจ)
  - **carry:** TestCase 136 เคสยังไม่เคยรันสักเคส · ยืนยันการลากบนโค้ดชุดล่าสุดยังไม่ได้กด · canvas sync มือกับ `ProfileHero` (ควรอ่านจาก SSOT เดียวกัน) · scope baseline

- **2026-08-08 (feature 00016 ส่วนขยาย): ระบบต้นทุนสินค้า — DEPLOYED PROD 3 รอบ** (`6ad9c98f` ฟีเจอร์ → `a247cf0a` ปิด critique P0/P1 → `b51afb48` a11y+ตัวกรอง; เอกสารต่อท้ายใน 00016 · retro `docs/retro/2026-08-08-product-cost-00016-ext-retrospective.md` · critique `.impeccable/critique/2026-08-08T04-34-29Z__*.md` 24/40)
  - 🛑 **user สั่ง "พัฒนาระบบต้นทุนสินค้า" แต่ทั้ง 3 ข้อที่ขอมีอยู่ครบบน prod แล้วตั้งแต่ 00016 — แค่ถูกล็อกหลัง Business Package ฿159/เดือน** ร้านทั่วไปจึงเห็นช่องราคาทุนเป็นสีเทาและเข้า `/expenses` ไม่ได้ = ดูเหมือนไม่มีฟีเจอร์. **บทเรียน: คำขอที่ฟังเหมือนฟีเจอร์ใหม่ อาจแปลว่า "ของที่มีอยู่ถูก gate ไว้" — สำรวจก่อนสร้างเสมอ** ถ้าเริ่ม implement ตามคำสั่งตรง ๆ จะได้ฟีเจอร์ซ้ำทั้งก้อน
  - **D-EXT-1 เปิดฟรีทั้งหมด** (กลับทิศจาก D-4/D-8/D-9 เดิม — Controller ทักท้วงว่าเสียจุดขายหลักของแพ็กเกจ user ยืนยัน) · **D-EXT-2 สิทธิ์ staff ต่อ `Product.cost` ยังไม่แก้** → **KG-EXT-01** ความเสี่ยงขยายจาก "เฉพาะร้านที่จ่ายเงิน" เป็น "ทุกร้านที่มี staff" · **D-EXT-3** เพิ่มกำไรรายออเดอร์ / มาร์จิ้นในรายการสินค้า / CSV `cost`
  - **ไม่มี migration เลย** — `Product.cost`/`OrderItem.cost` มีตั้งแต่ `20260708000000` · `resolveExpenseAccess()` เหลือ 3 variant · **`getSubscriptionStatus()` ที่เหลือ 8 จุดเป็นของฟีเจอร์อื่น ห้ามแตะ**
  - 🛑 **P0 จาก critique: การ์ดเรียกตัวเองว่า "กำไร" โดยคำนวณ `totalAmount − COGS` ซึ่งรวม VAT และไม่หักค่าใช้จ่าย ขณะที่ `NET_PROFIT_FORMULA` นิยามไว้แล้วในไฟล์เดียวกับที่การ์ดนั้น import `formatBaht` มา** — ไม่มี gate อัตโนมัติใดจับได้ (ดู Hard Rule 16) แก้เป็น "กำไรขั้นต้น" + เพิ่ม `GROSS_PROFIT_FORMULA` วางติดกัน
  - **`src/lib/order-profit.ts` = SSOT สูตรกำไรรายใบ + มาร์จิ้น** (ฟังก์ชันบริสุทธิ์) — `cost=null` ถูก **ข้าม** ไม่ใช่นับเป็น 0 เหมือน `pnl.service.ts` ผลคือกำไรที่ได้เป็น **เพดานบน** ต้องมีป้ายกำกับเสมอ · `isMissingCost()` ใน `products/components/data.ts` = SSOT ของ "ยังไม่ตั้งต้นทุน" ใช้ 4 จุด · **`src/lib/order-profit-presentation.ts` = SSOT ของ *คำ/สี/ไอคอน* ของกำไร** (สกัดออกมาเมื่อ `b08be66c` ตอนหน้ารายการออเดอร์ต้องใช้ชุดเดียวกัน) — เพิ่ม surface ใหม่ที่พูดถึงกำไรต้องเรียกตัวนี้ ห้ามก็อปคำไปเขียนซ้ำ (HR16)
  - **`src/services/expense-access.service.test.ts` มี 2 เคส `[blocker]`** — พิสูจน์ด้วย mutation แล้วว่าจับ regression ได้จริง (ลบบรรทัด guard → แดง 1 ข้อ, คืน → เขียว 6) **แดงเมื่อไหร่ห้าม merge**
  - **หนี้ contrast 11 จุดที่อยู่มาหลายเดือน** แย่สุด 1.58:1 — แก้ 9 จุดแล้ว (`057ee830`) · ที่เหลือคือ `--color-primary` ที่ใช้ 2 บทบาท (`bg-primary` 209 ไฟล์) ต้องแยก token ระดับ design system
  - **`API.md` + `UX-Design-Spec.md` ของ 00016 เสียหายทั้งไฟล์บน main มา 6 วัน** (ทุกบรรทัดถูกเติมแถวตารางนำหน้า heading = 0) กู้แล้ว `c177f5a5` — **ไม่มีชั้นไหนในระบบตรวจไฟล์ `.md` เลย** ยังไม่ได้สแกนไฟล์อื่นทั้งรีโป
  - **carry:** browser QA (user ตรวจเอง) · E2E · TestCase TC-EXT-01..13 · ตำแหน่งการ์ดกำไรบนมือถืออยู่ท้ายสุดใต้การ์ดอื่น 5 ใบ · สแกน `.md` ทั้งรีโปหาไฟล์ที่เสียแบบเดียวกัน

- **2026-08-08: แจ้งเตือนเข้าแอปผู้ขาย — ปลายทางถูกร้าน + บอกช่องทาง/เพจ** (docs `docs/20 - Features/00018 …/EXTENSIONS-2026-08-08.md`; ฝั่งเว็บ `90c1dc43` `3ca3ba57` + รอบนี้ · ฝั่งแอป `bb1f27f` ใน deep-mobile-seller)
  - 🛑 **payload ของ push ไม่มี `shopId` มีแต่ `url: /inbox/{conversationId}`** ขณะที่ ownership guard ของหน้าเธรด scope ด้วย `activeShopId` → ผู้ขาย 2 ร้านที่ผูกคนละเพจ กด noti ของร้าน B ตอน active ร้าน A แล้วขึ้น "ไม่เจอช่องแชทนี้" ทั้งที่มีสิทธิ์เต็ม. **แก้ฝั่ง server ไม่ใช่เติม payload** — `findConversationShopForUser()` (scope สิทธิ์ใน `WHERE` ไม่ใช่ดึงมาเทียบทีหลัง) แล้ว `ChatShopAutoSwitch` สลับให้ + `?switched=1` กันวน (ห้ามเอาออก) · เหตุผลที่ไม่เติม `shopId`: ต้อง build + ส่ง App Store ใหม่ และเครื่องที่ไม่อัปเดตจะพังตลอดไป
  - 🛑 **`useShopSwitcher` ไม่มี error state ให้อ่าน — "เคยเป็น `switching=true` แล้วกลับเป็น false" = ล้มเหลวเสมอ** (สำเร็จจะค้าง true จน unload) เดิม hardcode `show={true}` ผู้ใช้ติดหน้าสปินเนอร์ออกไปไหนไม่ได้เลยเมื่อสลับล้ม
  - **perf ~3.8 วิ → ~1.6 วิ**: `minOverlayMs` option (800 แทน default 3000 — 3000 มีไว้กันจอกระพริบตอน "กดสลับร้านเอง" แต่เส้นทางนี้ผู้ใช้แค่อยากอ่านข้อความ) + ย้าย `conversation.findFirst` ขึ้นก่อน `Promise.all` (ตัด 5 query ที่เสียเปล่า)
  - 🛑 **ฝั่งแอป: ต้องอ่าน launch notification response *หลัง* เช็ค OTA จบเท่านั้น** — `takeLaunchNotificationResponse()` ล้างค่า native ทิ้งหลังอ่าน ถ้าอ่านตอน mount แล้ว `Updates.reloadAsync()` ทำงาน คิวใน JS หายและค่า native ก็ถูกล้างไปแล้ว = ปลายทางหายทั้งสองทาง
  - **`src/lib/chat-channel.ts` (ใหม่) = SSOT ของ "ชื่อช่องทาง"** (`Deep`/`Messenger`/`Instagram`) — แยกจาก `CHANNEL_DISPLAY` ใน `ChannelBadge.tsx` เพราะไฟล์นั้น `'use client'` + import Icon wrapper ลากเข้า bundle ของ webhook route ไม่ได้ แต่พิมพ์คำเองก็ผิด **Hard Rule 16** (คำต่างกันสองหน้าจอ ไม่มี tsc/build ตัวไหนฟ้อง เพราะเป็นสตริงที่ถูกทั้งคู่)
  - noti เรียง **`[ชื่อเพจ]` → `[ชื่อคนส่ง]` → `[ข้อความ]`** (user เขียนโครงมาให้ตรง ๆ) = `title` / `subtitle` / `body` ผ่าน **`subtitle` ของ Expo Push (iOS-only — ยืนยันจากเอกสาร Expo)** · ชื่อเพจอยู่ `title` เพราะเป็นบรรทัดเด่นสุด ผู้ขายหลายเพจต้องรู้ "ใบนี้ของเพจไหน" ก่อนสนใจว่าใครทัก (เพจ = กล่องงาน ชื่อลูกค้า = รายละเอียดข้างใน) · เธรด `DEEP` ไม่มีเพจ → `title` ถอยไปใช้ชื่อช่องทาง (**ห้ามว่าง** ไม่งั้น iOS ดันบรรทัดอื่นขึ้นแทน) · **ไม่ใส่ชื่อร้านคู่** (ชื่อเพจแยกได้ครบทุกกรณีที่มีเพจ และ `ShopChannel.name` ≠ `Shop.shopName` ตั้งคนละที่ ใช้แทนกันไม่ได้)
  - 🛑 **ลำดับนี้แก้ 3 รอบในวันเดียว ทุกรอบหัวหน้าทักจากเครื่องจริง** — (1) ชื่อคนส่งขึ้นก่อน (2) เติม `"{ช่องทาง} · {ชื่อเพจ}"` ลงบรรทัดกลาง → *"ทำไมมันมี Messenger มาด้านหน้า เสียพื้นที่"* `Messenger · ` กิน 12 ตัวอักษรแรกแล้วดันชื่อเพจจนโดนตัด ซึ่ง**หางของชื่อเพจคือที่ที่เพจของร้านเดียวกันต่างกัน** = เอาพื้นที่ไปให้ของที่ซ้ำทุกใบ แล้วตัดตัวระบุที่ต้องการจริงทิ้ง (**คลาสเดียวกับเหตุผลที่เลือก `subtitle` แทน `title` เป๊ะ แค่ย้ายมาเกิดอีกบรรทัด** — เพราะประเมิน "ที่ว่าง" โดยไม่ได้วัด) (3) สลับให้เพจขึ้น `title`. ช่องทางไม่ได้หายไปไหน (ชื่อเพจ FB กับบัญชี IG คนละชื่อกันอยู่แล้ว + `channel` อยู่ใน `data`) · มีเทส `[blocker]` 2 ชั้น: `pageTitle` ห้ามขึ้นต้นด้วยชื่อช่องทาง/ห้ามมี `·` และ `pushNewChatMessage` ต้องวางเพจ-คนส่ง-ข้อความตามลำดับนี้
  - 🛑 **หนี้ Android เปลี่ยนหน้าตาไปพร้อมกับการสลับรอบ 3:** เดิม Android (ไม่มี `subtitle`) เสีย "มาจากเพจไหน" ตอนนี้เสีย **"ใครทัก"** แทน — วันเพิ่มคอลัมน์ `platform` ต้องประกอบข้อความของ Android ขึ้นใหม่ให้มีชื่อคนส่ง ไม่ใช่ก็อปชุด iOS ไปตรง ๆ
  - 🛑 **หนี้ Android:** `PushToken` เก็บแค่ `userId`+`token` ไม่มี `platform` → แยกรูปแบบข้อความรายเครื่องไม่ได้ วันปล่อย Android ต้องเพิ่มคอลัมน์ก่อน — Android ไม่ error แต่ข้อมูลหายเงียบ ๆ (ระหว่างนี้ใส่ `channel`/`channelName` ลง `data` ให้แล้ว แอปหยิบไปใช้เองได้โดยไม่ต้องแก้เว็บซ้ำ)
  - **E1/E2 ไม่มี migration · ไม่มี API route ใหม่ · ไม่ต้อง build/OTA แอป** (push เป็นสิ่งที่ server ส่งไปทั้งก้อน)
  - **E3 สวิตช์เปิด/ปิดแจ้งเตือน "รายร้าน" ที่ `/account`** (หัวหน้าเลือกเอง: "2 ดิ ตั้งค่าทีละร้านได้") — **มี migration + ต้อง OTA แอป**
    - 🛑 **ตารางใหม่ `ShopNotificationPref(userId, shopId)` ไม่ใช่คอลัมน์บน `ShopMember`** เพราะแถว `ShopMember` ถูกสร้าง**เฉพาะร้าน BUSINESS** (`business-shop.service.ts:50` เป็นที่เดียวที่ `shopMember.create`) เจ้าของร้าน PERSONAL ไม่มีแถวเลย → กลุ่มนั้นจะตั้งค่าไม่ได้ทั้งกลุ่มโดยไม่มีอะไรฟ้อง (หน้าจอขึ้นสวิตช์ครบ กดแล้ว "สำเร็จ" แต่ค่าไปไม่ถึงไหน)
    - 🛑 **"ไม่มีแถว = เปิด" (opt-out) ห้ามกลับเป็น opt-in** — ไม่ต้อง backfill ผู้ใช้เดิม และร้านใหม่ได้ noti ทันที; กลับกติกาโดยไม่ backfill = ทุกคนเงียบพร้อมกันโดยไม่มีอะไรบอก. อีกกับดัก: เคยปิดแล้วเปิดกลับ **แถวยังอยู่แต่ค่าเป็น true** ตรรกะที่เช็คแค่ "มีแถวไหม" จะตีเป็นปิดตลอดไป (ผูกเทส `[blocker]` ทั้งสองเคส)
    - 🛑 **กรองที่ `shopAudience()` ไม่ใช่ `pushToUsers()`** — ตัวหลังเป็น primitive ที่ฝั่งผู้ซื้อใช้ด้วย (เหรียญตรา/ประมูล) กรองผิดที่ = ปิดแจ้งเตือน "ร้าน" แล้วพลอยปิด noti ที่ไม่เกี่ยวกับร้าน. query ตัวที่สามอยู่ใน `Promise.all` เดิม → ไม่เพิ่ม round trip
    - วางที่ `/account` ไม่ใช่หน้าตั้งค่าร้าน เพราะหน้าร้านผูกกับ `activeShopId` ผู้ขาย 2 ร้านต้องสลับร้านก่อนถึงจะปิดอีกร้านได้ · ยังถูกหลัก "หน้านี้เป็นของตัวคน" เพราะเป็น *"ฉันอยากรับแจ้งเตือนของร้านไหน"* ไม่ใช่ *"ร้านนี้ตั้งค่าไว้ยังไง"*
    - **สิทธิ์ระดับเครื่อง (ต้อง OTA):** native inject `window.__DEEP_PUSH_PERMISSION__` + dispatch event `deep:push-permission` · `onMessage` allow-list รับ `deep:open-settings` → `Linking.openSettings()` · เว็บอ่านผ่าน `src/lib/native-bridge.ts`. 🛑 ต้อง **dispatch event** ไม่ใช่ตั้งตัวแปรเฉย ๆ (inject เกิดหลัง hydrate) · ต้องส่งสถานะ **แม้ยังไม่มี token** (คนที่ปฏิเสธสิทธิ์คือคนที่ต้องเห็นแถบเตือนที่สุด แต่เป็นคนที่ไม่มี token พอดี) · **`null` ≠ `'denied'`** (เดสก์ท็อปเป็น null เสมอ ตีเป็น "ถูกปิด" จะเตือนผิดทุกคน) · `onMessage` รับข้อความจากทุกหน้าที่ WebView โหลด รวมหน้า OAuth ของ Meta/LINE → allow-list ตาม `type` เท่านั้น
    - ของแถม: `PushToken.platform` (route รับค่านี้มาตั้งแต่วันแรกแต่โยนทิ้ง) — เขียนด้วย `?? undefined` ไม่ใช่ `null` ไม่งั้นแอปเวอร์ชันเก่าที่ไม่ส่งค่าจะลบของดีทิ้งทุก 5 นาที
    - 🛑 จับได้ระหว่างทาง: **`btn-warning` ไม่มีอยู่จริงในธีม Paces** (`theme/paces` มีแค่ `btn-primary`/`btn-light`) — คลาสเดียวกับ `btn-ghost` ที่พลาดใน 00033
  - **carry:** ทดสอบบนเครื่องจริงทั้ง E2/E3 (ขั้นตอนอยู่ที่ E2.9 และ E3.9) · จัดกลุ่ม noti ด้วย `thread-id` ของ iOS ยังไม่ยืนยันว่า Expo เปิดฟิลด์นี้ให้ · Android ยังเสีย "ใครทัก" จนกว่าจะประกอบข้อความแยกตาม `platform`

- **2026-08-08 (รอบค่ำ): เธรดที่ Meta AI ตอบไม่เข้าระบบ — ตัวที่ขาดคือ `standby` ไม่ใช่ `messaging_handovers` — DEPLOYED PROD** (`1d2c199e` → `1809776f` → `2477215b`; retro `docs/retro/2026-08-08-meta-ai-standby-retrospective.md` · docs `docs/20 - Features/00018 …/EXTENSIONS-2026-08-08.md` §"รอบค่ำ" · memory `project_meta_ai_thread_standby`)
  - 🛑 **แก้ถูกชั้นแต่ผิดตัวเมื่อเช้าวันเดียวกัน** — `messaging_handovers` บอกแค่ "สิทธิ์คุมห้องเปลี่ยนมือ" (pass/take/request) **ไม่ได้ขนข้อความมาด้วย** ตัวที่ขนคือ **`standby`** ซึ่ง `page` topic ไม่เคยมีเลยตั้งแต่วันแรก (ฝั่ง `instagram` มีตั้งแต่ 08-04 — นั่นคือเหตุผลที่เคส IG หายแต่ Messenger ไม่หาย). อาการคือ **เธรดเงียบทั้งห้อง ไม่ใช่แค่ข้อความ AI ที่หาย — ข้อความที่ "ลูกค้าพิมพ์เอง" ก็ไม่มา** จนกว่าคนจะกดรับเรื่องต่อ; ที่เห็นเข้ามาช้า ๆ คือ `syncMissingMessagesFromMeta()` ซึ่งยิงเฉพาะตอนมีคนเปิดเธรดนั้น (+throttle 5 นาที) และไม่ขยับรายการแชท = ไม่มีอะไรบอกว่ามีของใหม่
  - 🛑 **`list_topics` ของ Meta DevTools MCP ไม่ครบ — ห้ามใช้ตัดสินว่า field มีจริงไหม** มันไม่ลิสต์ `standby` ใน `page` และ `devtools_webhook_manage` **ปฏิเสธคำสั่งเพิ่ม** (`Fields not available for topic "page"`) ทั้งที่เอกสาร Messenger เขียนตรงตัวว่า subscribe ได้ → ต้องยิง `POST /{app-id}/subscriptions` ผ่าน Graph ตรงด้วย app token (`FB_CHAT_APP_ID|FB_CHAT_APP_SECRET`). **เป็น replace ไม่ใช่ append** — `GET` มาก่อนแล้วส่ง field เดิมครบทุกตัว ตกตัวเดียว = เลิกรับ field นั้นทั้งระบบเงียบ ๆ; ก่อนยิงพิสูจน์ `verify_token` ด้วย handshake ใส่ prod เอง (`?hub.mode=subscribe&hub.verify_token=…&hub.challenge=PING123` ต้องได้ `PING123`)
  - **`rawMessage->>'source'` คือเครื่องมือวินิจฉัยของทั้งเรื่อง** — `webhook` (มี ms) = ของสด · `graph-backfill` (ปัดเป็นวินาที) = webhook ไม่เคยส่ง. หลักฐานชี้ขาดคือเธรด `b6064da8` ที่เส้นแบ่งอยู่ที่ **"ใครถือห้อง" ไม่ใช่ "payload หน้าตายังไง"** (`11:39:06` backfill "You took over…" → `11:39:11.478` webhook กลับมาทันที เพจเดิม เธรดเดิม schema เดิม) — บั๊กฝั่งเราไม่มีทางเลือกพังตามแกนนี้ได้
  - 🛑 **ข้อจำกัดที่พิสูจน์กับ Graph แล้ว ห้ามเสนอสวนทาง:** `take_thread_control` → `(#27) not supported when Conversation Routing is not enabled` · `release_thread_control` → `(#100) Only the current thread owner can release` · `pass_thread_control` → `(#100) calling app is not the thread owner`. **แอปเราไม่เคยเป็นเจ้าของเธรด** แม้ตอนคนกด take over ใน Business Suite (เจ้าของคือ **Page Inbox `263902037430900`**, IG Inbox `1217981644879628`) · **Meta ไม่ยิง `messaging_handovers` ให้การสลับกับ AI ของตัวเอง** (ตาราง `ChatHandoverEvent` ว่างเปล่าแม้ user กด Respond yourself/Turn on AI responses แล้ว) ⇒ **ไม่มีทางรู้ app id ของ Meta AI ⇒ ปุ่ม "เปิด AI กลับ" ทำในแอปเราไม่ได้ ต้องเป็นลิงก์ออกไป Business Suite** · Messenger เลิกใช้ Handover Protocol → **Conversation Routing** จึงไม่แปลกที่ `secondary_receivers` = `(#100) nonexisting field` และ `thread_owner` ไม่มี `app_id`
  - **`ChatMessage.viaStandby`** = SSOT ของ "AI ถือห้องอยู่ไหม" — **derive จากข้อความล่าสุดเสมอ ห้ามเก็บธงแยกบน `Conversation`** (`stored-flag-vs-owner-truth.md`) · ตาราง `ChatHandoverEvent` เป็นตารางหลักฐาน (ไม่ผูก FK) ที่ต้องมีเพราะ **Vercel plan นี้ query runtime log ย้อนหลังไม่ได้** (`/v1/deployments/{id}/runtime-logs` 404, `/v2/.../events` คืนแต่ build log) — `console.log` ที่อ่านย้อนหลังไม่ได้มีค่าเท่ากับไม่มี
  - **UI:** composer replacement block (แทนที่**ทั้งแถบเครื่องมือ+textarea** ไม่ใช่ dim — เพราะ `tokenInvalid` dim = "ระบบพัง รอแก้" ส่วนเคสนี้ = "มีคนอื่นทำงานแทนอยู่" คนละชนิด) · `pacesConfirm` client gate ที่ **ไม่ยิง API** และ copy **ห้ามบอกว่า "AI จะหยุดตอบ"** (สั่งไม่ได้จริง) · `parseMetaAiHandoffNotice()` แปล 4 สตริงเป็นชิปไทย — 🛑 **สตริงต้นทาง query ตรงจากฐาน prod** เพราะตอนออกแบบเดาตัวที่ 4 ว่า "ลูกค้าขอคุยกับคน" ของจริงคือ **"…because your customer is ready to buy"** (คนละความหมายกันคนละเรื่อง); คำแปล "คุณเข้ามาดูแลแชทนี้แทนเอเจนต์ AI" = คำที่ Meta ใช้เองใน Business Suite ไทย ห้ามคิดใหม่
  - **บทเรียนการสื่อสาร:** รายงานผลสืบสวนเรียงตาม *กลไกที่ค้นพบ* (เปิดหัวด้วย error 3 บรรทัดของ Graph) ทำให้หัวหน้าอ่านแล้วสรุปว่า **"ทำอะไรไม่ได้"** ทั้งที่ติดแค่ 1 ใน 6 ข้อ — ต้องเรียงตาม **สิ่งที่ผู้ใช้ขอมาเอง** เป็นตาราง ✅/❌ แล้วค่อยอธิบายข้อจำกัด
  - **carry:** ยังไม่ยืนยันว่า **ส่งข้อความผ่านไหมตอน AI ถือห้อง** (ถ้าไม่ผ่านต้องเพิ่ม rule ใน `src/lib/chat-send-failure.ts` เมื่อรู้ error code จริง ห้ามเดา) · browser QA (user ตรวจเอง) · ไม่มีป้ายบนบับเบิลว่า "Meta AI ตอบ" (ถ้าทำต้องเลี่ยงไอคอน `robot`=DeepBot / `sparkles`=DeepAI) · ทาง B (เปิด Conversation Routing บนเพจ) ยังไม่แตะ

- **2026-08-08 (feature 00038): ตอบกลับคอมเมนต์ (Comment Auto-Reply & Private Reply) — 10 tasks ผ่าน SDD full-review** (`9129f224..ef4a78c2` ฟีเจอร์หลัก → `2ed8ed5f` ถอด GET config ไม่มีใครเรียก; docs 7/7 ที่ `docs/20 - Features/00038 - Comment Auto-Reply/` · retro `docs/retro/2026-08-08-feature-00038-comment-auto-reply-retrospective.md`)
  - สวิตช์ตอบกลับคอมเมนต์ Facebook อัตโนมัติต่อเพจ (สาธารณะ + ส่งข้อความส่วนตัว) + ปุ่มแมนนวล "ทักแชท" ใช้ core เดียวกัน + หน้าประวัติ + สถานะ 3 ชั้น (ยังไม่ตอบ/บอทตอบแล้ว/คนตอบแล้ว) 53 เทสเขียว, tsc 0
  - 🛑 **5 บั๊ก Critical ถูก reviewer จับก่อนขึ้น prod ทั้งที่ tsc/เทส/grep ของ implementer เองผ่านหมด** — ด่านกันซ้ำต้นฟังก์ชันใช้คนละคีย์กับตอนเขียน log (คอมเมนต์ที่สองของคนเดิมลอดยิง Graph ซ้ำ) · ไม่มี error boundary หลัง Graph สำเร็จ (DB ล้ม = สิทธิ์ Meta หายถาวรเงียบ ๆ) · ไม่จับ P2002 + จองแถวหลังยิง Graph · **การกันซ้ำสองชั้นจากคนละ task ฆ่ากันเอง** (Task 4 จองแถว AUTO ก่อน Task 3 เห็นแถวนั้นเองแล้วคืน `ALREADY_SENT` — private auto-reply ไม่ยิงออกสักครั้ง) · SQL `countUnansweredForShops` ขัด AC-CR-25 (โพสต์ที่บอทเคลียร์หมดค้าง badge ตลอดกาล)
  - **บทเรียนหลัก: 4 ใน 5 Critical มีต้นเหตุจากแผนที่ controller เขียนเอง ไม่ใช่จาก implementer** — แผนละเอียดมากไม่ได้แปลว่าถูก แค่ทำให้ผิดได้เร็วขึ้น · **เทสที่ mock เพื่อนบ้านทิ้งทั้งตัวเขียวตลอดไม่ว่าเพื่อนบ้านทำอะไร** — ต้องมีเทสอย่างน้อย 1 ตัวที่ไม่ mock แล้วยืนยัน Graph ถูกเรียกจริง · partial unique index พิสูจน์ถูกที่ชั้น DB (7 เคส) แต่โค้ดใช้ find-then-update จึงไม่เคยมีโอกาสทำงานเป็นตัวกันชน
  - ก่อนเริ่ม Task 2 พบ branch ตามหลัง `origin/main` 37 คอมมิต + 6/7 ไฟล์ถูกแก้ไปแล้วบน main (ชน 00037) — rebase ก่อนเริ่มกันเขียนทับงานคนอื่นได้จริง
  - **carry:** `CardAvatar` ซ้ำกับ `PageAvatar` (ควรรวมเป็น `size='lg'`) · `countUnansweredForShops` batch scope vs badge global scope (fast-follow, ไม่ critical) · **`/impeccable clarify` (critique รันแล้ว 34/40 · แก้ 4 finding ใน `7d64dfaf` — ตัวเด่นคือ action row ไม่มี `flex-wrap` ทั้งที่คอมเมนต์ในโค้ดและ UX spec อ้างว่า wrap ได้)** · browser QA (critique รอบนี้ก็ไม่มี browser inspection) · แท็บ 4 ตัวไม่มี edge fade · 5 minor deferred (`isUniqueConstraintError` string-match, dangling TD ref ใน SRS, `resolveChannelToken` error รายละเอียดหาย, webhook `verb=edited` DB round-trip เกิน, `logs` `postMessage` ไม่ตัดสั้น)

- **2026-08-09: ช่องพิมพ์บล็อกค้าง 18 เธรด + การ์ด carousel จาก Facebook — DEPLOYED PROD 5 คอมมิต** (`4c70dec3` `6aaaa2a6` `be3e6bcb` `2fa68aa1` `a622741d`; docs `docs/20 - Features/00018 …/EXTENSIONS-2026-08-08.md` §"ส่วนขยาย 2026-08-09" · mockup `docs/superpowers/specs/2026-08-09-meta-generic-card-carousel-mockup.html` · memory `project_meta_ai_thread_standby` + `project_fb_generic_card_carousel`)
  - 🛑 **`ChatMessage.viaStandby` ≠ "AI ถือห้อง" — มันแปลว่า "เราไม่ใช่เจ้าของเธรด" ซึ่งจริง *ตลอดเวลา*** (เจ้าของคือ Page Inbox `263902037430900` เสมอ) พอ AI คืนสิทธิ์แล้วคนตอบจาก Business Suite echo ก็ยังวิ่งมาทาง standby ธงจึงค้าง `true` → **UI บล็อกช่องพิมพ์ 18 เธรดพร้อมกันทั้งที่ผู้ขายกำลังคุยกับลูกค้าอยู่**. ที่หลุดเพราะทดสอบเจอแต่เคส **"คนกด take over"** (Page Inbox ยึดสิทธิ์จริง → ธงพลิกตามคาด) ยังไม่เคยเจอเคส **"AI ส่งคืนเอง"** (สิทธิ์ไม่เปลี่ยนมือ → ธงค้าง) — สองเคสหน้าตาเหมือนกันเป๊ะจากฝั่งผู้ใช้ **สัญญาณที่ถูกคือ `readMetaAiControlMarker()`** อ่านจาก marker 4 สตริงที่ Meta ประกาศเอง (คืน `'AI' | 'HUMAN'` ไล่จากล่างขึ้นบน) · **ไม่มี marker = ถือว่าคนคุม ไม่บล็อก** เพราะบล็อกผิด = พิมพ์ไม่ได้ทั้งที่กำลังคุยลูกค้า เสียหายกว่าปล่อยผ่าน
  - 🛑 **`template_type: 'generic'` ไม่ได้แปลว่าเป็นการ์ดสินค้า** — Meta ใช้โครงเดียวกันกับการ์ด**คำขอชำระเงิน** (`฿360.00 order`) และ**ขอโทรกลับ** ต้องมีอย่างน้อย 1 element ที่มี `image_url` ถึงนับ (บน prod: generic ที่ไม่มีรูป 54 ใบไม่ใช่สินค้าสักใบ) ไม่กั้น = การ์ดชำระเงินกลายเป็นกล่องรูปเทาว่างเปล่า **แย่กว่าก่อนทำฟีเจอร์** · 🛑 **ตัวที่จับได้คือ dry-run ของสคริปต์ backfill ไม่ใช่ tsc/build/เทส — และเทสเดิม 2 ข้อ *ยืนยันพฤติกรรมที่เป็นบั๊ก* เขียวมาตลอด** (เทสยืนยันแค่ว่าโค้ดทำตามที่คนเขียนเทสคิด) ⇒ ก่อน backfill ต้อง **ดูตัวอย่างแถวจริงที่จะแตะ ไม่ใช่ดูแค่จำนวน**
  - 🛑 **รูปบน fbcdn หมดอายุ ~4 วัน** (ถอด `oe=<hex>`) → ต้อง mirror ตอน ingest · **ผมตอบ user ผิด 2 รอบว่า "เธรดเก่า backfill ไม่ได้ รูปหมดอายุแล้ว"** โดยเหมารวมจากตัวอย่างใบเดียวไม่ได้นับจากฐาน — ความจริงการ์ดที่มีรูปเพิ่งเริ่มมี 08-08 จึงยังอยู่ในหน้าต่างพอดี **backfill สำเร็จ 12 ข้อความ / 26 รูป** (ล้ม 4 เพราะ URL ตอบ 403 จริง) · เป็นคลาส "ข้ออ้างว่าทำไม่ได้" ที่ไม่มีเทสไหนจับได้เพราะมันแปลว่าไม่มีโค้ดถูกเขียน (HR16 ทิศกลับ)
  - UI: `w-44` ไม่ใช่ `w-52` (จุดบีบ 1280px เนื้อบับเบิลเหลือ ~217px — `w-52` เหลือ peek 9px = มองไม่เห็นว่าเลื่อนได้) · `object-contain` ไม่ใช่ `cover` (รูปมีตัวหนังสือฝังอยู่) · ไม่ render `buttons[]` (postback ของลูกค้า กดแทนไม่ได้) · ห้าม `bg-primary`/`text-primary` ทั้งการ์ด (One Voice — ต่างจาก `ProductCardBubble` ที่กดได้จริง) · 🛑 **`aspect-ratio` ไม่ล็อกความสูงจริงถ้าลูกยังอยู่ใน flow** (img ที่ยังไม่รู้ขนาด/alt text ตอนโหลดไม่ขึ้น ดันกล่องได้) ต้อง `relative` + ลูก `absolute inset-0`
  - **carry:** ยังไม่ยืนยันว่า **ส่งข้อความผ่านไหมตอน AI ถือห้อง** (ค้นทั้งฐานแล้วไม่เคยมีใครลองสักครั้ง — ต้องกดจริง) · browser QA (user ตรวจเอง) · เทสแดง 18 ข้อยังค้าง (prisma mock ไม่ครบใน `activity.service`/`chat-*`/`auto-reply`) ทำให้ทุกการ verify ต้องพิสูจน์ซ้ำว่า "18 ตัวเดิมจริงไหม"

- **2026-08-09 (รอบบ่าย): `/inbox/comments` ยิง `/api/chat/comments/posts` ไม่หยุด — DEPLOYED PROD** (`3abcfba4`; retro `docs/retro/2026-08-09-comment-inbox-fetch-loop-retrospective.md` · convention `docs/conventions/hook-return-identity-in-deps.md`)
  - 🛑 **ต้นเหตุคือ dep array ไม่ใช่ realtime/poll** — `useListBusy()` คืน object literal ใหม่ทุก render แล้ว `CommentsClient` ใส่ทั้งก้อนใน deps ของ effect ที่ยิง fetch → fetch เสร็จ → `setPosts/setCounts` → re-render → อ็อบเจกต์ใหม่ → effect รันใหม่ → fetch อีก เร็วเท่า round-trip (`filterFirstRun` กันได้แค่ mount แรกซึ่งคนละเรื่อง) แก้ด้วยการ dep เฉพาะ `begin` ที่เป็น `useCallback`
  - 🛑 **memo อ็อบเจกต์ที่ hook คืนไม่ใช่ทางแก้** (`busy` ต้องเปลี่ยนอยู่แล้ว = ลูปเดิมแค่ช้าลงเป็นจังหวะ ~350ms จนไม่มีใครสังเกตอีก) · **`react-hooks/exhaustive-deps` แนะนำให้เขียนแบบที่พังเป๊ะ ๆ** gate ทุกตัวเขียวหมด
  - 🛑 **`/orders` ใช้ hook ตัวเดียวกันมา 2 วันไม่พัง เพราะเรียก `run()` จาก event handler อย่างเดียว ไม่เคยมี effect ผูกกับมัน** — หยิบของที่ "พิสูจน์แล้วว่าใช้ได้" ไปใช้ ไม่ได้พาการพิสูจน์ติดไปด้วยถ้าท่าเรียกต่างกัน (คู่กับ `sibling-surface-parity.md` ซึ่งยังถูกอยู่)
  - เทส `[blocker]` ใหม่สแกนทุกไฟล์ที่เรียก `useListBusy()` ห้ามตัวแปรที่รับค่าโผล่ใน dep array (ไม่ hardcode รายชื่อไฟล์) · เป็นเทสที่อ่านซอร์สเพราะ vitest ตั้ง `environment: "node"` และรีโปไม่มี jsdom/testing-library
  - **หมายเหตุเครื่องมือ:** `rg -r` คือ `--replace` ไม่ใช่ recursive — `rg -rn "foo" .` คืนซอร์สที่ทุก match ถูกแทนด้วย `n` โดยไม่มี error ใด ๆ เตือน (เกือบสรุปผิดว่าไฟล์ถูก minify) · เวิร์กทรีนี้ไม่มี `.env.local` → `npm run build` ล้มที่ `NEXTAUTH_SECRET` และ `npm test` (`dotenv -e .env`) รันไม่ได้ ต้องพิสูจน์ด้วย env dummy · `npx vitest run` เปล่า ๆ ดูดไฟล์ `e2e/*.spec.ts` (Playwright) เข้ามาด้วยเพราะ config ไม่มี `exclude` → ต้อง scope `npx vitest run src/`
  - **carry:** browser QA (user ตรวจเอง — ดู Network tab ว่ายิงครั้งเดียวตอนกรอง) · หนี้จาก retro §4 รอบก่อนยังไม่ได้แตะสักข้อ · `exclude: ['e2e/**']` ใน vitest config

- **2026-08-09 (00024): เลือกเวลาคิวงานด้วย "เวลาเริ่ม + ระยะเวลา" — DEPLOYED PROD 4 คอมมิต** (`8ea322a1` ฟีเจอร์+ปิด critique → `93a65f87` 2 บั๊กที่ user เจอบน prod → `caefadf1` a11y+เอกสาร+hook → `fd36226e` ชิป "เต็ม"; ม็อกอัพ `docs/superpowers/specs/2026-08-09-appointment-time-step-mockup.html` · critique 29/40 · retro `docs/retro/2026-08-09-appointment-duration-picker-retrospective.md`)
  - 🛑 **เลิกถามเวลาสิ้นสุดในขั้นเลือกเวลา — ถาม "ใช้เวลา" แทน** `pendingEnd` เป็น `useMemo` ที่ derive จาก `start + duration` → start/end หลุด sync กันไม่ได้อีก ซึ่ง desync คือรูปร่างของบั๊ก `endTouched` ทั้งดุ้น (ตั้ง `true` ตอนเปิดถ้าฟอร์มเคยมี `endTime` แล้ว auto-fill ตายทั้งชีต). ต้นเหตุที่ user เห็นคือ **iOS เปิดวงล้อ `<input type="time">` ที่เวลาปัจจุบันเสมอเมื่อช่องว่าง** — แค่แตะดูก็ได้ค่าที่แทบไม่มีทางถูก และมันเป็น native picker ตัวเดียวในจอ. SSOT ใหม่ใน `src/lib/appointments.ts`: `resolveInitialDuration` / `minutesBetweenTimes` / `formatDurationTH` / `nextShowAllHours` (เทส `[blocker]` 5 ตัว พิสูจน์ด้วย mutation ทุกตัว)
  - 🛑 **`formatDurationTH` = SSOT ของ *คำเรียก* ระยะเวลาทั้งระบบ (HR16)** — `ResourceList`/`PublicServiceList` เคยเขียน `${นาที} นาที` ดิบเอง จะอ่านว่า "90 นาที" ขณะที่จอใหม่อ่านว่า "1 ชม. 30 นาที" เลขเดียวกันคนละคำคนละหน้าจอ ไม่มี gate ไหนฟ้อง
  - 🛑 **จุด `bg-warning` 6px สื่อคนละความหมายในสองที่มาตลอด** — ปฏิทิน `used > 0` (มีคิวบ้าง) · ชิปเวลา `overlap >= capacity` (**เต็ม**) และ `aria-label` ของชิปยังเขียนว่า "มีคิวแล้ว" ทั้งที่เงื่อนไขคือเต็ม. แก้โดย **ย้ายตัวแบกความหมายจาก "จุด" ไปที่ "สีตัวหนังสือ"** (จุดเป็น non-text ต้องการ 3:1 ได้ 1.55:1 · `text-warning-ink` บนพื้น `warning/15` วัดด้วยเกณฑ์ข้อความ 4.5:1 ได้ 6.20–6.56:1) + ไอคอนสามเหลี่ยมเป็นตัวบอกด้วยรูปร่างตาม WCAG 1.4.1 — ปฏิทินสื่อ "เต็ม" ด้วยกากบาทแทนเลขวันอยู่แล้ว ชิปแค่ไม่เคยพูดตาม
  - 🛑 **BR-RSV-18: "เต็มแล้ว" เป็นคำเตือนที่ยังกดยืนยันได้ ไม่ใช่ตัวบล็อก** — ไฟล์อ้างกฎนี้ 4 ครั้งเพื่ออธิบายว่าทำไมไม่ disable ชิปที่ชนคิว แล้วเอาเลขเดียวกันไป disable **ปุ่มยืนยัน** ซึ่งเป็นด่านจริง (พนักงานอีกคนยกเลิกนัดขณะชีตเปิดค้าง = ช่องว่างจริงแต่กดไม่ได้ถาวร) ตอนนี้แยกธง `blocking` ออกจาก `invalid`
  - **บทเรียนที่เจ็บที่สุด:** ผมประกาศเองตอน merge ว่า "ยังไม่เคยเปิดดูจริงสักครั้ง" แล้ว push — บั๊ก 2 ตัวที่ตามมาเห็นได้ใน 5 วินาทีแรกที่เปิดหน้าจอ. **การรู้ตัวว่าไม่ได้ตรวจ + เขียนกำกับไว้ ไม่ได้ลดความเสียหาย** มันแค่ทำให้สืบสวนเร็วขึ้น (คลาสเดียวกับ `unlinked.ts` ที่ยอมรับเองว่าเดาแล้วปล่อยไป 23 ใบ)
  - **carry:** browser QA ทั้งหมด (user ตรวจเอง) · เวลาทำการต่อคิวงาน (ต้อง migration — user เคาะว่ายังไม่ทำ) · ความละเอียดเวลาเริ่ม 60 นาที (ร้าน 25/45 นาทีต้องตกไป "กำหนดเอง" ซึ่งถามเวลาสิ้นสุด = ทางหนีจากโมเดลใหม่คือโมเดลเก่า) · **นัดข้ามคืนทำไม่ได้** (ร้านบาร์/กะดึก — ต้องแตะ service layer) · ขอบชิป 1.14:1 / วงแหวน "วันนี้" 1.22:1 · 🛑 **เทสแดง 98 ข้อ / 30 ไฟล์บน `main`** (CLAUDE.md เคยบันทึก 18 → โต 5 เท่า) ยืนยันแล้วว่าไม่ได้มาจากรอบนี้ แต่แปลว่า **เทสทั้งชุดใช้เป็นสัญญาณไม่ได้แล้ว** ต้องรันเทียบ 2 ครั้งเพื่อพิสูจน์ว่าไม่ได้ทำพัง

- **2026-08-10: "อัปโหลดคลิปเกิน 1 นาทีไม่ได้" — ต้นเหตุคือเพดาน 4.5MB ของ Vercel ที่ไม่มีใครรู้ว่ามี** (convention `docs/conventions/upload-body-size-limit.md` · SRS NFR-1.3 + §7.9 sync แล้ว)
  - 🛑 **ทุก upload ของระบบวิ่งผ่าน body ของ function → ตันที่ 4.5MB ทุกจุด** ขณะที่โค้ดเขียนเพดานไว้ 4 ชุด (5MB `validateUpload` · 25MB `ATTACHMENT_MAX_SIZE` · 10MB รูปสินค้า/ห้องพัก · 5MB สลิป/เอกสาร) **ไม่มีชุดไหนมีผลจริง** — ร้านที่แจ้งเข้ามาเดาว่าเป็นเรื่อง "ความยาวคลิป" เพราะคลิปสั้นบางอันเล็กพอผ่าน. 413 ของแพลตฟอร์มตอบด้วย body ที่ไม่ใช่ JSON → `res.json()` พัง → ตกไปข้อความ fallback "ลองใหม่อีกครั้ง" = **ผู้ใช้ถูกเชิญให้กดวนสิ่งที่ไม่มีทางสำเร็จ**
  - **แก้เป็น direct upload:** `POST /api/uploads/ticket` → client `PUT` เข้า storage ตรง → `POST /api/uploads/commit` (อ่านขนาดจริงด้วย HEAD, ไม่ผ่าน = **ลบไฟล์ทิ้ง** + 413) · ย้าย call site ทั้ง **17 จุด** ไป `@/lib/upload-client` · `/api/upload` + `/api/chat/upload` เหลือเป็น legacy ที่ไม่มีใครเรียก
  - **พิสูจน์กับ bucket prod ก่อนเขียนโค้ด (spike):** presigned PUT 1MB → 200 · **30MB → `413 EntityTooLarge` และไฟล์ไม่ถูกเขียนลง bucket เลย** (`file_size_limit` บังคับจริงบนเส้นทาง S3) · OPTIONS preflight ได้ `allow-origin: *` + PUT → เบราว์เซอร์ยิงข้ามโดเมนได้ ไม่ต้องตั้ง CORS · ไฟล์ probe ลบทิ้งแล้วทั้งคู่
  - 🛑 **กับดักที่เกือบพลาดเอง 2 ข้อ:** (1) `guardApi` จำกัด mutation **30/นาที** ต่อผู้ใช้ล็อกอิน แต่ flow ใหม่ยิง 2 request ต่อไฟล์ และแนบรูปกริดได้ 24 ใบ = 48 → ต้องเพิ่ม bucket `/api/uploads/*` เพดาน 300 ไม่งั้นโดน 429 กลางชุด **บางใบขึ้นบางใบไม่ขึ้น** (2) commit **ลบไฟล์** ที่ไม่ผ่าน ถ้า claim ไม่ผูก `fileId` กับ `userId` จะกลายเป็นช่องลบไฟล์ร้านอื่น — "uuid เดายาก" ไม่ใช่ authorization
  - **เพดาน 25MB ของ Meta ยกไม่ได้** — คลิป iPhone 1 นาที (40–90MB) ยังส่งเข้า Messenger/IG ไม่ได้ ตอนนี้แก้ด้วย *ข้อความที่บอกทางออก* (`oversizeMessage` = SSOT ของคำพูดนี้ วิดีโอได้คำแนะนำ 720p/ตัดคลิป ไฟล์อื่นไม่ได้) ยังไม่ทำ: บีบอัดในเบราว์เซอร์ · ส่งเป็นลิงก์แทนไฟล์แนบ
  - เทส `[blocker]` 3 ไฟล์ (22 เคส) พิสูจน์ด้วย mutation 6 แบบครบทุกตัว — ตัวสำคัญคือ `upload-no-multipart-callers.test.ts` ที่ **สแกนซอร์สทั้ง `src/`** ห้ามใครกลับไป `fetch('/api/upload')` (แพตเทิร์นเดิมคือของที่ก็อปง่ายที่สุดเพราะทุกหน้าเขียนไว้อย่างนั้นมาตลอด)
  - **carry:** ยังไม่ได้รัน `next build` (จะทับ `.next` ของ dev server ที่ user อาจเปิดอยู่) · browser QA ทุก surface ที่แตะ (17 จุด — user ตรวจเอง) · `/api/app/upload` ของแอปมือถือยังตัน 4.5MB (เปลี่ยน contract ต้องปล่อยแอปใหม่) · `POST /api/orders/[token]/slip` + badge ของ admin ยังรับ multipart · ไฟล์ที่ commit ไม่สำเร็จ (ผู้ใช้ปิดจอกลางทาง) ค้างใน bucket โดยไม่มีตัวเก็บกวาด
  - หมายเหตุ: หน้า `VerificationMobile.tsx` มี finding ของ impeccable hook ค้างอยู่ 9–11 ข้อ (`design-system-font-size` ที่ `text-[13px]/[20px]` ฯลฯ) — **ของเดิมก่อนรอบนี้ทั้งหมด** ไม่ได้แก้เพราะอยู่นอกขอบเขต (รอบนี้แตะแค่ตรรกะอัปโหลด ไม่ได้แตะ typography)

Safety checkpoint: `git checkout pre-paces-wipe` restores the pre-2026-04-13 state.

@AGENTS.md
