/**
 * ขนาดหน้าของรายการคอมเมนต์ใน `/inbox/comments` — ตัวเลขเดียวที่ทั้งฝั่งดึงและฝั่งตัดสิน
 * "ยังมีอีกไหม" ต้องใช้ร่วมกัน (Hard Rule 16)
 *
 * 🛑 ทำไมต้องแยกออกมาเป็นไฟล์กลาง ไม่ประกาศไว้ในเซอร์วิส
 * `CommentsClient.tsx` เป็น client component — import จาก `page-comment.service.ts` จะลาก Prisma
 * เข้า bundle ของเบราว์เซอร์ ก่อนหน้านี้จึง hardcode เลขไว้คนละที่ (เซอร์วิส 30 / client 25)
 * แล้ว `hasMore` ตัดสินผิดเงียบ ๆ: หน้าที่ได้มา 27 แถวคือหน้าสุดท้ายแล้ว แต่ยังขึ้นปุ่ม
 * "โหลดเพิ่ม" ให้กด แล้วกดไปได้ศูนย์แถว — ไม่มี `tsc`/เทสตัวไหนเห็น เพราะเลขทั้งสองตัว "ถูก"
 * ในตัวเอง สิ่งที่ผิดคือมันควรเป็นเลขเดียวกัน
 */
export const COMMENT_LIST_PAGE_SIZE = 25
