/**
 * format-money.ts — SSOT ของการแสดง "จำนวนเงินบาท" ฝั่ง seller/admin
 *
 * ก่อนหน้านี้ feature 00016 มี 3 นโยบายพร้อมกันในฟีเจอร์เดียว:
 *   /expenses        Intl {2,2}                     → ฿1,123.70
 *   /sales           Intl currency {max:0}          → ฿1,124
 *   การ์ด/ชีตยอดขาย   n.toLocaleString('th-TH')       → ฿1,123.7   ← ตัดศูนย์ท้ายทิ้ง อ่านไม่ออก
 * เงินก้อนเดียวกันจึงแสดงไม่เหมือนกันสามแบบในสามหน้า (พบตอน user ใช้จริงบน prod 2026-08-02)
 *
 * นโยบายเดียวจากนี้ไป: **ทศนิยมโผล่เฉพาะเมื่อมีสตางค์จริง** → `฿3,680` และ `฿1,123.70`
 * (ไม่มี `฿3,680.00` ที่เป็น noise และไม่มี `฿1,123.7` ที่เป็นบั๊ก)
 *
 * และห้าม render `฿-1,123.7` — เครื่องหมายลบชนสัญลักษณ์สกุลเงินบังคับให้ผู้อ่านถอดรหัสก่อนเข้าใจ
 * ทิศทางเป็นหน้าที่ของ **คำ + สี** ไม่ใช่เครื่องหมาย (PRODUCT.md §Users: digital-literacy ต่ำ/ผู้สูงวัย)
 */
const GROUP_0 = new Intl.NumberFormat('th-TH', { minimumFractionDigits: 0, maximumFractionDigits: 0 })
const GROUP_2 = new Intl.NumberFormat('th-TH', { minimumFractionDigits: 2, maximumFractionDigits: 2 })

const hasSatang = (n: number) => Math.round(n * 100) % 100 !== 0

/** ขนาดของจำนวนเงิน — คืนค่าสัมบูรณ์เสมอ ไม่มีเครื่องหมาย (ทิศทาง = หน้าที่ของ label + สี) */
export const formatBaht = (n: number): string => {
  const abs = Math.abs(n)
  return '฿' + (hasSatang(abs) ? GROUP_2 : GROUP_0).format(abs)
}

/**
 * ตัวเลขเงินบาทแบบ **ไม่มี ฿ และคงเครื่องหมายลบไว้** — `7,200` / `-1,623.70`
 *
 * ต่างจาก formatBaht 2 อย่าง: (ก) ไม่มีสัญลักษณ์สกุลเงิน (ข) ไม่ทำ Math.abs
 * เหตุผลของนโยบาย "ห้าม ฿-1,123.7" คือ *เครื่องหมายลบชนสัญลักษณ์สกุลเงิน* — พอไม่มี ฿ ให้ชน
 * เหตุผลนั้นก็หมดไป จึงปล่อยเครื่องหมายลบให้ทำหน้าที่บอกทิศทางได้ตรง ๆ (user สั่ง 2026-08-04:
 * "ถ้าติดลบ ก็ -7,200 ไปเลย" + "ไม่ต้อง ฿ มาก็ได้ เพราะเราขายคนไทย")
 *
 * SCOPE: การ์ดยอดขาย command center + ชีตยอดขายเต็มจอ เท่านั้น
 * ห้ามลากไปใช้ที่ /expenses, /sales, ตารางบิล ฯลฯ — ที่นั่นยังต้องมี ฿ ตามเดิม (คนละบริบท:
 * หน้าพวกนั้นมีตัวเลขหลายสกุลความหมายปนกัน ฿ จึงยังทำหน้าที่แยกแยะอยู่)
 */
export const formatNumberNoSymbol = (n: number): string => {
  const sign = n < 0 ? '-' : ''
  const abs = Math.abs(n)
  return sign + (hasSatang(abs) ? GROUP_2 : GROUP_0).format(abs)
}

/**
 * นิยามของกำไรสุทธิ — เขียนที่เดียว ให้ทุก surface import ไปใช้
 * เดิมเขียนไว้ 2 ที่ด้วยคำไม่เหมือนกัน ("รายได้" ที่หนึ่ง "ยอดขายที่ยืนยันแล้ว" อีกที่หนึ่ง)
 * ทั้งที่หมายถึงเงินก้อนเดียวกันเป๊ะ
 */
export const NET_PROFIT_FORMULA = 'กำไรสุทธิ = ยอดขายที่ยืนยันแล้ว − ต้นทุนสินค้า − ค่าใช้จ่าย'

/**
 * นิยามของกำไร **ขั้นต้น** — คนละตัวกับกำไรสุทธิข้างบน วางติดกันโดยตั้งใจ
 *
 * ต่างกัน 2 อย่างที่ผู้ใช้ต้องรู้ ไม่ใช่รายละเอียดทางบัญชี:
 *   1. ขั้นต้นคิดจาก **ยอดที่ลูกค้าจ่ายจริงของใบนั้น** (ถ้าออเดอร์มี VAT ก็รวมอยู่ในนั้น)
 *   2. ขั้นต้น **ยังไม่หักค่าใช้จ่ายร้าน** เลยสักบาท
 * ผลคือถ้าเอากำไรขั้นต้นรายใบมาบวกกันทั้งเดือน มันจะ **ไม่เท่า** กำไรสุทธิที่หน้า /sales เสมอ
 * และนั่นถูกต้องแล้ว — แต่ต้องบอกผู้ใช้ ไม่ใช่ปล่อยให้เขาไปเจอเองแล้วสรุปว่าระบบคิดผิด
 * (impeccable critique 2026-08-08 P0: เดิมการ์ดรายใบเรียกตัวเองว่า "กำไร" เฉย ๆ
 *  ซึ่งชนกับนิยามบรรทัดบนที่ประกาศไว้ในไฟล์เดียวกัน)
 */
export const GROSS_PROFIT_FORMULA = 'กำไรขั้นต้น = ยอดที่ลูกค้าจ่าย − ต้นทุนสินค้า · ยังไม่หักค่าใช้จ่ายร้าน'

/**
 * นิยามของกำไรที่ **หน้า /sales** ใช้ — ตัวที่สามในไฟล์นี้ วางติดกันโดยตั้งใจ (Hard Rule 16)
 *
 * 🛑 **ตัวเลขนี้จะไม่เท่ากับ `NET_PROFIT_FORMULA` ที่หน้า /expenses เสมอ** และนั่นถูกต้องแล้ว:
 * หน้า /sales หักเฉพาะเงินที่ผูกกับ "การขายของ" (ต้นทุนสินค้า + ค่าส่งที่จ่ายขนส่งจริง)
 * ส่วนค่าใช้จ่ายอื่นของร้าน (ค่าเช่า เงินเดือน โฆษณา ฯลฯ ที่ร้านบันทึกเองในหน้า /expenses)
 * ยังไม่ถูกหักที่หน้านี้ — มติ user 2026-08-09: "เอา Expense ออกจาก chart นี้ก่อน"
 *
 * ตราบใดที่ทั้งสองหน้ายังนิยามต่างกัน **ห้ามใช้คำว่า "กำไรสุทธิ" ที่หน้า /sales** และต้อง render
 * สูตรนี้ให้ผู้ใช้เห็นเสมอ ไม่ใช่เก็บไว้ในคอมเมนต์ — บทเรียน 2026-08-08 P0: การ์ดที่เรียกตัวเองว่า
 * "กำไร" เฉย ๆ โดยคำนวณคนละสูตรกับที่ประกาศไว้ในไฟล์เดียวกัน ทำให้ผู้ขายบวกเลขสองหน้าจอแล้ว
 * ไม่ตรงกันโดยไม่มีอะไรฟ้อง (ไม่มี gate อัตโนมัติตัวไหนจับคลาสนี้ได้เลย)
 */
export const SALES_PROFIT_FORMULA =
  'กำไรจากการขาย = ยอดขายที่ยืนยันแล้ว − ต้นทุนสินค้า − ค่าส่ง · ยังไม่หักค่าใช้จ่ายอื่นของร้าน'

/**
 * ป้าย/ข้อความ/สีของกำไรสุทธิ ที่ต้องเหมือนกันทุก surface
 *
 * ชื่อตัวชี้วัดใช้คู่คำขนาน "กำไรสุทธิ ↔ ขาดทุนสุทธิ" — เดิมทางลบเรียก "ผลประกอบการ" ซึ่ง
 * (ก) เป็นศัพท์ทางการที่ขัดน้ำเสียงแบรนด์ และ (ข) ทำให้ดูเหมือนเป็นตัวเลขคนละตัวกับตอนเป็นบวก
 * `text` ไม่มีคำนำหน้า — ทิศทางสื่อผ่าน label + สี ทุก surface จึงต้อง render label ด้วยเสมอ
 */
export const profitDisplay = (n: number) => {
  const positive = n >= 0
  return {
    positive,
    label: positive ? 'กำไรสุทธิ' : 'ขาดทุนสุทธิ',
    text: formatBaht(n),
    toneClass: positive ? 'text-success-ink' : 'text-danger-ink',
  }
}

/**
 * %เปลี่ยนแปลงเทียบช่วงก่อนหน้า — helper กลาง กันสูตรหลุดกันตามจุดที่ใช้
 * คืน null เมื่อ "เทียบแล้วอ่านไม่รู้เรื่อง": ไม่มีฐาน (null) หรือฐาน ≤ 0 ซึ่งเปอร์เซ็นต์อ่านกลับหัว
 * UI ต้องซ่อน badge ทั้งก้อนเมื่อได้ null — ห้ามแสดง 0% หรือ +100% จากฐานศูนย์
 *
 * `invert` — ธีมใช้กฎเดียว "ค่าขึ้น = เขียว" กับทุกการ์ด แต่กฎนั้นใช้กับ "ต้นทุน"/"ค่าใช้จ่าย"
 * ไม่ได้ เพราะค่าใช้จ่ายเพิ่มขึ้นไม่ใช่ข่าวดี ทาเขียวเมื่อไหร่คือสีสื่อความหมายผิด
 * กลับเครื่องหมายที่นี่ที่เดียว badge component จึงไม่ต้องรู้เรื่อง invert เลย
 */
export const pctChangeVsPrev = (current: number, prev: number | null, invert = false): number | null => {
  if (prev == null || prev <= 0) return null
  const pct = Math.round(((current - prev) / prev) * 1000) / 10
  return invert ? -pct : pct
}
