/**
 * verify-badge — ป้าย "ยืนยันถึงระดับไหน" ของร้าน (SSOT ของทั้งคำและโทนสี)
 *
 * 🛑 ทำไมต้องเป็นไฟล์กลาง: ป้ายนี้โผล่บนหน้าจอที่ผู้ซื้อคนเดียวกันเห็น **ห่างกันไม่กี่วินาที**
 * — หน้าออเดอร์สาธารณะ `/o/{token}` แล้วกดเข้าสู่ระบบไปเจอ `OrderLinkShell` ต่อทันที
 * ถ้าคำหรือสีไม่ตรงกัน ผู้ใช้จะสังเกตได้ทันทีและมันบั่นทอนสิ่งเดียวที่ป้ายนี้มีไว้ทำ (HR16)
 *
 * 🛑 L1 ไม่ใช่สีเขียว — Verified-Means-Green สงวนเขียวไว้กับสิ่งที่ปลอมยาก
 * L1 = ยืนยันเบอร์ด้วย OTP ซึ่งซื้อซิมเติมเงินที่ร้านสะดวกซื้อก็ทำได้ ให้เขียวเท่ากับ
 * ร้านที่ส่งเอกสารให้แอดมินตรวจ (L2) หรือจดทะเบียนธุรกิจ (L3) คือการทำให้สัญญาณเฟ้อ
 * — ระดับ 0 ไม่มีป้ายเลย ไม่ใช่ป้ายที่เขียนว่า "ยังไม่ยืนยัน" (ไม่ประจานร้านใหม่)
 */

export type VerifyBadgeTone = 'neutral' | 'green' | 'gold'

export type VerifyBadge = {
  label: string
  tone: VerifyBadgeTone
  /** ชื่อ icon ของ @iconify/react (tabler) */
  icon: string
}

/**
 * สีพื้น/สีตัวอักษรต่อโทน — อยู่ที่นี่เพราะสองจอที่ใช้ป้ายนี้เขียนคนละระบบสไตล์
 * (จอหนึ่งเป็น MUI sx อีกจอเป็น Tailwind class) ถ้าปล่อยให้แต่ละที่แปลโทนเป็นสีเอง
 * มันจะเลื่อนออกจากกันทันทีที่มีคนแก้ข้างเดียว
 *
 * 🛑 ตัวอักษรใช้เฉด "ink" เสมอ ห้ามใช้สี main บนพื้นจาง — วัดได้ 1.8–3.5:1 ซึ่งตก AA
 * ทุกคู่ ค่าที่ใช้คือเฉดเดิมแค่เข้มขึ้น ไม่เปลี่ยนฮิว (contrast-fix-keeps-hue.md)
 */
export const VERIFY_BADGE_PALETTE: Record<VerifyBadgeTone, { bg: string; fg: string }> = {
  green: { bg: 'rgba(40,199,111,0.15)', fg: '#18804A' },
  gold: { bg: 'rgba(255,159,67,0.15)', fg: '#874C00' },
  neutral: { bg: 'rgba(47,43,61,0.08)', fg: 'rgba(47,43,61,0.75)' },
}

/**
 * ชื่อ "สิ่งที่ต้องทำ" ของแต่ละระดับ — ใช้เป็นหัวแถวในตารางยืนยันตัวตน (แท็บเกี่ยวกับร้าน)
 *
 * ต่างจาก `resolveVerifyBadge().label` ตรงที่ตัวนั้นผันเป็นสถานะ "ผ่านแล้ว" (มีคำว่า "แล้ว")
 * ส่วนตัวนี้เป็นชื่อกลาง ๆ ของระดับ ใช้ได้ทั้งแถวที่ผ่านและยังไม่ผ่าน — ต้องอยู่ไฟล์เดียวกัน
 * กับ label ที่ผันแล้ว ไม่งั้นวันหนึ่งจะมีคนแก้คำข้างเดียวจนสองที่พูดคนละเรื่อง (HR16)
 */
export const VERIFY_LEVEL_TITLES: Record<1 | 2 | 3, string> = {
  1: 'ยืนยันเบอร์โทรศัพท์',
  2: 'ยืนยันเอกสารตัวตน',
  3: 'จดทะเบียนธุรกิจ',
}

/**
 * ป้ายของแถวที่ยังไม่ผ่าน
 *
 * 🛑 "ยังไม่ยืนยัน" ไม่ใช่ "ไม่ผ่าน"/"ถูกปฏิเสธ" — หน้าสาธารณะ query เฉพาะ `status: 'APPROVED'`
 * จึงไม่รู้ด้วยซ้ำว่าร้านเคยยื่นหรือยัง การเขียนว่า "ไม่ผ่าน" คือการอ้างข้อเท็จจริงที่เราไม่มี
 * และเป็นการตัดสินร้านที่อาจแค่ยังไม่ได้เริ่มทำ (user เคาะ 2026-08-11: ไม่โชว์สถานะรอตรวจ)
 */
export const VERIFY_LEVEL_NOT_YET_LABEL = 'ยังไม่ยืนยัน'

/**
 * อาร์ตเวิร์กตราระดับ — ติดมุมขวาล่างของรูปโปรไฟล์ร้าน (user สั่ง 2026-08-11)
 *
 * อยู่ไฟล์เดียวกับ label/tone ด้วยเหตุผลเดิมของไฟล์นี้: ถ้าแยกไปอยู่ที่อื่น วันหนึ่งจะมีคนเพิ่ม
 * ระดับหรือสลับรูปข้างเดียวจนตรากับคำที่อยู่ห่างกันสองบรรทัดพูดคนละเรื่อง (HR16)
 *
 * 🛑 **สีของอาร์ตเวิร์กชุดนี้ยังไม่ตรงกับ `VERIFY_BADGE_PALETTE` ข้างบน** — SSOT กำหนด
 *    L2 = เขียว / L3 = ทอง แต่รูปที่ใช้อยู่เป็นน้ำเงิน / ม่วง+ทอง ⇒ ชิปข้อความกับตราจะโผล่บน
 *    จอเดียวกันแต่คนละสี และม่วงของ L3 ยังไปกินโควตา One Voice (ม่วง ≤10%) ของหน้าโปรไฟล์
 *    ซึ่งมีม่วงอยู่แล้ว 3 จุด. **user รับทราบและจะแก้ไฟล์รูปเองให้ตรงกฎ (2026-08-11)** —
 *    ห้ามแก้ `VERIFY_BADGE_PALETTE` ให้ตรงรูปแทน เพราะ Verified-Means-Green เป็นกฎ north-star
 *    ใน DESIGN.md ไม่ใช่ค่าสีที่เลือกไว้เฉย ๆ. เมื่อรูปชุดใหม่มาถึง ไฟล์แทนที่ได้เลยโดยไม่ต้องแก้โค้ด
 *
 * 🛑 มีไฟล์ `level_4.png` อยู่ใน `public/images/level/` ด้วยแต่ **จงใจไม่แมป** — ระบบมีแค่ 3 ระดับ
 *    (`VERIFY_LEVEL_TITLES`) การแมปมันเข้ามาโดยไม่มีระดับที่ 4 จริงจะทำให้เลข "4" ในรูปขัดกับ
 *    ตารางระดับในแท็บ "เกี่ยวกับร้าน" ที่มี 3 แถว. วันที่มีระดับ 4 จริงให้เพิ่มที่นี่ที่เดียว
 *
 * 🛑 ไม่มี key 0 — ระดับ 0 ไม่มีตราเลย (ดูเหตุผลหัวไฟล์: ไม่ประจานร้านใหม่) ผู้เรียกต้องเช็ค
 *    `maxVerifyLevel` ก่อน ไม่ใช่หวังว่า lookup จะคืน undefined แล้วรอดไปเอง
 */
export const VERIFY_LEVEL_IMAGE: Record<1 | 2 | 3, string> = {
  1: '/images/level/level_1.png',
  2: '/images/level/level_2.png',
  3: '/images/level/level_3.png',
}

/** ตราของระดับที่ระบุ — คืน null เมื่อไม่มีตราให้แสดง (ระดับ 0 หรือค่านอกช่วง) */
export function resolveVerifyLevelImage(maxVerifyLevel: number): { src: string; alt: string } | null {
  // clamp ขึ้นบน: ถ้าวันหนึ่งมีระดับ 4 ในฐานแต่ยังไม่มีรูป ให้ใช้ตราสูงสุดที่มี ไม่ใช่หายไปเฉย ๆ
  const level = maxVerifyLevel >= 3 ? 3 : maxVerifyLevel === 2 ? 2 : maxVerifyLevel === 1 ? 1 : 0
  if (level === 0) return null
  const key = level as 1 | 2 | 3
  return {
    src: VERIFY_LEVEL_IMAGE[key],
    // ชื่อที่ screen reader อ่าน — ต้องมีคำว่า "ระดับ N" เพราะเลขในอาร์ตเวิร์กอ่านไม่ออกที่ 36px
    // และสีอย่างเดียวสื่อความหมายไม่ได้ตามเกณฑ์ WCAG 1.4.1
    alt: `ระดับ ${key} · ${VERIFY_LEVEL_TITLES[key]}`,
  }
}

export function resolveVerifyBadge(maxVerifyLevel: number): VerifyBadge | null {
  if (maxVerifyLevel >= 3) {
    return { label: 'จดทะเบียนธุรกิจแล้ว', tone: 'gold', icon: 'tabler-building-store' }
  }
  if (maxVerifyLevel === 2) {
    return { label: 'ยืนยันเอกสารแล้ว', tone: 'green', icon: 'tabler-file-certificate' }
  }
  if (maxVerifyLevel === 1) {
    return { label: 'ยืนยันเบอร์แล้ว', tone: 'neutral', icon: 'tabler-phone-check' }
  }

  return null
}
