/**
 * agent-sla — เกณฑ์ SLA ของการตอบแชท (feature 00059)
 *
 * pure module — ไม่แตะ DB
 *
 * ── ทำไมต้องเป็นไฟล์ของตัวเอง ────────────────────────────────────────────────
 * โจทย์ข้อ 7 สั่งตรงตัวว่า "ห้ามฝังกฎธุรกิจลึกอยู่ในโค้ด query" และตอนสำรวจยืนยันแล้วว่า
 * **ระบบยังไม่เคยมีแนวคิด SLA อยู่เลย** (grep `sla` ทั้ง `src/` ได้ 0 ไฟล์) ⇒ ต้องตั้งใหม่
 * แต่ตั้งให้ "ย้ายไปเก็บในฐานข้อมูลทีหลังได้โดยไม่ต้องแก้ query สักบรรทัด"
 *
 * วิธีที่เลือก: ทุกที่ที่ต้องรู้เกณฑ์ ต้องเรียก `resolveSlaConfig()` แล้วอ่านค่าจากออบเจ็กต์
 * วันที่ร้านตั้งเกณฑ์เองได้ ให้เพิ่มคอลัมน์บน `Shop` แล้วเติมตรงนี้ที่เดียว — ผู้เรียกทุกจุด
 * (service/route/หน้าจอ) ไม่ต้องแก้เลย
 *
 * 🛑 ห้าม hardcode เลข 300 ที่อื่นเด็ดขาด และห้ามเขียน `wait_sec <= 300` ลงใน SQL ตรง ๆ —
 * ค่าต้องถูกส่งเข้าไปเป็นพารามิเตอร์เสมอ (ไม่งั้นวันที่เกณฑ์เปลี่ยน ตัวเลขบนการ์ดกับตัวเลข
 * ในตารางจะคนละเกณฑ์กันโดยไม่มีอะไรฟ้อง — Hard Rule 16)
 */

/**
 * เกณฑ์ตั้งต้น: ตอบครั้งแรกภายใน 5 นาที
 *
 * ที่มา: โจทย์ระบุ "First response <= 5 minutes" เป็นค่าเริ่มต้น — ยังไม่ใช่ตัวเลขที่ร้านตกลง
 * ด้วย ⇒ หน้าจอต้องเขียนเกณฑ์กำกับไว้ข้าง ๆ ตัวเลขเสมอ ห้ามแสดง "SLA 92%" ลอย ๆ
 */
export const DEFAULT_FIRST_RESPONSE_SLA_SEC = 5 * 60

export type SlaConfig = {
  /** เพดานเวลาตอบครั้งแรก (วินาที) */
  firstResponseSec: number
  /** ที่มาของค่า — หน้าจอใช้บอกผู้ใช้ว่านี่คือค่าตั้งต้นของระบบ ไม่ใช่ค่าที่ร้านตั้งเอง */
  source: 'SYSTEM_DEFAULT' | 'SHOP'
}

/**
 * เกณฑ์ที่ใช้กับร้านนี้
 *
 * รับ `shop` เข้ามาแล้ว (ยัง) ไม่ใช้ โดยตั้งใจ — พารามิเตอร์นี้คือจุดที่ค่าจากฐานข้อมูลจะเสียบเข้ามา
 * ในอนาคต การรับไว้ตั้งแต่แรกทำให้ผู้เรียกทุกจุดถูก "บังคับให้ส่งบริบทของร้าน" มาแล้ว
 * วันที่ฟีเจอร์ตั้งค่าเกิดขึ้นจริงจึงไม่ต้องไล่แก้ call site
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars -- พารามิเตอร์นี้คือจุดเสียบค่าจากฐานข้อมูลในอนาคต (ดูคอมเมนต์เหนือฟังก์ชัน) ตั้งใจรับไว้ก่อนใช้
export function resolveSlaConfig(_shop?: { id: string } | null): SlaConfig {
  return { firstResponseSec: DEFAULT_FIRST_RESPONSE_SLA_SEC, source: 'SYSTEM_DEFAULT' }
}

/**
 * เธรดนี้ตอบทันเกณฑ์ไหม
 *
 * 🛑 `null` (ไม่เคยถูกตอบ) = **ไม่ทัน** ไม่ใช่ "ไม่นับ" — เธรดที่ไม่มีใครตอบเลยคือเคสที่ SLA
 * มีไว้จับโดยตรง ถ้าตัดออกจากตัวหาร ทีมที่ปล่อยลูกค้าทิ้งจะได้ SLA 100%
 */
export function meetsFirstResponseSla(firstResponseSec: number | null, cfg: SlaConfig): boolean {
  if (firstResponseSec === null) return false
  return firstResponseSec <= cfg.firstResponseSec
}
