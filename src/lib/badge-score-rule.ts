/**
 * badge-score-rule.ts — SSOT ของ "เหรียญ 1 ใบมีผลกับ Trust Score เท่าไหร่"
 *
 * 🛑 ทำไมต้องแยกไฟล์ ไม่ไปวางใน trust-score.service.ts ตรง ๆ ตาม Hard Rule 16:
 * `BadgeDetailModal.tsx` เป็น `'use client'` — import จาก service จะลาก `@/lib/prisma`
 * เข้า client bundle. ไฟล์นี้จึงเป็นตัวกลางที่ไม่มี dependency อะไรเลย แล้วให้ **ทั้งสองฝั่ง
 * import ตัวเดียวกัน**: `calcBadgeScore()` ใช้ตัวเลขไปคำนวณ ส่วนโมดัลใช้ประโยคไปแสดง
 * ⇒ วันที่แก้สูตร ประโยคบนจอขยับตาม ไม่มีทางหลุดคนละทิศ
 *
 * บทเรียนที่ทำให้ไฟล์นี้เกิด (impeccable critique 2026-08-09, P0):
 * โมดัลเขียนกับทุกใบว่า "Trust Score เพิ่มขึ้น 10%" ขณะที่ของจริงคือ `Math.min(10, count)`
 * = 1 เหรียญได้ 1 คะแนนจาก 100 และใบที่ 11 เป็นต้นไปได้ 0 — ตัวเลขที่หน้าจอสัญญาสูงกว่า
 * ที่ระบบจ่ายจริง 10 เท่า ไม่มี gate ไหนจับได้เพราะทั้งสองฝั่ง "ถูก" ในตัวเอง
 */

/** เหรียญ 1 ใบ = กี่คะแนนใน Trust Score (เต็ม 100) */
export const BADGE_SCORE_PER_BADGE = 1

/** เพดานคะแนนรวมที่ได้จากเหรียญทั้งหมด — ใบที่เกินเพดานไม่เพิ่มคะแนนอีก */
export const BADGE_SCORE_MAX = 10

/**
 * ประโยคอธิบายผลของเหรียญ สำหรับแสดงบนหน้าจอผู้ขาย
 * ไม่ใช้สรรพนามบุคคลที่สอง ("ของคุณ") เพราะหน้านี้เปิดได้ทั้งเจ้าของร้านและพนักงาน
 * ที่ owner เชิญมา — ประธานคือ "ร้าน" ให้ตรงกับ H1 "ความสำเร็จของร้านค้า"
 */
export const BADGE_SCORE_EFFECT_TEXT =
  `นับเป็น ${BADGE_SCORE_PER_BADGE} คะแนนความน่าเชื่อถือของร้าน (เหรียญทั้งหมดรวมกันสูงสุด ${BADGE_SCORE_MAX} คะแนน)`
