import { readFileSync } from 'node:fs'
import { join } from 'node:path'

import { describe, expect, it } from 'vitest'

/**
 * [blocker] โครง 2 คอลัมน์ของหน้าออเดอร์ `/o/[token]` — **ห้ามกระทบมือถือ**
 *
 * หน้านี้ถูกเปิดจาก **WebView ของแอปผู้ขาย iOS/Android** ซึ่งกว้างเท่ามือถือเสมอ
 * (หัวหน้าสั่งไว้ตรง ๆ: "มี webview ที่จัด responsive … ห้ามกระทบ")
 *
 * โครง 2 คอลัมน์ตามม็อกอัพ v3 ย้ายบล็อกข้าม DOM จริง (การ์ดเงินไปคอลัมน์ขวา ·
 * รายการบริการไปคอลัมน์ซ้าย) ⇒ **ถ้าไม่มีอะไรกันไว้ ลำดับบนมือถือจะสลับทันที**
 * กลไกที่กันคือคู่นี้ ซึ่งต้องอยู่ครบทั้งคู่เสมอ:
 *
 *   1. กล่องคอลัมน์เป็น `display: 'contents'` บนมือถือ ⇒ ไม่เกิดกล่องจริง
 *      ลูกทุกใบยังเป็น flex item ของคอนเทนเนอร์เดิม
 *   2. ลูกทุกใบมี `order: N` เรียงตามลำดับที่ควรเห็นบนมือถือ
 *
 * ขาดข้อไหนข้อหนึ่ง = ลำดับมือถือเปลี่ยนโดยไม่มี tsc/build/เทสตัวไหนฟ้อง
 * (เป็นความเสียหายที่เห็นเฉพาะบนเครื่องจริง ซึ่งคือที่ที่เราไม่มีทางเห็นจากเครื่อง dev)
 *
 * 🛑 แดง = ห้าม merge
 */
const REL = 'src/app/(marketing)/o/[token]/OrderDetailMobile.tsx'

const src = (() => {
  const raw = readFileSync(join(process.cwd(), REL), 'utf8')
  return raw
    .replace(/\{\/\*[\s\S]*?\*\/\}|\/\*[\s\S]*?\*\//g, (m) => m.replace(/[^\n]/g, ' '))
    .replace(/(?<!:)\/\/.*$/gm, (m) => ' '.repeat(m.length))
})()

describe('[blocker] โครง 2 คอลัมน์ต้องไม่ทำให้ลำดับบนมือถือเปลี่ยน', () => {
  it('กล่องคอลัมน์ต้องเป็น display:contents บนมือถือ — ห้ามเป็นกล่องจริง', () => {
    /* ถ้าเป็น flex/block บนมือถือ ลูกจะถูกกักอยู่ในกล่องของตัวเอง ⇒ `order` ข้ามกล่องไม่ได้
       แล้วลำดับจะกลายเป็น "ซ้ายทั้งก้อน แล้วขวาทั้งก้อน" ทันที

       🛑 ห้ามนับ `display: 'contents'` ดิบ ๆ — ร่างแรกทำอย่างนั้นแล้วแดงทันทีที่เพิ่ม
       การ์ด hero/ราง (ซึ่งใช้ท่าเดียวกันแต่คนละหน้าที่) ทั้งที่ของยังถูกทุกอย่าง
       ด่านต้องผูกกับ **รูปแบบของกล่องคอลัมน์** ไม่ใช่จำนวนที่บังเอิญเท่ากันวันที่เขียน */
    const stacks = [
      ...src.matchAll(/display: 'contents',\s*\[ORDER_TWO_COL_MQ\]:\s*\{\s*display: 'flex',\s*flexDirection: 'column'/g),
    ]
    expect(stacks.length, 'ต้องมีกล่องคอลัมน์ซ้าย/ขวาอย่างละใบ').toBe(2)
  })

  it('การ์ด hero + ราง ต้องเป็นการ์ด "เฉพาะจอกว้าง" — บนมือถือต้องไม่มีขอบ/เงาโผล่มา', () => {
    /* ม็อกอัพห่อสองส่วนนี้เป็นการ์ด แต่ของเดิมบนมือถือเป็นแผ่นเต็มความกว้างติดขอบจอ
       ถ้าปล่อยให้เป็น `display:block` ตั้งแต่มือถือ จะได้ขอบ+เงา+รัศมีโผล่บน WebView ทันที
       ซึ่งคือ "กระทบมือถือ" ตรงตัว

       🛑 mutation พิสูจน์แล้วว่าถ้าไม่มีข้อนี้ การเปลี่ยนใบใดใบหนึ่งเป็นกล่องจริง
       จะผ่านทุกด่าน (ข้ออื่นตรวจเฉพาะใบที่ *ยังเป็น* contents อยู่) */
    /* 🛑 **ห้ามผูกกับจำนวนการ์ด** — ผมเขียนแบบนั้นแล้วแดงทันทีที่เพิ่มการ์ดใบที่สาม
       (ข้อมูลร้าน) ทั้งที่ของถูกทุกอย่าง · เป็นความพลาดแบบเดียวกันครั้งที่สามในงานนี้
       กฎที่ต้องบังคับคือ **"ทุกใบที่กลายเป็นการ์ดบนจอกว้าง ต้องเป็น contents บนมือถือ"**
       ซึ่งเป็นจริงไม่ว่าจะมีกี่ใบ และยังจับกรณีที่ใครทำใบใดใบหนึ่งเป็นกล่องจริงได้ */
    const cards = [...src.matchAll(/\[ORDER_TWO_COL_MQ\]:\s*\{\s*display: 'block',/g)]
    expect(cards.length, 'ต้องมีการ์ดเฉพาะจอกว้างอย่างน้อย hero + ราง').toBeGreaterThanOrEqual(2)

    for (const m of cards) {
      /**
       * 🛑 **ห้ามใช้หน้าต่างความยาวคงที่ (`slice(index-200, index)`)** — ร่างเดิมทำแบบนั้น
       * แล้วแดงทันทีที่หน้ามีกล่องแบบที่สองซึ่งใช้รูปเดียวกัน: กล่อง `.order-info` (2026-08-30)
       * ที่ `display:'none'` บนมือถือ แล้วเปิดเป็น `block` บนจอกว้าง — ของถูกทุกอย่าง
       * แต่ anchor ของด่านชนกันเอง
       *
       * รูป `display:'block'` ใต้ MQ มี **2 ความหมายที่ถูกทั้งคู่**:
       *   `display:'contents'` ที่ฐาน = การ์ดห่อที่ยุบตัวเองบนมือถือ (ห้ามกลายเป็นกล่องจริง)
       *   `display:'none'`     ที่ฐาน = กล่องที่มีเฉพาะจอกว้างโดยตั้งใจ
       * สิ่งที่ผิดจริงคือ **ไม่มีทั้งสองอย่าง** = กล่องที่โผล่บนมือถือด้วยขอบ/เงาของจอกว้าง
       *
       * ยึด **ขอบเขตของ `sx={{` ที่มันอยู่** แทนระยะตัวอักษร ⇒ ทนต่อการเพิ่มคอมเมนต์
       */
      const sxAt = src.lastIndexOf('sx={{', m.index)
      expect(sxAt, 'ทุกกฎ MQ ต้องอยู่ใน sx ของตัวเอง').toBeGreaterThan(-1)
      /* 🛑 ต้องถอยไปถึง **ชื่อแท็ก** ด้วย ไม่ใช่แค่ `sx={{` — ร่างก่อนหน้าตัดที่ `sx={{` พอดี
         แล้วข้อ "ต้องเป็น <Card>" แดงทันที เพราะคำว่า `<Card` อยู่ *ก่อน* จุดตัด
         (ของถูกทุกอย่าง ด่านตัดผิดที่เอง) */
      const elAt = src.lastIndexOf('<', sxAt)
      const before = src.slice(elAt, m.index)
      const base = before.match(/display: '(contents|none)',/)
      expect(base, 'การ์ดจอกว้างต้องยุบเป็น contents หรือซ่อนทิ้งบนมือถือ').toBeTruthy()

      /* 🛑 **ผิวการ์ดต้องมาจาก `<Card>` ของธีม Vuexy เท่านั้น** (Hard Rule 1)
         ร่างก่อนหน้าเขียน `border` + `boxShadow` + `borderRadius` เอง ⇒ ได้เงา/ขอบ/รัศมี
         คนละชุดกับการ์ดใบอื่นบนหน้าเดียวกัน ซึ่งใช้ `<Card>` อยู่แล้ว
         (หัวหน้าทัก 2026-08-29: "ขอบต้องทำ ธีม Vuexy นะ")
         skin ของโปรเจกต์คือ `default` ⇒ Card = เงา `customShadows-md` และ **ไม่มีขอบ**

         🛑 บังคับเฉพาะเคส `contents` — นั่นคือ "การ์ดที่ยุบตัวเองบนมือถือ" ซึ่งเป็นของที่กฎนี้
         พูดถึงจริง ๆ · เคส `none` คือกล่องธรรมดาที่มีเฉพาะจอกว้าง (เช่น `.order-info` ของ v5)
         ซึ่งไม่ใช่การ์ด ไม่มีผิวเป็นของตัวเอง และไม่เคยโผล่บนมือถือให้ผิวขัดกับใคร */
      if (base![1] === 'contents') {
        expect(before, 'ต้องเป็น <Card> ของธีม ไม่ใช่ <Box> ที่แต่งผิวเอง').toMatch(/<Card\b/)
      }
    }

    /* และ sx ของการ์ดเหล่านั้นต้องไม่ประกาศผิวเอง — จำกัดขอบเขตการตรวจไว้ที่บล็อกของการ์ด
       (ที่อื่นบนหน้ายังมีขอบ/เงาที่ถูกต้องของมันเอง เช่น ขอบบนของแถบ CTA และวงจุดบนราง)

       🛑 **ห้ามใช้หน้าต่างความยาวคงที่** — ไฟล์นี้แทนคอมเมนต์ด้วยช่องว่าง **ที่ยาวเท่าเดิม**
       การ์ดพวกนี้มีคอมเมนต์อธิบายยาวกว่า 700 ตัวอักษร ⇒ หน้าต่าง 700 ไปไม่ถึงโค้ดจริงเลย
       เทสจึงเขียวแม้เติม `boxShadow` เข้าไปตรง ๆ (พิสูจน์ด้วย mutation แล้วว่าเงียบจริง) */
    for (const m of cards) {
      /* 🛑 ข้อนี้พูดถึง **การ์ด** เท่านั้น — กล่องที่ `display:'none'` บนมือถือแล้วเปิดบนจอกว้าง
         (เช่น `.order-info` ของม็อกอัพ v5) เป็นกล่องข้อมูลธรรมดา มีขอบของตัวเองได้ตามปกติ
         เหมือนกล่องข้อมูลอื่นทั้งหน้า · แยกด้วย `display` ที่ฐาน ไม่ใช่ด้วยตำแหน่ง */
      const sxAt = src.lastIndexOf('sx={{', m.index)
      if (!/display: 'contents',/.test(src.slice(sxAt, m.index))) continue

      const end = src.indexOf('\n            }}', m.index)
      expect(end, 'ต้องหาจุดจบของ sx ได้').toBeGreaterThan(m.index)
      const block = src.slice(m.index, end)
      for (const banned of ['boxShadow:', 'border:', 'borderColor:', 'borderRadius:']) {
        expect(block, `การ์ดห้ามประกาศ ${banned} เอง — ปล่อยให้ <Card> ของธีมจัดการ`).not.toContain(banned)
      }
    }
  })

  it('ทุกกล่องที่เป็น display:contents ต้องเป็นของ "เฉพาะจอกว้าง" — ห้ามมีใบไหนโผล่บนมือถือ', () => {
    /* `display:contents` คือกลไกที่ทำให้กล่องหายไปบนมือถือ — ใบไหนที่ไม่ได้คู่กับ
       `[ORDER_TWO_COL_MQ]` แปลว่ามีกล่องที่ไม่เคยกลายเป็นอะไรเลย = โค้ดตาย
       หรือแย่กว่านั้นคือคนตั้งใจให้มันมีผลบนมือถือ ซึ่งเป็นสิ่งที่ห้าม */
    for (const m of src.matchAll(/display: 'contents',/g)) {
      const after = src.slice(m.index, m.index + 120)
      expect(after, 'display:contents ต้องตามด้วย [ORDER_TWO_COL_MQ]').toMatch(/\[ORDER_TWO_COL_MQ\]/)
    }
  })

  it('กฎ 2 คอลัมน์ทุกข้อต้องอยู่ใต้ media query เท่านั้น — ห้ามหลุดมาเป็นกฎฐาน', () => {
    /* 🛑 `grid-template-columns` ที่หลุดออกมานอก media query = มือถือกลายเป็น 2 คอลัมน์
       บนจอ 390px ทันที ซึ่งคือความเสียหายที่กฎข้อนี้มีไว้กัน */
    const at = src.indexOf('gridTemplateColumns')
    expect(at, 'ต้องมีโครง 2 คอลัมน์').toBeGreaterThan(-1)

    /* ต้องอยู่ใต้ตัวเปิด media query และต้องเป็นตัวคงที่ตัวเดียวของหน้านี้
       (ห้ามใครพิมพ์ media query เองซ้ำ — ค่าจะ drift จาก `content-width.ts`)

       🛑 **ห้ามใช้หน้าต่างความยาวคงที่** — ไฟล์นี้แทนคอมเมนต์ด้วยช่องว่างที่ยาวเท่าเดิม
       การเพิ่มคอมเมนต์อธิบายเหนือบรรทัดนี้จึงดันหมุดหลุดกรอบ แล้วแดงทั้งที่ของถูก
       (พลาดแบบเดียวกันเป็นครั้งที่สี่ในงานนี้ — วัดจาก "ตัวไหนใกล้กว่า" แทน)

       วิธีที่ทนทาน: ตัวเปิด `[ORDER_TWO_COL_MQ]: {` ต้องอยู่ **ใกล้กว่า** ตัวเปิด `sx={{`
       ที่อยู่ก่อนหน้า ⇒ พิสูจน์ว่ากฎอยู่ในบล็อก media query ไม่ใช่ใน sx ชั้นฐาน */
    const mq = src.lastIndexOf('[ORDER_TWO_COL_MQ]: {', at)
    const baseSx = src.lastIndexOf('sx={{', at)
    expect(mq, 'ต้องมีตัวเปิด ORDER_TWO_COL_MQ ก่อนหน้า').toBeGreaterThan(-1)
    expect(mq, 'gridTemplateColumns ต้องอยู่ในบล็อก media query ไม่ใช่ sx ชั้นฐาน').toBeGreaterThan(baseSx)
    expect(src, 'ห้าม hardcode media query เอง').not.toMatch(/'@media \(min-width:8\d\dpx\)'/)
  })

  it('ลูกของคอลัมน์ทุกใบต้องมี order — และห้ามซ้ำกัน', () => {
    /* `order` คือสิ่งเดียวที่พาลำดับมือถือกลับมา — ใบที่ลืมใส่จะได้ order 0
       แล้วกระโดดไปอยู่บนสุดของหน้าโดยไม่มีอะไรฟ้อง (0 น้อยกว่าทุกค่าที่เราตั้ง) */
    /* 🛑 `(?<![A-Za-z])` จำเป็น — `order:` เป็น **สตริงย่อยของ `border:`** ⇒ regex เปล่า ๆ
       จับ `border: 0,` มาเป็น "ใบที่ลืมตั้ง order" แล้วแดงทั้งที่ไม่มีใครทำผิด
       (เกิดจริง 2026-08-30 ตอนเพิ่มแถวการกระทำที่มี `border: 0`) · คลาสเดียวกับ grep gate
       ของ HR9 ที่แดงค้างเพราะ match คำเปล่า ๆ */
    const orders = [...src.matchAll(/(?<![A-Za-z])order:\s*(\d+),/g)].map((m) => Number(m[1]))
    expect(orders.length, 'ต้องมีบล็อกที่ตั้ง order').toBeGreaterThan(5)
    expect(new Set(orders).size, `order ซ้ำกัน: ${orders.join(',')}`).toBe(orders.length)
    expect(Math.min(...orders), 'ห้ามมี order 0 — 0 คือค่าของใบที่ลืมตั้ง').toBeGreaterThan(0)
  })

  it('order ต้องเรียงต่อเนื่องจาก 1 ไม่มีเลขข้าม — เลขข้าม = มีบล็อกหายไประหว่างทาง', () => {
    /* ถ้ามีคนลบบล็อกทิ้งแล้วไม่ไล่เลขใหม่ จะเหลือช่องว่าง ซึ่งตัวมันเองไม่พัง
       แต่แปลว่ามีคนแก้โครงนี้โดยไม่ได้อ่านกติกา — ให้แดงไว้ก่อนจะปลอดภัยกว่า */
    /* ขอบเขตคำจำเป็นด้วยเหตุผลเดียวกับข้อบน — `border:` มี `order:` อยู่ข้างใน */
    const orders = [...src.matchAll(/(?<![A-Za-z])order:\s*(\d+),/g)].map((m) => Number(m[1])).sort((a, b) => a - b)
    expect(orders).toEqual(Array.from({ length: orders.length }, (_, i) => i + 1))
  })

  it('ทุกใบต้องซ่อนตัวเองเมื่อไม่มีเนื้อหา — ไม่งั้นได้ช่องว่างจากเงื่อนไขที่เป็นเท็จ', () => {
    /* กล่องห่อที่ว่างยังกิน `gap` ของ flex/grid ⇒ ออเดอร์ที่ไม่มีสลิป/ไม่มีนัด
       จะได้ช่องโหว่กลางหน้าโดยไม่มีอะไรอยู่ในนั้น */
    /* ขอบเขตคำจำเป็นด้วยเหตุผลเดียวกับข้อบน — `border:` มี `order:` อยู่ข้างใน */
    const wrappers = [...src.matchAll(/(?<![A-Za-z])order:\s*\d+,[^}]*\}/g)].map((m) => m[0])
    expect(wrappers.length).toBeGreaterThan(5)
    for (const w of wrappers) {
      expect(w, `กล่องห่อต้องมี &:empty → display:none — ${w.slice(0, 40)}`).toMatch(/'&:empty'/)
    }
  })

  it('[blocker] จุดบนรางต้องประกอบผ่าน dot() ตัวเดียว — ห้ามเขียน sx ของจอกว้างเอง', () => {
    /* 🛑 บทเรียนสด ๆ 2026-08-29: ร่างแรกแยกเป็น 3 helper (`size`/`halo`/fontSize) แล้ว
       spread เข้า sx เดียวกัน — **ทั้งสามมีคีย์ `[ORDER_TWO_COL_MQ]` เหมือนกัน**
       ⇒ ตัวหลังเขียนทับตัวหน้าเงียบ ๆ จุดโตเป็น 44px จริงแต่ **วงแหวนหายทั้งราง**
       `tsc`/`eslint`/เทสผ่านหมด เพราะอ็อบเจกต์ถูกต้องทุกตัวอักษร — สิ่งที่หายคือคีย์ซ้ำ
       จับได้ตอนนับ `box-shadow:0 0 0 6px` ใน HTML ที่เสิร์ฟจริงแล้วได้ 0

       กติกาที่บังคับ: ทุกกิ่งของจุดต้องประกอบผ่าน `dot()` ซึ่งรวม override ของจอกว้าง
       ไว้ก้อนเดียว — ใครเขียน sx เองจะพลาดซ้ำได้ทันที */
    const fn = src.slice(src.indexOf('function TimelineDot'), src.indexOf('function connectorColor'))
    expect(fn, 'ต้องมี helper รวมของจอกว้าง').toMatch(/const dot = \(/)

    const branches = [...fn.matchAll(/\.\.\.dot\(\{/g)]
    expect(branches.length, 'ทุกกิ่งของจุด (done/cur/fin/cx/mute-up) ต้องเรียก dot()').toBe(5)

    /* `dot()` เองต้องมี `[ORDER_TWO_COL_MQ]` **ก้อนเดียว** — สองก้อนคือรูปร่างของบั๊กที่เพิ่งเจอ

       🛑 หมุดปิดขอบเขตต้องเป็น **โค้ด** ไม่ใช่คอมเมนต์ — ร่างแรกใช้ `fn.indexOf('// done')`
       ทั้งที่ไฟล์นี้ **ตัดคอมเมนต์ทิ้งก่อนสแกน** ⇒ ได้ -1 แล้ว slice กินเกือบทั้งฟังก์ชัน
       นับได้ 3 (helper + Typography อีกสองตัว) แดงตั้งแต่ยังไม่ mutate */
    const insideDot = fn.slice(fn.indexOf('const dot = ('), fn.indexOf("if (state === 'done')"))
    expect([...insideDot.matchAll(/\[ORDER_TWO_COL_MQ\]/g)].length, 'dot() ต้องมี MQ ก้อนเดียว').toBe(1)
  })

  it('[blocker] แถบ CTA ต้องกว้างเท่าคอลัมน์เนื้อหา ไม่ใช่ค่าของจอ guest', () => {
    /* ปุ่มที่ยืนยันงานถาวร ควรอยู่ในกรอบเดียวกับสิ่งที่มันกำลังยืนยัน —
       ถ้าใช้ `orderContentWidthSx` (880) ขณะเนื้อหาเป็น 1200 ปุ่มจะเยื้องจากขอบการ์ด */
    expect(src, 'หน้านี้ต้องไม่ใช้ความกว้างของจอ guest แล้ว').not.toMatch(/orderContentWidthSx/)
    const uses = [...src.matchAll(/\.\.\.orderDetailWidthSx/g)]
    expect(uses.length, 'ต้องใช้ทั้งคอลัมน์เนื้อหาและแถบ CTA').toBe(2)
  })
})

/**
 * [blocker] หน้านี้ต้องไม่เลื่อนซ้ายขวาได้
 *
 * 🛑 หัวหน้าเจอบนจอจริง 2026-08-29: "ทำไมจอมันเลื่อนซ้ายขวาได้อ่ะ"
 *
 * ต้นเหตุคือคอลัมน์ขวาถูกประกาศเป็น `minmax(330px, .75fr)` — ค่าพื้นแข็งทำให้กริด
 * **ไม่มีทางแคบกว่า 330px + gap** ไม่ว่ากล่องจะเหลือที่เท่าไร บวกกับ grid item
 * มี `min-width: auto` เป็นค่าตั้งต้น ⇒ เนื้อหาที่กว้างกว่าช่องดันคอลัมน์ออกไปอีก
 *
 * เป็นคลาสเดียวกับ `flex-header-truncation.md` ซึ่งเคยเกิดบน prod มาแล้ว
 * (ชื่อเพจ 34 ตัวอักษรดันการ์ดหลุดขอบซ้ายทั้งจอ) — `truncate`/`ellipsis` ที่ลูก
 * **ไม่มีผลเลย** ถ้ากล่องแม่ไม่ยอมหด
 */
describe('[blocker] กันจอเลื่อนซ้ายขวา', () => {
  it('คอลัมน์กริดต้องยอมหด — ห้ามมีพื้นแข็งเป็น px', () => {
    const at = src.indexOf('gridTemplateColumns')
    expect(at, 'ต้องมีโครง 2 คอลัมน์').toBeGreaterThan(-1)
    const decl = src.slice(at, at + 120)
    expect(decl, 'ทุกคอลัมน์ต้องเริ่มที่ minmax(0,…)').toMatch(/minmax\(0,[^)]*\)\s+minmax\(0,[^)]*\)/)
    expect(decl, 'ห้ามมี minmax ที่ขั้นต่ำเป็น px').not.toMatch(/minmax\(\s*\d+px/)
  })

  it('กล่องคอลัมน์ทุกใบต้องมี minWidth: 0 — ไม่งั้น min-width:auto ดันจอ', () => {
    /* 🛑 อีกครั้งที่ **ห้ามใช้หน้าต่างความยาวคงที่** — ร่างแรกตัดมา 600 ตัวอักษร
       แล้ว mutation ที่ถอด `minWidth: 0` ออกจากคอลัมน์หนึ่ง **ยังเขียว**
       (คอมเมนต์ถูกแทนด้วยช่องว่างยาวเท่าเดิม หน้าต่างจึงไปไม่ถึงบรรทัดจริง)

       ผูกด้วย **จำนวนที่ต้องเท่ากัน** แทน: กล่องคอลัมน์มีกี่ใบ ก็ต้องมี `minWidth: 0`
       เท่านั้นใบ — ถอดออกใบเดียวก็ไม่เท่าแล้ว */
    const stacks = [
      ...src.matchAll(/display: 'contents',\s*\[ORDER_TWO_COL_MQ\]:\s*\{\s*display: 'flex',\s*flexDirection: 'column'/g),
    ]
    expect(stacks.length, 'ต้องเจอกล่องคอลัมน์').toBe(2)

    /* 🛑 นับ `minWidth: 0` ทั้งไฟล์ไม่ได้ — รางกับแถวที่มาก็ใช้ค่าเดียวกันอย่างถูกต้อง
       ต้องผูกกับ **ลายเซ็นของกล่องคอลัมน์** โดยเฉพาะ

       เคล็ดที่ทำให้ทนคอมเมนต์: ไฟล์นี้แทนคอมเมนต์ด้วย **ช่องว่าง** ⇒ `\s*` คร่อมข้ามได้เอง
       ไม่ว่าจะเขียนคำอธิบายยาวแค่ไหน (ต่างจากหน้าต่างความยาวคงที่ซึ่งพังทุกครั้ง) */
    const guarded = [...src.matchAll(/flexDirection: 'column',\s*gap: 4\.5,\s*minWidth: 0,/g)]
    expect(guarded.length, 'คอลัมน์ทุกใบต้องมี minWidth: 0 ต่อท้าย gap').toBe(stacks.length)
  })

  it('กล่องเลื่อนของหน้าต้องมี overflowX: clip เป็นกันชนชิ้นสุดท้าย', () => {
    /* ชิ้นที่สามของ "ชุดกันล้น" — ไม่ใช่การกลบ เพราะต้นเหตุถูกแก้ที่คอลัมน์แล้ว
       อันนี้กันเคสที่ยังไม่เจอไม่ให้กลายเป็น "ทั้งหน้าเลื่อนได้" ซึ่งผู้ใช้เจอก่อนเราเสมอ

       🛑 `clip` ไม่ใช่ `hidden` — ดูเหตุผลที่ describe ท้ายไฟล์ */
    expect(src, "ต้องมี overflowX: clip").toMatch(/overflowX: 'clip'/)
  })
})

/**
 * [blocker] `width`/`height` ที่ตั้งใจให้เป็นพิกเซล ต้องเขียนเป็นสตริง
 *
 * 🛑 ระบบ `sizing` ของ MUI ตีความ **เลข ≤ 1** ใน `width`/`height` เป็น **สัดส่วน**
 * ⇒ `width: 1` = `100%` ไม่ใช่ 1px
 *
 * กล่องข้อความสำหรับ screen reader ที่ตั้งใจให้เล็ก 1 พิกเซล จึงกลายเป็นกล่อง
 * **กว้างเต็มจอ 4 ใบ** วางแบบ absolute แล้วดันความกว้างเอกสารจาก 1440 → 1870
 * ⇒ ทั้งหน้าเลื่อนซ้ายขวาได้ (หัวหน้าเจอเอง 2026-08-29 และผมเดาผิดอยู่ 3 รอบ
 * ก่อนจะยอมเปิดเบราว์เซอร์จริงมาวัด — `tsc`/eslint/เทสผ่านหมด เพราะ `1`
 * ถูกต้องตามชนิดทุกประการ สิ่งที่ผิดคือ *หน่วย* ที่ MUI เติมให้)
 */
describe('[blocker] ค่าพิกเซลใน sx ต้องไม่ถูกตีเป็นสัดส่วน', () => {
  it("กล่อง sr-only ต้องใช้ '1px' ไม่ใช่ 1", () => {
    const at = src.indexOf('STEP_STATE_SR_TEXT[step.state]')
    expect(at, 'ต้องมีข้อความสำหรับ screen reader').toBeGreaterThan(-1)
    const block = src.slice(Math.max(0, at - 900), at)
    expect(block, "ต้องเป็น width: '1px'").toMatch(/width: '1px'/)
    expect(block, "ต้องเป็น height: '1px'").toMatch(/height: '1px'/)
    expect(block, 'ห้ามเป็นเลข 1 เปล่า ๆ — MUI จะแปลงเป็น 100%').not.toMatch(/(width|height): 1,/)
  })

  it("ห้ามมี width/height เป็นเลข 1 ที่ไหนในไฟล์นี้", () => {
    /* ค่าเดียวที่ MUI ตีความกลับด้านกับที่คนเขียนตั้งใจเสมอ — กันไว้ทั้งไฟล์
       (ถ้าจงใจอยาก 100% ให้เขียน '100%' ตรง ๆ จะได้อ่านออกว่าตั้งใจ) */
    expect(src, 'ห้าม width: 1,').not.toMatch(/\bwidth: 1,/)
    expect(src, 'ห้าม height: 1,').not.toMatch(/\bheight: 1,/)
  })

  it('หน้าต้องกันการเลื่อนแนวนอนด้วย clip ไม่ใช่ hidden', () => {
    /* 🛑 `hidden` = "ไม่แสดงแถบเลื่อน" ไม่ใช่ "ห้ามเลื่อน" — กล่องยังเป็น scroll container
       ที่เลื่อนด้วยนิ้ว/trackpad/สคริปต์ได้ · และมันบังคับ `overflow-y` เป็น `auto` ด้วย
       `clip` ตัดจริงและไม่สร้าง scroll container */
    expect(src, "ต้องใช้ overflowX: 'clip'").toMatch(/overflowX: 'clip'/)
    expect(src, "ห้ามกลับไปใช้ hidden").not.toMatch(/overflowX: 'hidden'/)
  })
})
