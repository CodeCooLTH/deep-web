/**
 * ระยะขอบในของการ์ดบนหน้าออเดอร์ `/o/[token]` — **นิยามเดียวทุกใบ**
 *
 * 🛑 มีไฟล์นี้เพราะแต่ละการ์ดเคยตั้งค่าเอง แล้วไม่มีใบไหนตรงกันเลยสักคู่
 * (หัวหน้าทัก 2026-08-30: "padding ไม่เท่ากัน เช็คทุก card เลยนะ
 * ไม่ได้ทำ component หรอแล้วเรียกใช้อ่ะ") · วัดจากเบราว์เซอร์จริงได้:
 *
 *   /dashboard (ธีมอ้างอิง)  ทุกใบ 24/24
 *   /o/[token]  รายละเอียดคำสั่ง 6/18 · รายการบริการ 6/7 · ช่วยเหลือ 7/7
 *               แนบสลิป 9/7 · ช่องทางชำระเงิน 8/7 · รีวิว 8/7 · trust 8/7
 *               นัดหมาย 24/24  ← ใบเดียวที่ตรงกับระบบ
 *
 * ค่าที่ใช้ = **24px** ตามที่ `/dashboard`, `/orders` และการ์ดนัดหมายใช้อยู่แล้ว
 * (= `spacing(6)` ของธีมนี้ซึ่งฐาน 4px · ตรงกับค่าตั้งต้นของ `CardContent` ใน MUI ด้วย)
 *
 * 🛑 ห้ามเขียน `px:`/`py:`/`p:` ใหม่ที่ตัวการ์ด — spread ตัวนี้แล้วเติมเฉพาะสิ่งที่ต่างจริง
 * (เช่น `display: 'flex'`) มีเทส `[blocker]` กันไว้
 */
import type { SxProps, Theme } from '@mui/material/styles'

/** ระยะขอบในมาตรฐานของการ์ด — 24px รอบด้าน */
export const cardBodySx = { p: 6 } as const satisfies SxProps<Theme>

/**
 * ระยะขอบ **แนวนอน** สำหรับบล็อกที่ซ้อนกันหลายชั้นในการ์ดใบเดียว (หัวออเดอร์ · ราง · หัวร้าน)
 * — ใบพวกนั้นแบ่งเป็นหลายบล็อกที่คุมระยะบน-ล่างเอง แต่ขอบซ้าย-ขวาต้องตรงกับการ์ดใบอื่น
 */
export const cardInlinePadSx = { px: 6 } as const satisfies SxProps<Theme>

/**
 * ระยะขอบในของ **กล่องข้อมูลที่ซ้อนอยู่ในการ์ด** (แถบคำอธิบาย · กล่องเตือน · แถบสถานะ)
 *
 * 🛑 วัดจากเบราว์เซอร์จริง 2026-08-30 ได้ `5px 6px` ซึ่งแน่นจนตัวหนังสือเกือบชนขอบ
 * และไม่เข้าคู่กับการ์ดที่ครอบมันซึ่งเว้น 24px รอบด้าน — กล่องในควรเว้นราวครึ่งหนึ่งของ
 * กล่องนอก (12px) จึงอ่านเป็นชั้นที่ซ้อนกันอย่างตั้งใจ ไม่ใช่กล่องที่ถูกบีบ
 *
 * ค่าเดียวกันทุกกล่อง — เหตุผลเดียวกับ `cardBodySx`: ปัญหาคือไม่มีที่เดียวที่ตัวเลขอยู่
 */
export const infoBoxSx = { px: 3, py: 2.5 } as const satisfies SxProps<Theme>
