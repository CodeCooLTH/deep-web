import type { SxProps, Theme } from '@mui/material/styles'

/**
 * ความกว้างของคอลัมน์เนื้อหาบนหน้าออเดอร์ `/o/[token]` — จุดเดียวทั้ง 4 ที่ที่ใช้
 * (คอลัมน์เนื้อหา + แถบ CTA ล่างจอ ของทั้งจอ guest และจอหลังล็อกอิน)
 *
 * 🛑 **ค่าเดิมเขียนเป็น key ในอ็อบเจกต์ของ `maxWidth` ซึ่ง MUI ทิ้งทั้งคู่ — เพดาน 720
 * จึงไม่เคยมีผลจริงเลยสักจอ** ตรวจด้วยการรัน `styleFunctionSx` กับธีมจริง ไม่ใช่อ่านโค้ดเดา:
 *
 *   { xs: '100%', '@media (min-width:768px)': 720, lg: 880 }
 *     → { '@media (min-width:0px)': { maxWidth: '100%' },
 *         '@media (min-width:1200px)': { maxWidth: 880 },
 *         '@media (min-width:768px)': 720 }     ← ค่าเปล่า ไม่ใช่ declaration = CSS เสีย
 *
 *   { xs: '100%', 'min-[768px]': 720 }
 *     → { '@media (min-width:0px)': { maxWidth: '100%' },
 *         'min-[768px]': 720 }                  ← ชื่อ property ที่ไม่มีอยู่จริง
 *
 * กลไก: `iterateBreakpoints` เอา key ไปเทียบกับ **breakpoint ของธีม** (xs/sm/md/lg/xl)
 * key ที่ไม่ตรงจะถูกโยนลง output ดิบ ๆ โดยไม่ผ่านตัวประกอบ declaration (และไม่มี error
 * ไม่มี warning) ⇒ จอ guest กว้างเต็มจอทุกขนาด · จอหลังล็อกอินกว้างเต็มจอตั้งแต่ 768–1199
 * แล้วค่อยหดเป็น 880 ที่ 1200 ซึ่งเป็นอาการที่ FR-018 ตั้งใจแก้ตั้งแต่แรกแต่แก้ไม่ติด
 *
 * 768 ไม่ใช่ breakpoint ของธีมนี้ (Vuexy = MUI default: sm 600 / md 900 / lg 1200) จึงต้อง
 * เขียนเป็น media query ที่ระดับบนสุดของ sx ไม่ใช่เป็น key ในค่าของ property
 */
export const orderContentWidthSx: SxProps<Theme> = {
  maxWidth: '100%',
  marginInline: 'auto',
  // 768 = จุดที่คอลัมน์เดียวเริ่มยาวเกินระยะกวาดตาบนแท็บเล็ต
  '@media (min-width:768px)': { maxWidth: 720 },
  // 1200 = `lg` ของธีม — จอเดสก์ท็อปกว้างพอให้เนื้อหาหายใจได้อีกขั้นโดยยังไม่ยาวเกินอ่าน
  '@media (min-width:1200px)': { maxWidth: 880 },
}

/**
 * จุดที่หน้า **รายละเอียดออเดอร์** เปลี่ยนเป็น 2 คอลัมน์ — ตามม็อกอัพ v3 ที่หัวหน้าส่งมา
 * (`@media(max-width:860px){ .content-grid{grid-template-columns:1fr} }`)
 *
 * เขียนเป็น min-width จึงเป็น 861 ไม่ใช่ 860 — ที่ 860 พอดีต้องยังเป็นคอลัมน์เดียว
 *
 * 🛑 ไม่ใช่ breakpoint ของธีม (Vuexy = sm 600 / md 900 / lg 1200) ⇒ ต้องเขียนเป็น
 * media query ที่ระดับบนสุดของ sx เท่านั้น ห้ามเป็น key ในค่าของ property
 * (เหตุผลเดียวกับ 768 ด้านบน — MUI ทิ้ง key ที่ไม่ตรง breakpoint เงียบ ๆ)
 */
export const ORDER_TWO_COL_MQ = '@media (min-width:861px)'

/**
 * ความกว้างของ **หน้ารายละเอียดออเดอร์หลังยืนยันตัวตน** ซึ่งเป็นจอเดียวที่เป็น 2 คอลัมน์
 *
 * ต่างจาก `orderContentWidthSx` ตรงเพดานตอนจอกว้าง: 1200 = `--max` ของม็อกอัพตรงตัว
 * (คอลัมน์ขวา 330 + ซ้ายที่เหลือ + gap 18) — จอ guest/จอกั้นยังใช้ตัวเดิมซึ่งเป็น
 * คอลัมน์เดียว และไม่ควรกว้างตาม เพราะบรรทัดจะยาวเกินระยะกวาดตา
 */
export const orderDetailWidthSx: SxProps<Theme> = {
  maxWidth: '100%',
  marginInline: 'auto',
  '@media (min-width:768px)': { maxWidth: 720 },
  /* 🛑 **ตั้งไว้ที่ตัว `[ORDER_TWO_COL_MQ]` เลย ไม่ใช่เขียน `@media (min-width:1200px)`
     เพิ่มทีหลัง** — ลองแล้วมันไม่ชนะ (วัดจากเบราว์เซอร์จริงยังได้ค่าเดิม) และช่วง 861–1199
     เพดานนี้ไม่เคยผูกอยู่แล้วเพราะจอแคบกว่านั้น ⇒ ตั้งค่าเดียวจบ ไม่ต้องพึ่งลำดับของ
     media query ที่พิสูจน์แล้วว่าเชื่อไม่ได้ */
  [ORDER_TWO_COL_MQ]: { maxWidth: 1152 },
  /**
   * 🛑 จอ ≥1920 ขยายอีกขั้น — เลขชุดเดียวกับคอนเทนต์ของ `/settings/profile`
   * (`min-[1920px]:max-w-[calc(1440px-48px)]`) ซึ่งเป็นธีมที่หัวหน้าให้ยึด
   *
   * (เคยเขียนเหตุผลไว้ว่า "ให้ตรงกับแถบเมนูของ `FrontLayout`" — **ใช้ไม่ได้แล้ว**
   * เพราะหัวหน้าสั่งถอด shell ออกจากหน้านี้ 2026-08-30 · เลขยังคงไว้เพราะเป็นความกว้าง
   * ที่พิสูจน์แล้วว่าอ่านสบายบนจอกว้าง ไม่ใช่เพราะต้องเรียงกับอะไร)
   */
  '@media (min-width:1920px)': { maxWidth: 1392 },
}
