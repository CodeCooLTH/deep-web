/**
 * sales-chart-axis — กติกา "ป้ายวันบนแกน x" ที่การ์ดยอดขาย (SalesChartCard) และชีตเต็มจอ
 * (SalesChartSheet) ใช้ร่วมกัน
 *
 * แยกออกมาเป็นไฟล์กลางเพราะ SalesChartCard import SalesChartSheet อยู่แล้ว — ถ้าชีตย้อนไป
 * import ฟังก์ชันจากการ์ดจะเป็น circular import. ตัวไฟล์นี้เป็น pure module (ไม่มี React/prisma)
 * จึงถูก import จากทั้งสองฝั่งได้ปลอดภัย
 *
 * ที่มาของกติกา (ย้ายมาจาก SalesChartCard.tsx ไม่ได้คิดใหม่):
 * ทำไมไม่โชว์ป้ายครบ 31 ตัว — เคยลองแล้ว (2026-08-05) ต้องหมุน -60° + ย่อเหลือ 9px ถึงจะไม่ทับกัน
 * ผลคืออ่านไม่ออกต้องเอียงคอ และป้ายเอียงยังกินความสูงของพื้นที่วาดจนแท่งวันที่ยอดน้อยหายไปทั้งแท่ง
 * (user: "ตรงตัวเลขวันที่ ... มันดูยากมาก")
 */

/** หมุดกลางบนแกน x — ทุก 5 วัน (หัวเดือน/ท้ายเดือน/วันนี้ เติมให้ใน axisAnchorDays) */
const AXIS_ANCHOR_DAYS = [5, 10, 15, 20, 25]

/**
 * วันที่ที่จะโชว์ตัวเลขบนแกน x — แท่งยังครบทุกวันเหมือนเดิม โชว์ป้ายเฉพาะหมุด
 *
 * กติกาหมุด: หัวเดือน + ท้ายเดือนเสมอ (ปักกรอบของช่วง) + ทุก 5 วัน + วันนี้
 * หมุดกลางที่อยู่ใกล้วันนี้ไม่ถึง 3 วันถูกตัดทิ้ง แล้วให้วันนี้ยืนแทน — 31 ช่องบนจอมือถือ ~330px
 * = ช่องละ ~10.6px ส่วนเลข 2 หลักที่ 10px กว้าง ~13px จึงต้องห่างกัน ≥3 ช่องถึงจะไม่เบียด
 * (ไล่เทสครบเดือน 28/30/31 วัน × วันนี้ต้นถึงปลายเดือน: ระยะห่างน้อยสุดที่เกิดขึ้นได้ = 3 ช่อง)
 *
 * @param bucketCount จำนวนวันของเดือนนั้น
 * @param today วันที่ของ "วันนี้" (1-based); ส่ง bucketCount เมื่อเดือนนั้นจบไปแล้ว = ไม่มีวันนี้
 */
export function axisAnchorDays(bucketCount: number, today: number): Set<number> {
  const mid = AXIS_ANCHOR_DAYS.filter((d) => d < bucketCount && Math.abs(d - today) >= 3)
  // วันนี้ที่ชิดหัว/ท้ายเดือนไม่ต้องเติม — ป้ายหัวเดือน/ท้ายเดือนยืนแทนอยู่แล้ว
  const withToday = Math.abs(today - 1) >= 3 && Math.abs(today - bucketCount) >= 3 ? [today] : []
  return new Set([1, bucketCount, ...mid, ...withToday])
}
