import logoDeep from '@/assets/images/logo-deep-wordmark.png'
import Link from 'next/link'

/**
 * AuthLogo — โลโก้บนหน้า login / สมัคร / ลืมรหัสผ่าน ของผู้ขาย
 *
 * ## 🛑 เดิมเป็นโลโก้ "Paces" ซึ่งเป็นแบรนด์ของ **ธีม** ไม่ใช่ของเรา
 *
 * `logo.png` / `logo-black.png` ลงวันที่เดียวกับตอนก็อปธีมเข้ามา (14 มิ.ย.) แล้วไม่มีใครเปลี่ยน
 * ⇒ หน้าจอแรกที่ผู้ใช้ทุกคน (และทีมรีวิวของ Apple) เห็น เป็นชื่อของบริษัทอื่นมาตลอด
 *
 * ผู้ใช้เดินเส้นทางนี้: ไอคอนแอปบนหน้าโฮม → เปิดแอป → **หน้านี้** — แบรนด์ต้องต่อเนื่องกัน
 *
 * ## ขนาดคุมที่ CSS ไม่ใช่ที่นี่
 *
 * `.auth-logo img` (`pages/_auth.css`) เป็น CSS ที่ไม่ได้ห่อ `@layer` จึงชนะ utility เสมอ
 * — ดูเหตุผลเต็มที่คอมเมนต์ในไฟล์นั้น
 *
 * ไฟล์ที่ใช้เป็นตัว **ตัดขอบขาวออกแล้ว** (`logo-deep-wordmark.png` 1628×479) ต้นฉบับที่
 * user ส่งมา (`logo-deep-app.png` 2172×724) มีขอบขาวเปล่ากินความสูงไป 39% ⇒ ตั้งความสูง
 * เท่าไรก็เห็นตัวอักษรจริงแค่ 61% ของนั้น (วัดทีละพิกเซลแล้ว: เนื้ออยู่ y 151..589)
 * — ต้นฉบับยังอยู่ในโฟลเดอร์ ไม่ได้ลบ
 *
 * ## ทำไมรูปเดียวใช้ทั้งสองโหมด
 *
 * ไฟล์นี้เป็น PNG **ทึบ พื้นขาว** (`hasAlpha: no`, มุมบนซ้าย #fefefe) ตัวหนังสือกรมท่า
 * ⇒ โหมดสว่างกลืนกับการ์ดพอดี · โหมดมืดจะเห็นเป็นแผ่นขาวรองโลโก้ ซึ่ง **อ่านออกและถูกแบรนด์**
 * ต่างจากของเดิมที่โหมดหนึ่งถูกอีกโหมดเป็นชื่อบริษัทอื่น
 *
 * อยากได้โหมดมืดที่ไม่มีแผ่นขาว → ต้องมีไฟล์ตัวหนังสือขาวพื้นโปร่งเพิ่ม แล้วค่อยแยก 2 รูป
 * (ทำจากไฟล์นี้ไม่ได้ เพราะพื้นถูกเบิร์นมากับรูปแล้ว)
 */
const AuthLogo = () => {
  return (
    <Link href="/" className="auth-logo">
      {/* 🛑 ห้ามใส่ `h-*` ที่นี่ — `.auth-logo img` ใน `pages/_auth.css` เป็น CSS ที่ไม่อยู่ใน
          layer ⇒ ชนะ utility เสมอ ใส่ไปก็ไม่มีผลและจะเข้าใจผิดว่าคุมขนาดได้จากตรงนี้
          (พลาดมาแล้ว 2026-08-19 — ใส่ h-12 แต่ของจริงเป็น h-10 ตามที่ CSS สั่ง)
          ขนาดอยู่ที่ `_auth.css` ที่เดียว */}
      <img src={logoDeep.src} alt="Deep" />
    </Link>
  )
}

export default AuthLogo
